# Aquarium 개발 지침

## 제품과 범위

- 사용자와 한국어로 소통한다.
- `docs/PRD.md`, `docs/SRS.md`, 설계 문서, `docs/TASK.md`를 먼저 읽는다. 사용자 후속 지시가 문서보다 우선한다.
- M5 맥북용 Unreal 앱이 우선이다. 승인 없이 웹 앱으로 대체하지 않는다.
- 실사에 가까운 산호초 바다와 자연스러운 유영이 최우선이다. 기능 통과와 그래픽 승인 상태를 구분한다.
- 무료 에셋만 사용한다. 파일별 출처·라이선스·원본 재배포 조건을 `docs/ASSETS.md`에 남긴다.
- 학생 이름을 원격 서버, 분석 도구 또는 로그에 전송하지 않는다. 초기 버전은 세션 내 별명만 사용한다.

## 작업 방식

- 요청된 Superpowers 스킬의 최신 설치본을 읽고 적용한다. 현재 설치 위치는 `~/.codex/skills/`이며 다른 머신에서는 설치 여부부터 확인한다.
- 구현·버그 수정은 TDD: 테스트 작성 → 의도한 동작 실패 확인 → 최소 구현 → 전체 관련 테스트 통과 → 리팩터링.
- 실패 원인과 RED/GREEN 명령·결과를 구현 기록에 남긴다. 파일 존재 검사나 소스 문자열 검사로 실제 동작 테스트를 대체하지 않는다.
- 순수 C++ 규칙 테스트, Unreal 연동 테스트, 실제 화면의 시각 검증을 분리한다. 순수 C++ 테스트 통과를 Unreal 빌드 성공으로 보고하지 않는다.
- 그래픽·애니메이션은 실제 렌더링 결과와 영상/프레임 측정으로 검증한다. 수행하지 않은 검증은 미실행으로 적는다.
- 읽지 않은 스킬, 연결되지 않은 MCP, 미설치 프로그램을 사용했다고 말하지 않는다.
- MCP가 없어도 공식 CLI·Python API 등으로 가능한 작업을 수행할 수 있다. 설치 및 접근 권한은 실행 환경의 승인 절차를 따른다.

## Git과 산출물

- 원격 저장소: `https://github.com/Hansawsome/aquarium`.
- 기존 변경을 먼저 확인하고 보존한다. 기능별 브랜치를 사용하며 의미 있는 단위로 커밋·푸시한다.
- 비밀정보, 학생 입력, 개인 기기 식별자, Unreal Engine 소스, 빌드 캐시는 커밋하지 않는다.
- 대형 바이너리는 실제 도입 시 Git LFS와 저장 용량을 확인한다. 재배포 불가 원본은 공개 저장소에 넣지 않는다.
- 완료 보고에는 바뀐 내용, 실제 검증 결과, 남은 제약을 포함한다.
