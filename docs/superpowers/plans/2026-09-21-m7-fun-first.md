# M7 — 재미 1차(소리·반응) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `docs/scenario.md`가 합의한 1차 묶음 네 장면을 만든다 — **① 소리 ② 클릭 과장 ③ 빈물 먹이 ④ 무리 갈라지기 과장.** 규칙 103 → **139**, Automation 60 → **82**.

**Architecture:** 새 모드를 얹지 않는다. 이미 있는 것(36마리·조종·클릭 반응·예쁜 바다)의 볼륨을 올린다.

- **소리**: MetaSound·SoundCue 그래프는 에디터 Python으로 만들 수 없다(M4b에서 Niagara로 부딪힌 것과 같은 벽). 따라서 **평범한 `USoundWave` + C++ 재생**만 쓴다. 반응음은 `assets/audio/make_sounds.py`(**순수 표준 라이브러리** `wave`/`array`/`math`)가 WAV로 합성하고 — 생성 스크립트 자체가 출처다 — 앰비언스만 CC0 녹음이다. 소리는 전부 **2D**(`SpawnSound2D`)다: 카메라가 고정이라 감쇠 에셋이 아예 필요 없다. 이것이 `SoundAttenuation` 에셋을 만들지 않는 이유다.
- **먹이·기포·분리 감지**: 전부 **규칙 계층의 순수 함수 + Catch2**로 들어간다(`Food.h`, `Bubbles.h`, `Reaction.h`, `Audio.h`, `Split.h`). 엔진은 위치를 넣고 조향 벡터를 받는다. `Boids.h`·`Obstacles.h`와 같은 모양이다.
- **규칙 순서**는 M4c/M5가 고정한 것에 **먹이 한 칸만** 끼운다:
  `입력/Wander → **먹이** → 도망 → 무리 → 장애물 → 경계 → StepMotion → Clamp`
  먹이는 "어디로 가고 싶은가"의 답을 **대체**하므로 입력/Wander와 같은 층이고, 도망보다 **앞**이다(놀란 물고기가 먹이에 끌려 되돌아오면 아이가 클릭의 결과를 읽지 못한다 — M5가 무리를 도망 뒤로 보낸 것과 같은 이유). **경계는 끝까지 마지막이다.** M3의 "어떤 화면 비율에서도 영역을 벗어나지 않는다" 보장이 여기에 걸려 있고 M4c 설계가 그 이유를 기록해 두었다.
- **액터 틱 없음**: 먹이 알갱이와 기포는 액터가 아니라 **월드 서브시스템이 소유한 POD 배열**이고, 그리기는 `UInstancedStaticMeshComponent` **하나**가 한다. 먹이 1000개가 떠 있어도 틱은 1개다. 이것이 "자원 제한 없음"(시나리오 금지 사항)을 비용 없이 지키는 방법이다.
- **레벨은 건드리지 않는다.** `ReefM1.umap`은 M7에서 **한 바이트도 바뀌지 않는다**(먹이·기포는 런타임 스폰). 따라서 `verify_scene.py`의 `SCENE_OK actors=69 fish=36 props=22 ...` 기대값도 그대로다.

**Tech Stack:** C++17 규칙 계층 + Catch2, Unreal 5.8.2(C++20 모듈 + 에디터 Python), Python 3(표준 라이브러리만), ffmpeg.

**근거 문서:** [`docs/scenario.md`](../../scenario.md) — 결정표·장면·금지 사항·검증표가 구속력을 갖는다. 이 계획은 그 결정을 다시 논의하지 않고 태스크로 옮긴다.

**공통 명령**:
- 규칙 테스트: `cd /Users/hans/dev/aquarium && cmake -S . -B build && cmake --build build -j && ctest --test-dir build`
- UE 빌드(**반드시 두 번**): `"/Users/Shared/Epic Games/UE_5.8/Engine/Build/BatchFiles/Mac/Build.sh" AquariumEditor Mac Development -Project="$PWD/unreal/Aquarium/Aquarium.uproject" -WaitMutex`
- Automation: `"/Users/Shared/Epic Games/UE_5.8/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" -ExecCmds="Automation RunTests Aquarium; Quit" -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -E "Test Completed|Tests Failed"`
- 에디터 Python: `"/Users/Shared/Epic Games/UE_5.8/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" -run=pythonscript -script="$PWD/unreal/Aquarium/Scripts/<이름>.py" -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -E "_OK|Traceback"`

---

## 모든 태스크에 걸리는 규약 열 가지 (전부 이 프로젝트가 실제로 대가를 치른 것)

1. **이 ctest는 `100% tests passed out of N` 형식을 쓴다.** `"0 tests failed out of"`를 grep하면 **아무것도 검증하지 않은 채 통과한다.**
2. **규칙 테스트 mtime 함정** — 같은 초 안에 다시 빌드하면 `make`가 재컴파일을 건너뛰고 ctest가 **낡은 결과**를 보고한다. RED 확인 전에는 반드시 `touch`로 mtime을 올린다(각 RED 스텝에 명령으로 박아 두었다).
3. **UE 모듈은 자동화 전에 두 번 빌드한다.** UBT가 `UnrealEditor.modules`를 한 빌드 늦게 쓴다.
4. **에디터 Python은 `-FullStdOutLogOutput` 없이 돌리면 아무것도 출력하지 않는다** — `AUDIO_OK`도, `AssertionError`도. 그런데 에셋은 덮어쓴다. **종료 코드는 무관한 `GameFeatureData` 오류 때문에 항상 1이다. 판정은 `*_OK` grep으로만 한다.**
5. **머티리얼은 조용히 실패하고 회색 기본 머티리얼로 떨어지면서 에디터 뷰포트에서는 멀쩡해 보인다.** 캡처 실행 로그에서 `Failed to compile Material` 0건을 grep한다. **`-nullrhi`가 아닌 실제 게임 실행 로그여야 한다.**
6. **테스트는 아무것도 검사하지 않으면서 통과할 수 있다.** `PropMaterialsCompile`은 Success를 내면서 `checked 0 of 13`을 찍었고, `SchoolMatesPullTogether`는 무리를 완전히 끈 채로 통과했으며, `UpVectorStaysUpright`는 구조상 빨간불이 될 수 없었고, `FleeDoesNotSlide`는 물고기가 직선으로 헤엄치는 동안 초록불이었으며, M5 캡처 하네스는 **계획한 클릭 7개가 전부 빈 물을 맞혔는데도** 경고 0건에 그럴듯한 클립을 냈고, M6의 개발 플래그 스캔은 UTF-16 문자열을 못 봐서 대조군이 0/11을 읽었다. **이 계획의 모든 신규 테스트는 변이로 빨간불을 먼저 확인한다. 제안한 변이로 빨간불이 켜지지 않으면 그것은 통과의 증거가 아니다 — 빨간불이 켜지는 변이를 찾거나, 찾지 못했다는 사실을 후속 항목에 적는다.**
7. **같은 규칙을 두 군데 두면 검증이 결함을 영원히 못 잡는다(파생하고 베끼지 않는다).** 기포가 사라지는 높이는 물고기 유영 영역에서 **파생**하고, 분리 감지 반경은 `BoidsParams::avoidOnlyRadius`에서 **파생**하며, 테스트 기대값은 규칙 계층 파라미터에서 **파생**한다. 숫자를 리터럴로 베끼지 않는다.
8. **개발 플래그는 값을 받지 않는 불리언만 새로 만든다.** `-AquariumAutoInput`(`<방향문자><초>`, R/L/U/D/0, 콜론 없음, 대각선 없음)과 `-AquariumAutoClick`(`<초>@<nx>x<ny>`)의 파서는 **알아볼 수 없는 토큰에 경고만 찍고 조용히 무시한다.** M7이 새로 만드는 `-AquariumNoAudio`·`-AquariumNoFood`·`-AquariumMuted`는 전부 `FParse::Param` 불리언이라 **새 문법이 없고 따라서 새 무음 드롭 경로도 없다.** 값을 받는 플래그는 `-AquariumFxLog=<경로>` 하나뿐이고, 이것은 파싱 실패가 곧 "파일 없음"으로 드러난다. 캡처 하네스는 여전히 기존 두 파서의 **경고 0건과 `armed N`을 단언한다.**
9. **계획의 오류는 모든 마일스톤에서 하나씩 나왔다** — 틀린 기대 HEAD, 물리적으로 불가능한 테스트 기대값, 존재하지 않는 플래그 토큰. **전제는 믿지 말고 확인한다.** 아래 태스크마다 "전제 확인" 스텝이 있고, 어긋나면 진행하지 말고 보고한다. 특히 **테스트 개수(139/80)는 계획의 산수일 뿐이다.** 매 태스크에서 ctest/Automation이 실제로 찍은 숫자를 근거로 삼고, 누적이 어긋나면 멈추고 보고한다.
10. **소리는 헤드리스로 들을 수 없다.** 자동 검증이 닿는 한계는 "재생 호출이 일어났고 그 컴포넌트에 기대한 `USoundWave`가 꽂혔다"와 "파형 생성이 결정적이다"까지다. 실제로 제대로 들리는지는 **사용자 수동 확인 두 건**이며 Task 18에서 인계한다. 테스트가 "소리가 났다"를 주장하게 쓰지 않는다.

---

### Task 0: 전제 확인과 기준 보관

**Files:** 없음 (읽기 전용)

- [ ] **Step 1: 브랜치·작업 트리·HEAD 확인**

```bash
cd /Users/hans/dev/aquarium
git status --short && git rev-parse --abbrev-ref HEAD && git rev-parse --short HEAD
```
기대: 첫 줄 출력 없음, `main`, `5283593`. **HEAD가 다르면 멈추고 보고한다**(계획서가 지어낸 HEAD로 진행한 전례가 있다). 작업 브랜치를 새로 만든다:

```bash
cd /Users/hans/dev/aquarium && git switch -c feat/m7-fun-first && git rev-parse --abbrev-ref HEAD
```
기대: `feat/m7-fun-first`.

- [ ] **Step 2: 현재 규칙 103개 통과 확인**

```bash
cd /Users/hans/dev/aquarium && cmake -S . -B build && cmake --build build -j 2>&1 | tail -3 && ctest --test-dir build 2>&1 | tail -3
```
기대: `100% tests passed out of 103`. (규약 1 — `0 tests failed out of`를 찾지 말 것.)

- [ ] **Step 3: 현재 Automation 60개 통과 확인 (빌드 두 번)**

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
for p in 1 2; do "$UE/Engine/Build/BatchFiles/Mac/Build.sh" AquariumEditor Mac Development -Project="$PWD/unreal/Aquarium/Aquarium.uproject" -WaitMutex 2>&1 | grep -E "^Result:|error:"; done
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" -ExecCmds="Automation RunTests Aquarium; Quit" -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -cE "Test Completed.*Success"
```
기대: `Result: Succeeded` 두 번, 마지막 줄 `60`.

- [ ] **Step 4: 소리가 정말로 하나도 없는지 확인(이 계획의 핵심 전제)**

```bash
cd /Users/hans/dev/aquarium && grep -rn "SoundWave\|AudioComponent\|SpawnSound\|PlaySound" unreal/Aquarium/Source/ | wc -l && ls assets/audio 2>&1 | head -1
```
기대: `0`, 그리고 `assets/audio` 없음. 0이 아니면 전제가 틀린 것이므로 진행하지 말고 보고한다.

- [ ] **Step 5: `UTickableWorldSubsystem`이 실재하는지 확인** — 먹이·기포 서브시스템이 이것을 상속한다.

```bash
grep -rn "class ENGINE_API UTickableWorldSubsystem\|class UTickableWorldSubsystem" "/Users/Shared/Epic Games/UE_5.8/Engine/Source/Runtime/Engine/Classes/Subsystems/WorldSubsystem.h"
```
기대: 한 줄 이상 일치. **일치가 없으면** 대신 `FTickableGameObject` 다중 상속으로 간다(그 경우 Task 10·12의 헤더에서 기반 클래스만 바꾸고 `Tick`/`GetStatId`/`IsTickable`을 직접 구현한다). 어느 쪽을 택했는지 후속 항목에 적는다.

- [ ] **Step 6: 파이썬 환경 확인 — numpy는 없다(확인 완료). 표준 라이브러리만 쓴다.**

```bash
python3 -c "import wave, array, math, struct, zlib, hashlib; print('stdlib ok', __import__('sys').version.split()[0])"
python3 -c "import numpy" 2>&1 | tail -1
```
기대: 첫 줄 `stdlib ok 3.x`, 둘째 줄 `ModuleNotFoundError: No module named 'numpy'`. **numpy가 오히려 있다고 나와도 쓰지 않는다** — 이 계획의 모든 생성 스크립트는 표준 라이브러리만으로 쓰여 있고, 의존성을 늘리지 않는 것이 결정이다.

- [ ] **Step 7: 기준 산출물 보관**

```bash
B=/private/tmp/claude-501/-Users-hans-dev-aquarium/670aff5b-0935-4cef-86d7-e73d4193a3f3/scratchpad/m7_before
mkdir -p "$B"
cd /Users/hans/dev/aquarium
cp docs/reviews/2026-09-21-m5-click.mp4 "$B/before-click.mp4"
cp docs/reviews/2026-09-21-m5-flee.png "$B/before-flee.png"
cp docs/reviews/2026-09-21-m5-perf.md "$B/before-perf.md"
ls -l "$B"
```

- [ ] **Step 8: 개선 전 클립을 직접 본다** — `$B/before-click.mp4`. 확인할 것: (a) **완전 무음이다**, (b) 클릭당 물고기 한 마리가 "비켰다"로 보이는 정도, (c) 빈 물 클릭이 아무 일도 안 한다. 뒤에서 "놀랐다로 보인다", "빈 물이 먹이가 됐다"를 주장할 때의 기준이다. **열어 보지 않고 다음으로 가지 않는다.**

- [ ] **Step 9: 「나가기」 버튼의 현재 위치·크기 확인** — 시나리오 "제약과 위험"이 요구하는 유일한 확인 작업이다(바꾸는 것이 아니라 확인).

```bash
cd /Users/hans/dev/aquarium && sed -n '/ButtonSlot->SetAnchors/,/SetAutoSize/p' unreal/Aquarium/Source/Aquarium/HudWidget.cpp && grep -n "kEdgeMargin\|kLabelFontSize\|kLabelPadding" unreal/Aquarium/Source/Aquarium/HudWidget.cpp
```
기대: 앵커 `(1,0)`(우상단), 오프셋 `-16, 16`, 자동 크기, 폰트 20. **판정**: 구석의 작은 버튼이라는 M2 설계가 유지되고 있으면 이번 마일스톤에서 건드리지 않는다. 다르면(예: 화면 중앙·대형) 후속 항목에 적고 사용자에게 보고한다. 결과를 Task 18의 요약에 한 줄로 남긴다.

**커밋 없음** (읽기 전용 태스크).

---

### Task 1: 반응음 합성 스크립트 (`assets/audio/make_sounds.py`)

**Files:** Create `assets/audio/make_sounds.py`, Create `assets/audio/.gitignore` 없음(WAV는 커밋한다)

시나리오 결정: **반응 소리는 전부 파이썬 합성이고 출처는 우리 코드다.** 실제 물고기 놀라는 소리는 세상에 없으므로 사실성을 따라가면 "무음이 정답"이 되어 버린다. 합성이면 재현 가능하고, 무엇보다 **누를 때마다 음높이를 흔들 수 있다.**

다섯 개를 만든다. 이름은 이후 모든 태스크에서 이대로 쓴다.

| 파일 | 역할 | 길이 | 루프 |
|---|---|---|---|
| `S_Swim.wav` | 헤엄 물살 「푸슝」 — 속도에 따라 음높이가 변한다 | 1.000 s | **예** |
| `S_Startle.wav` | 물고기가 놀람 「꺅」 | 0.220 s | 아니오 |
| `S_Nibble.wav` | 먹이를 먹음 「뽁」 | 0.120 s | 아니오 |
| `S_Split.wav` | 무리가 갈라짐 「촤악」 | 0.500 s | 아니오 |
| `S_Bubble.wav` | 내 물고기 재롱 「뽀글」 | 0.300 s | 아니오 |

「꺅」과 「뽁」은 **시나리오 장면 3의 조건 1** 때문에 서로 확실히 달라야 한다(아이가 0.1초 만에 물고기를 맞혔는지 빈 물을 맞혔는지 소리로 안다). 그래서 둘을 **다른 축 두 개**로 벌려 놓았다 — 「꺅」은 위로 미끄러지는 **연속음**(0.22 s, 상승 글라이드), 「뽁」은 **한 번 톡 치는 타격음**(0.12 s, 즉시 감쇠, 저역 중심). 길이·포락선·주파수 방향이 전부 반대라서 스피커가 나빠도 구분된다.

- [ ] **Step 1: 스크립트를 만든다**

`assets/audio/make_sounds.py`:

```python
#!/usr/bin/env python3
# 반응음 다섯 개를 WAV로 합성한다. 이 스크립트 자체가 출처다(docs/ASSETS.md 참고).
#
# 의존성: 파이썬 표준 라이브러리뿐이다. numpy를 쓰지 않는다 -- 이 기계의 system
# python3에도 UE 번들 python3에도 numpy가 없고(2026-09-21 확인), 소리 다섯 개
# 때문에 의존성을 늘리는 것은 재현성 제1원칙에 손해다.
#
# 결정성: 난수를 쓰되 반드시 고정 시드의 random.Random 인스턴스만 쓴다.
# 전역 random 모듈을 쓰면 실행 순서에 따라 파형이 달라진다.
#
# 실행:  python3 assets/audio/make_sounds.py
# 판정:  마지막 SOUNDS_OK 줄. 종료 코드가 아니라 그 줄을 본다.
import array
import hashlib
import math
import os
import random
import struct
import sys
import wave

RATE = 44100
HERE = os.path.dirname(os.path.abspath(__file__))


def clamp(v, lo=-1.0, hi=1.0):
    return lo if v < lo else (hi if v > hi else v)


def write_wav(path, samples, rate=RATE):
    """16비트 모노 PCM. 피크를 0.89로 정규화해 어떤 소리도 클리핑하지 않게 한다."""
    peak = max(abs(s) for s in samples) if samples else 0.0
    gain = (0.89 / peak) if peak > 1e-9 else 0.0
    data = array.array("h", (int(round(clamp(s * gain) * 32767.0)) for s in samples))
    if sys.byteorder == "big":
        data.byteswap()
    with wave.open(path, "wb") as w:
        w.setnchannels(1)
        w.setsampwidth(2)
        w.setframerate(rate)
        w.writeframes(data.tobytes())
    return len(samples)


def fade(samples, in_s=0.004, out_s=0.010, rate=RATE):
    """양 끝을 0으로 내린다. 끝이 0이 아니면 재생 시작·끝에 「딱」 소리가 난다.
    S_Swim은 루프이므로 이 함수가 곧 이음매를 안 들리게 하는 장치이기도 하다."""
    n = len(samples)
    ni = max(1, int(in_s * rate))
    no = max(1, int(out_s * rate))
    for i in range(min(ni, n)):
        samples[i] *= i / ni
    for i in range(min(no, n)):
        samples[n - 1 - i] *= i / no
    return samples


def swim(rate=RATE):
    """1초 루프. 넓은 대역 잡음을 한 옥타브 폭으로 흔드는 필터에 통과시킨 물살 소리.
    음높이 변화는 여기서 굽지 않는다 -- 엔진이 재생 피치로 준다(aquarium::SwimPitch)."""
    rnd = random.Random(20260921)
    n = int(1.0 * rate)
    out = [0.0] * n
    lp1 = lp2 = 0.0
    for i in range(n):
        t = i / n                      # 0..1, 루프 위상
        white = rnd.uniform(-1.0, 1.0)
        # 시간에 따라 컷오프가 오르내리는 2극 저역통과. 물이 밀리는 「쉬—」.
        cut = 0.10 + 0.05 * math.sin(2.0 * math.pi * t)
        lp1 += cut * (white - lp1)
        lp2 += cut * (lp1 - lp2)
        # 아주 낮은 웅— 하나를 섞어 물속의 두께를 만든다.
        hum = 0.18 * math.sin(2.0 * math.pi * 3.0 * t)
        out[i] = 0.85 * lp2 + hum * 0.25
    return fade(out, in_s=0.05, out_s=0.05, rate=rate)


def startle(rate=RATE):
    """「꺅」 0.22초. 700Hz에서 2200Hz로 미끄러져 올라가는 연속음 + 짧은 잡음 숨.
    위로 올라가는 글라이드가 '놀랐다'를 만든다(내려가면 '시무룩'이 된다)."""
    rnd = random.Random(7)
    n = int(0.220 * rate)
    out = [0.0] * n
    phase = 0.0
    for i in range(n):
        t = i / n
        f = 700.0 + (2200.0 - 700.0) * (t ** 0.7)
        phase += 2.0 * math.pi * f / rate
        env = math.exp(-3.2 * t)
        body = math.sin(phase) + 0.30 * math.sin(2.0 * phase)
        breath = 0.22 * rnd.uniform(-1.0, 1.0) * math.exp(-11.0 * t)
        out[i] = env * body * 0.8 + breath
    return fade(out, rate=rate)


def nibble(rate=RATE):
    """「뽁」 0.12초. 320Hz에서 140Hz로 빠르게 떨어지는 타격음. 꺅과 반대 방향·
    반대 길이·반대 대역이라 스피커가 나빠도 구분된다(시나리오 장면 3 조건 1)."""
    n = int(0.120 * rate)
    out = [0.0] * n
    phase = 0.0
    for i in range(n):
        t = i / n
        f = 320.0 * math.exp(-2.1 * t) + 120.0
        phase += 2.0 * math.pi * f / rate
        env = math.exp(-16.0 * t)
        out[i] = env * (math.sin(phase) + 0.18 * math.sin(3.0 * phase))
    return fade(out, in_s=0.001, out_s=0.006, rate=rate)


def split(rate=RATE):
    """「촤악」 0.5초. 넓은 대역 잡음이 확 열렸다가 닫힌다 -- 물이 양쪽으로
    갈라지는 소리. 물고기 한 마리가 아니라 스무 마리가 내는 소리라 길다."""
    rnd = random.Random(31337)
    n = int(0.500 * rate)
    out = [0.0] * n
    hp = prev = 0.0
    for i in range(n):
        t = i / n
        white = rnd.uniform(-1.0, 1.0)
        # 1극 고역통과: 시작은 밝게 「촤」, 뒤로 갈수록 어둡게 「악」.
        a = 0.62 - 0.45 * t
        hp = a * (hp + white - prev)
        prev = white
        env = (1.0 - math.exp(-40.0 * t)) * math.exp(-4.3 * t)
        out[i] = hp * env
    return fade(out, rate=rate)


def bubble(rate=RATE):
    """「뽀글」 0.3초. 서로 다른 크기의 기포 세 개. 내 물고기 재롱에 쓰인다 --
    도망 소리와 절대 같으면 안 된다(시나리오: 내 물고기는 놀라지 않는다)."""
    n = int(0.300 * rate)
    out = [0.0] * n
    for k, (start, f0, f1, amp) in enumerate(
        ((0.00, 480.0, 900.0, 1.00), (0.09, 620.0, 1180.0, 0.72), (0.18, 380.0, 760.0, 0.55))
    ):
        phase = 0.0
        i0 = int(start * rate)
        for i in range(i0, n):
            t = (i - i0) / rate
            # 기포는 튀어 오르며 음이 올라간다(수면으로 갈수록 작아지는 소리).
            f = f0 + (f1 - f0) * (1.0 - math.exp(-24.0 * t))
            phase += 2.0 * math.pi * f / rate
            out[i] += amp * math.exp(-26.0 * t) * math.sin(phase)
    return fade(out, in_s=0.002, out_s=0.010, rate=rate)


SOUNDS = (
    ("S_Swim.wav", swim),
    ("S_Startle.wav", startle),
    ("S_Nibble.wav", nibble),
    ("S_Split.wav", split),
    ("S_Bubble.wav", bubble),
)


def main():
    os.makedirs(HERE, exist_ok=True)
    rows = []
    for name, fn in SOUNDS:
        path = os.path.join(HERE, name)
        samples = fn()
        count = write_wav(path, samples)
        with open(path, "rb") as f:
            digest = hashlib.sha256(f.read()).hexdigest()[:16]
        peak = max(abs(s) for s in samples)
        assert count > 0, name
        assert peak > 1e-6, "%s is silent" % name      # 무음 WAV는 조용히 통과하는 실패다
        rows.append("%s:%d:%s" % (name, count, digest))
        print("  %-14s frames=%6d sha256=%s" % (name, count, digest))
    print("SOUNDS_OK count=%d rate=%d [%s]" % (len(rows), RATE, " ".join(rows)))


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 실행하고 결과를 본다**

```bash
cd /Users/hans/dev/aquarium && python3 assets/audio/make_sounds.py
```
기대: 다섯 줄 + `SOUNDS_OK count=5 rate=44100 [...]`. 프레임 수는 44100 / 9702 / 5292 / 22050 / 13230이어야 한다(길이 × 44100). **다르면 계획의 산수가 틀린 것이므로 멈추고 실제 값을 보고한다.**

- [ ] **Step 3: 결정성을 확인한다 — 이것이 시나리오 검증표의 "파형 생성의 결정성"이다**

해시를 계획서에 리터럴로 박지 않는다(규약 7). 대신 **두 번 돌려 바이트가 같은지**를 본다.

```bash
cd /Users/hans/dev/aquarium
A=$(shasum -a 256 assets/audio/*.wav | awk '{print $1}' | shasum -a 256)
python3 assets/audio/make_sounds.py > /dev/null
B=$(shasum -a 256 assets/audio/*.wav | awk '{print $1}' | shasum -a 256)
[ "$A" = "$B" ] && echo "DETERMINISTIC_OK" || { echo "NOT DETERMINISTIC"; exit 1; }
```
기대: `DETERMINISTIC_OK`.

- [ ] **Step 4: 결정성 검사가 무는지 확인한다(변이)** — 규약 6.

`make_sounds.py`의 `startle()`에서 `rnd = random.Random(7)`을 `rnd = random.Random()`(시드 없음)으로 잠시 바꾸고 Step 3을 다시 돌린다.
기대: `NOT DETERMINISTIC`. **빨간불이 켜지지 않으면** 이 검사는 공허하므로, 켜지는 변이를 찾거나 찾지 못했다고 후속 항목에 적는다. 확인 후 **반드시 되돌린다**(`git diff`로 되돌림을 확인).

- [ ] **Step 5: 실제로 들어 본다(제작자 확인)** — 헤드리스가 못 하는 일이다. 다섯 개를 한 번씩 재생한다.

```bash
cd /Users/hans/dev/aquarium && for f in assets/audio/S_*.wav; do echo "$f"; afplay "$f"; sleep 0.3; done
```
확인할 것: (a) 「꺅」과 「뽁」이 **확실히 다르게** 들린다, (b) 어느 것도 「딱」 하는 끊김이 없다, (c) `S_Swim`을 반복 재생했을 때 이음매가 안 들린다(`afplay` 두 번 연속으로 대충 확인, 정식 확인은 Task 5). 아니면 파라미터를 고치고 Step 2~4를 반복한다.

- [ ] **Step 6: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add assets/audio && git commit -q -F - << 'EOF'
feat(m7): 반응음 5종 합성 스크립트와 WAV (표준 라이브러리만)

헤엄·꺅·뽁·촤악·뽀글을 assets/audio/make_sounds.py가 굽는다. numpy 없음이
확인돼 wave/array/math만 쓴다. 「꺅」과 「뽁」은 길이·포락선·주파수 방향이
전부 반대라 스피커가 나빠도 구분된다(시나리오 장면 3 조건 1).
결정성은 두 번 실행해 바이트 비교로 확인했고, 시드를 빼는 변이로 빨간불을
확인했다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1```

---

### Task 2: 앰비언스 — 없으면 **큰 소리로 실패**한다

**Files:** Create `assets/audio/ambience/SOURCE.md`, Create `scripts/check_ambience.sh`, Modify `docs/ASSETS.md`

시나리오 결정: **배경 소리는 CC0 녹음**이다. 앰비언스는 아이가 "듣는" 소리가 아니라 방의 공기이고, 여기서 합성음이 삑삑거리면 아이는 이유를 모른 채 "싼 느낌"만 받는다.

**이 계획을 실행하는 에이전트는 파일을 내려받을 수 없다.** 그래서 이 태스크는 **파일이 없으면 다음 태스크로 조용히 넘어가지 않고 멈춘다.** 앰비언스 없이 출하되는 것이 이 마일스톤에서 가장 조용한 실패 방식이기 때문이다.

**기대 파일 — 정확히 이 경로, 이 이름:**

```
assets/audio/ambience/A_Underwater.wav
```

**요구 사항**: WAV(PCM), 모노 또는 스테레오, 44.1 kHz 또는 48 kHz, **20초 이상**, **이음매 없이 반복되는 수중 앰비언스**(웅— 하는 먹먹한 저음과 보글보글), 라이선스 **CC0**. 후보 출처: Freesound의 CC0 필터, Pixabay Sound Effects, BBC Sound Effects(라이선스 확인 필요 — CC0가 아니면 쓰지 않는다).

- [ ] **Step 1: 출처 기록 틀을 만든다**

`assets/audio/ambience/SOURCE.md`:

```markdown
# A_Underwater.wav 출처

이 파일은 **직접 만든 것이 아니다.** docs/ASSETS.md "도입 시 필수 기록"의 열을 전부 채운 뒤에만
저장소에 들어간다. 아래 값이 비어 있으면 scripts/check_ambience.sh가 실패한다.

- 에셋 이름:
- 저작자:
- 원본 URL:
- 다운로드일:
- 라이선스: CC0 1.0 (라이선스 URL: )
- 출처 표기 문구(필요 시):
- 수정: (없음 / 잘라내기·페이드만)
- 게임 배포 가능: 예
- 공개 저장소 재배포 가능: 예
- 로컬 경로: assets/audio/ambience/A_Underwater.wav
```

- [ ] **Step 2: 검사 스크립트를 만든다**

`scripts/check_ambience.sh`:

```bash
#!/bin/bash
# 앰비언스가 실재하고 출처가 기록돼 있는지 본다. 없으면 무엇을 어디에 두어야
# 하는지 명확히 말하고 실패한다 -- 조용히 앰비언스 없이 출하되는 것이 이
# 마일스톤에서 가장 눈에 안 띄는 실패다.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
WAV="$ROOT/assets/audio/ambience/A_Underwater.wav"
SRC="$ROOT/assets/audio/ambience/SOURCE.md"

if [[ ! -f "$WAV" ]]; then
  cat >&2 << 'MSG'
ERROR: 배경 앰비언스가 없다.

  기대 경로 : assets/audio/ambience/A_Underwater.wav
  형식      : WAV(PCM), 44.1 또는 48 kHz, 20초 이상, 이음매 없이 반복 가능
  라이선스  : CC0 (다른 라이선스는 쓰지 않는다)
  내용      : 웅- 하는 먹먹한 저음 + 보글보글, 돌고래/파도/음악 없음

  파일을 놓은 뒤 assets/audio/ambience/SOURCE.md의 모든 항목을 채우고
  docs/ASSETS.md의 "도입한 외부 에셋" 표에 행을 추가한다.

  이 검사를 건너뛰고 진행하지 않는다. 앰비언스 없이 나가면 시나리오 장면 1이
  절반만 구현된 채로 출하되고, 그것은 부분 구현이 아니라 잘못된 평가를 낳는다.
MSG
  exit 1
fi

# 길이·형식은 추측하지 않고 실제 헤더에서 읽는다.
python3 - "$WAV" << 'PY'
import sys, wave
with wave.open(sys.argv[1], "rb") as w:
    rate = w.getframerate(); n = w.getnframes(); ch = w.getnchannels(); sw = w.getsampwidth()
secs = n / float(rate)
print("AMBIENCE ch=%d rate=%d width=%d frames=%d seconds=%.2f" % (ch, rate, sw, n, secs))
assert rate in (44100, 48000), "unexpected sample rate: %d" % rate
assert sw == 2, "expected 16-bit PCM, got %d bytes/sample" % sw
assert secs >= 20.0, "ambience is only %.2f s; need >= 20 s" % secs
PY

for field in "저작자:" "원본 URL:" "다운로드일:"; do
  line="$(grep -F "$field" "$SRC" || true)"
  value="${line#*"$field"}"
  if [[ -z "${value// /}" ]]; then
    echo "ERROR: SOURCE.md의 '$field' 항목이 비어 있다 (docs/ASSETS.md 필수 기록)" >&2
    exit 1
  fi
done
grep -qF "A_Underwater" "$ROOT/docs/ASSETS.md" || { echo "ERROR: docs/ASSETS.md에 앰비언스 행이 없다" >&2; exit 1; }
echo "AMBIENCE_OK"
```

```bash
cd /Users/hans/dev/aquarium && chmod +x scripts/check_ambience.sh
```

- [ ] **Step 3: 검사가 **무는지** 먼저 확인한다(파일이 아직 없는 상태에서)**

```bash
cd /Users/hans/dev/aquarium && ./scripts/check_ambience.sh; echo "exit=$?"
```
기대: 위의 ERROR 안내문 + `exit=1`. **이것이 이 검사의 RED다** — 있는 그대로 빨간불이 켜지는 것을 눈으로 본다.

- [ ] **Step 4: 파일을 구해서 놓는다 — 사람이 하는 일**

에이전트가 내려받을 수 없다. **이 스텝에서 멈추고 사용자에게 요청한다**: 위 요구 사항에 맞는 CC0 수중 앰비언스를 `assets/audio/ambience/A_Underwater.wav`에 놓고, `SOURCE.md`의 항목을 채워 달라고. 그 사이 **Task 14(무리 갈라지기 과장)를 먼저 진행해도 된다** — 넷 중 가장 싸고 다른 셋과 독립이라 순서를 바꿔도 손해가 없다(시나리오가 그렇게 적어 두었다).

- [ ] **Step 5: `docs/ASSETS.md`에 행을 추가한다** — "도입한 외부 에셋" 표 마지막 행 뒤에, `SOURCE.md`에 실제로 적힌 값으로:

```markdown
| 수중 앰비언스 `A_Underwater` (배경 소리, 루프) | (SOURCE.md의 저작자) | (SOURCE.md의 URL) | (다운로드일) | CC0 1.0 ((라이선스 URL)) | (표기 문구 또는 "불필요") | (없음 / 잘라내기·페이드만) | 가능 | 가능 | `assets/audio/ambience/` |
```

그리고 "직접 제작" 표 아래에 M7 절을 붙인다:

```markdown
M7 추가(2026-09-21): **반응음 5종은 직접 제작이고 생성 스크립트 자체가 출처다.**
`assets/audio/make_sounds.py`(파이썬 표준 라이브러리만, numpy 없음)가
`S_Swim.wav`(1.000 s, 루프) · `S_Startle.wav`(0.220 s) · `S_Nibble.wav`(0.120 s) ·
`S_Split.wav`(0.500 s) · `S_Bubble.wav`(0.300 s)를 16비트 모노 44.1 kHz PCM으로 굽는다.
난수는 전부 고정 시드 `random.Random`이라 **두 번 실행한 바이트가 같다**(확인 방법은 해시
리터럴이 아니라 두 번 실행 비교다). 외부 녹음은 배경 앰비언스 한 건뿐이며 위 표에 있다.
UI 아이콘 2종(`T_SoundOn`/`T_SoundOff`)도 `assets/ui/make_icons.py`가 굽는 직접 제작물이고
외부 이미지를 쓰지 않는다.
```

- [ ] **Step 6: 이제 초록불을 확인한다**

```bash
cd /Users/hans/dev/aquarium && ./scripts/check_ambience.sh
```
기대: `AMBIENCE ch=... seconds=...` + `AMBIENCE_OK`.

- [ ] **Step 7: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add assets/audio/ambience scripts/check_ambience.sh docs/ASSETS.md && git commit -q -F - << 'EOF'
feat(m7): CC0 배경 앰비언스와 출처 기록, 없으면 멈추는 검사

앰비언스는 합성하지 않는다(시나리오 결정). 파일이 없으면 check_ambience.sh가
무엇을 어디에 두어야 하는지 말하고 실패한다 -- 조용히 앰비언스 없이 출하되는
것이 이 마일스톤에서 가장 눈에 안 띄는 실패라서다. 파일이 없는 상태에서 먼저
빨간불을 확인했다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 3: 에디터 Python — WAV를 `USoundWave`로 임포트 (`import_audio.py`)

**Files:** Create `unreal/Aquarium/Scripts/import_audio.py`

**MetaSound·SoundCue 그래프는 만들지 않는다.** 에디터 Python으로 만들 수 없고, 그것이 재현성 제1원칙과 충돌한다(M4b에서 Niagara로 실제로 부딪힌 벽). 여기서 만드는 것은 **평범한 `USoundWave` 여섯 개**뿐이고, 재생·피치·음량은 전부 C++이 한다.

확인된 API(2026-09-21): `unreal.AssetImportTask` + `unreal.SoundFactory`로 임포트하고, 루프 여부는 팩토리가 아니라 **임포트된 `USoundWave`의 `looping` 속성**에 설정한다. `SoundFactory.b_auto_create_cue = False`로 두어야 `_Cue` 에셋이 딸려 생기지 않는다.

- [ ] **Step 1: 스크립트를 만든다**

`unreal/Aquarium/Scripts/import_audio.py`:

```python
# assets/audio/*.wav 를 /Game/Audio 아래 USoundWave로 임포트한다.
# 멱등하다: 같은 경로에 replace_existing=True로 다시 임포트하므로 _1 사본이 생기지 않는다.
#
# SoundCue도 MetaSound도 만들지 않는다 -- 사운드 그래프는 에디터 파이썬으로
# 만들 수 없고, 그래서 이 게임의 소리는 SoundWave + C++ 재생만으로 이루어진다.
#
# 헤드리스 실행:
#   UnrealEditor-Cmd Aquarium.uproject -run=pythonscript -script=Scripts/import_audio.py
#     -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput
# 판정은 마지막 AUDIO_OK 줄로만 한다. 종료 코드는 무관한 GameFeatureData 오류
# 때문에 항상 1이다.
import os
import unreal

ROOT = os.path.abspath(os.path.join(unreal.Paths.project_dir(), "..", ".."))
AUDIO_SRC = os.path.join(ROOT, "assets", "audio")
DEST = "/Game/Audio"

asset_tools = unreal.AssetToolsHelpers.get_asset_tools()
eal = unreal.EditorAssetLibrary

# (원본 파일, 에셋 이름, 루프 여부)
WAVS = [
    (os.path.join(AUDIO_SRC, "S_Swim.wav"), "S_Swim", True),
    (os.path.join(AUDIO_SRC, "S_Startle.wav"), "S_Startle", False),
    (os.path.join(AUDIO_SRC, "S_Nibble.wav"), "S_Nibble", False),
    (os.path.join(AUDIO_SRC, "S_Split.wav"), "S_Split", False),
    (os.path.join(AUDIO_SRC, "S_Bubble.wav"), "S_Bubble", False),
    (os.path.join(AUDIO_SRC, "ambience", "A_Underwater.wav"), "A_Underwater", True),
]


def import_sound(filename, name, looping):
    assert os.path.exists(filename), "missing wav: %s (앰비언스라면 scripts/check_ambience.sh 참고)" % filename
    factory = unreal.SoundFactory()
    # 자동 큐 생성을 끈다: SoundCue는 이 프로젝트가 쓰지 않는 에셋이고,
    # 생기면 배포물에 출처 없는 에셋이 하나 늘어난다.
    factory.set_editor_property("b_auto_create_cue", False)
    task = unreal.AssetImportTask()
    task.filename = filename
    task.destination_path = DEST
    task.destination_name = name
    task.automated = True
    task.replace_existing = True
    task.save = True
    task.options = factory
    asset_tools.import_asset_tasks([task])
    paths = [str(p) for p in task.imported_object_paths]
    assert paths, "import produced no asset: %s" % filename
    sound = unreal.load_asset(DEST + "/" + name)
    assert sound is not None, "could not load back: %s/%s" % (DEST, name)
    sound.set_editor_property("looping", looping)
    eal.save_loaded_asset(sound)
    # 되읽어서 확인한다. set_editor_property가 조용히 무시되는 경우를 잡는다.
    check = unreal.load_asset(DEST + "/" + name)
    got = check.get_editor_property("looping")
    assert bool(got) == bool(looping), "%s looping=%s, wanted %s" % (name, got, looping)
    dur = float(check.get_editor_property("duration"))
    assert dur > 0.01, "%s has zero duration" % name
    return name, dur, bool(got)


rows = []
for filename, name, looping in WAVS:
    n, dur, loop = import_sound(filename, name, looping)
    print("  imported %-14s duration=%.3f looping=%s" % (n, dur, loop))
    rows.append("%s:%.3f:%d" % (n, dur, 1 if loop else 0))

# 임포트되지 않은 잔여물이 없는지 확인한다 -- _Cue가 딸려 왔다면 여기서 보인다.
existing = sorted(os.path.basename(p) for p in eal.list_assets(DEST, recursive=False, include_folder=False))
print("AUDIO_OK count=%d assets=[%s] listed=[%s]" % (len(rows), " ".join(rows), " ".join(existing)))
assert len(existing) == len(WAVS), "unexpected extra assets in %s: %s" % (DEST, existing)
```

- [ ] **Step 2: 돌린다**

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" \
  -run=pythonscript -script="$PWD/unreal/Aquarium/Scripts/import_audio.py" \
  -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -E "AUDIO_OK|imported|Traceback|AssertionError"
```
기대: `imported` 6줄 + `AUDIO_OK count=6 ...`, `listed=[A_Underwater S_Bubble S_Nibble S_Split S_Startle S_Swim]`. **`AUDIO_OK`가 안 보이면 실패다** — 종료 코드를 보지 않는다(규약 4).

- [ ] **Step 3: 멱등성 확인** — 한 번 더 돌린다.

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" \
  -run=pythonscript -script="$PWD/unreal/Aquarium/Scripts/import_audio.py" \
  -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -E "AUDIO_OK|Traceback"
git -C /Users/hans/dev/aquarium status --short unreal/Aquarium/Content/Audio | head
```
기대: 같은 `AUDIO_OK` 줄, 그리고 `_1` 이름이 붙은 에셋이 **없다**(`listed=`에 6개만).

- [ ] **Step 4: 루프 플래그 단언이 무는지 확인(변이)** — `WAVS`에서 `("...S_Swim.wav", "S_Swim", True)`를 잠시 `False`로 바꾸고 다시 돌린다.
기대: `AssertionError: S_Swim looping=False, wanted True`가 아니라 — 이 경우는 요청대로 False를 썼으니 통과한다. **그러므로 올바른 변이는 다른 것이다**: `sound.set_editor_property("looping", looping)` 한 줄을 주석 처리하고 돌린다. 기대: `AssertionError: S_Swim looping=False, wanted True`. 확인 후 되돌린다. **빨간불이 안 켜지면**(예: 임포트 기본값이 이미 True) 후속 항목에 적는다.

- [ ] **Step 5: SETUP.md 파이프라인 순서에 한 줄 추가** — `docs/SETUP.md`의 스크립트 루프(`for s in import_fish.py import_props.py build_reef_m1.py verify_scene.py; do`)를 아래로 바꾼다:

```bash
for s in import_fish.py import_props.py import_audio.py import_ui.py build_fx_m7.py build_reef_m1.py verify_scene.py; do
```

`import_ui.py`와 `build_fx_m7.py`는 Task 7·Task 10에서 만든다. **순서가 중요하다**: 머티리얼·사운드·텍스처가 먼저 존재해야 레벨 빌드와 런타임 로드가 성공한다.

- [ ] **Step 6: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add unreal/Aquarium/Scripts/import_audio.py unreal/Aquarium/Content/Audio docs/SETUP.md && git commit -q -F - << 'EOF'
feat(m7): WAV 6종을 USoundWave로 임포트하는 멱등 에디터 파이썬

SoundCue도 MetaSound도 만들지 않는다(스크립트로 만들 수 없는 에셋은 쓰지
않는다는 재현성 원칙). 루프 플래그는 팩토리가 아니라 임포트된 SoundWave에
설정하고 되읽어 확인하며, 설정 줄을 지우는 변이로 빨간불을 확인했다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 4: 규칙 계층 — 소리의 규칙 (`Audio.h`), 규칙 103 → 110

**Files:** Create `rules/include/aquarium/Audio.h`, Create `tests/test_audio.cpp`, Modify `CMakeLists.txt`

엔진에 두면 Catch2가 닿지 못하는 세 가지를 순수 함수로 뺀다.

1. **속도 → 음높이·음량** (시나리오: "전속력으로 달릴 때 소리가 올라가면 그 자체가 놀이가 된다")
2. **재생마다 흔드는 음높이** (시나리오: 같은 파일 반복이면 금방 질린다)
3. **연타 음량 완화기(`VoiceLimiter`)** — 시나리오 장면 2 요구사항 4가 못 박은 자리다. **연타는 기본 사용법이므로 재생을 거절하지 않는다.** 겹쳐서 지지직거리는 것만 막아야 하므로, 밀도가 올라가면 **음량만** 내리고 **언제나 소리는 난다**. `Admit()`은 0을 돌려주지 않으며, 애초에 "재생 안 함"이라는 반환값이 없다 — 구조상 무시가 불가능하다.

- [ ] **Step 1 (RED): 테스트를 먼저 쓴다**

`tests/test_audio.cpp`:

```cpp
#include <catch2/catch_test_macros.hpp>
#include <catch2/matchers/catch_matchers_floating_point.hpp>
#include <catch2/catch_approx.hpp>

#include "aquarium/Audio.h"

using aquarium::AudioParams;
using aquarium::VoiceLimiter;
using Catch::Approx;

namespace {
const AudioParams kA{};
}

TEST_CASE("swim pitch rises with speed") {
    // 시나리오 결정표: "속도가 빠를수록 음이 높아진다". 가만히 있으면 기준 음.
    REQUIRE(aquarium::SwimPitch(0.f, kA) == Approx(kA.pitchAtRest));
    REQUIRE(aquarium::SwimPitch(kA.speedForMaxPitch, kA) == Approx(kA.pitchAtFullSpeed));
    // 사이는 단조 증가여야 한다. "조금 빨라졌는데 음이 내려갔다"는 놀이가 되지 않는다.
    float prev = -1.f;
    for (int i = 0; i <= 20; ++i) {
        const float s = kA.speedForMaxPitch * static_cast<float>(i) / 20.f;
        const float p = aquarium::SwimPitch(s, kA);
        REQUIRE(p > prev);
        prev = p;
    }
}

TEST_CASE("swim pitch is clamped above full speed") {
    // 도망 속도 배율(2.2배)이 플레이어에게 걸릴 일은 없지만, 속도는 어떤 경로로든
    // maxSpeed를 넘을 수 있다. 음높이가 무한정 올라가면 삑 소리가 된다.
    REQUIRE(aquarium::SwimPitch(kA.speedForMaxPitch * 10.f, kA) == Approx(kA.pitchAtFullSpeed));
    REQUIRE(aquarium::SwimPitch(-5.f, kA) == Approx(kA.pitchAtRest));
}

TEST_CASE("swim volume is silent at rest and loud at speed") {
    // 멈춰 있는데 물살 소리가 나면 "내가 한 것"이 아니게 된다 -- 장면 1의 전부가 그것이다.
    REQUIRE(aquarium::SwimVolume(0.f, kA) == Approx(0.f));
    REQUIRE(aquarium::SwimVolume(kA.speedForMaxPitch, kA) == Approx(kA.swimVolumeMax));
    REQUIRE(aquarium::SwimVolume(kA.speedForMaxPitch * 0.5f, kA) > 0.f);
}

TEST_CASE("jittered pitch differs between plays but stays in a musical band") {
    // "매번 조금씩 다르다"(장면 2 요구사항 2)를 소리 쪽에서 담당하는 함수.
    float lo = 99.f, hi = -99.f;
    int distinct = 0;
    float previous = -1.f;
    for (unsigned s = 1; s <= 32; ++s) {
        const float p = aquarium::JitterPitch(s, kA);
        REQUIRE(p >= 1.f - kA.pitchJitter - 1e-4f);
        REQUIRE(p <= 1.f + kA.pitchJitter + 1e-4f);
        if (p != previous) ++distinct;
        previous = p;
        lo = p < lo ? p : lo;
        hi = p > hi ? p : hi;
    }
    // 32번 중 최소 30번은 직전과 달라야 한다. 상수를 돌려주는 구현을 잡는 단언이다.
    REQUIRE(distinct >= 30);
    // 그리고 실제로 대역을 쓰고 있어야 한다(전부 1.0 근처면 "다르다"가 안 들린다).
    REQUIRE(hi - lo > kA.pitchJitter);
}

TEST_CASE("jittered pitch is deterministic for a given seed") {
    REQUIRE(aquarium::JitterPitch(12345u, kA) == Approx(aquarium::JitterPitch(12345u, kA)));
}

TEST_CASE("voice limiter never refuses a play (scenario: 연타가 기본 사용법)") {
    // 시나리오 장면 2 요구사항 4: "직전 클릭 처리 중이라 무시됨"이 되면 즉각성이 무너진다.
    // 그래서 이 타입에는 거절을 표현할 반환값 자체가 없고, 음량은 절대 0이 되지 않는다.
    VoiceLimiter v;
    for (int i = 0; i < 50; ++i) {          // 초당 서너 번이 아니라 한 프레임에 50번
        const float gain = v.Admit(kA);
        REQUIRE(gain >= kA.minVoiceGain);
        REQUIRE(gain <= 1.f);
    }
}

TEST_CASE("voice limiter ducks under a burst and recovers after quiet") {
    VoiceLimiter v;
    REQUIRE(v.Admit(kA) == Approx(1.f));    // 첫 소리는 온전한 음량이다
    for (int i = 0; i < 9; ++i) v.Admit(kA);
    const float burst = v.Admit(kA);
    REQUIRE(burst < 1.f);                   // 연타 중에는 줄어들고
    REQUIRE(burst >= kA.minVoiceGain);      // 그래도 들린다
    v.Step(kA.voiceWindow * 2.f);           // 아이가 손을 뗀다
    REQUIRE(v.Admit(kA) == Approx(1.f));    // 다음 한 번은 다시 온전하다
}
```

`CMakeLists.txt`의 `add_executable(rules_tests` 목록에 `tests/test_audio.cpp`를 추가한다(`tests/test_facing.cpp` 다음 줄).

```bash
cd /Users/hans/dev/aquarium && touch tests/test_audio.cpp && cmake -S . -B build && cmake --build build -j 2>&1 | tail -5
```
기대: **컴파일 실패** — `fatal error: 'aquarium/Audio.h' file not found`. 이것이 RED다.

- [ ] **Step 2 (GREEN): `rules/include/aquarium/Audio.h`를 쓴다**

```cpp
#pragma once
#include <cstdint>

namespace aquarium {

// 소리의 "규칙"만 여기 있다. 어떤 파일을 재생하는지, 어떤 컴포넌트에 꽂는지는
// 엔진의 일이다. 이 헤더는 Unreal을 모르고, 그래서 Catch2가 전부 볼 수 있다.
struct AudioParams {
    // 헤엄 소리: 속도 0에서 pitchAtRest, speedForMaxPitch에서 pitchAtFullSpeed.
    // speedForMaxPitch는 엔진이 AFishActor::MaxSpeed로 덮어쓴다(리터럴을 베끼지 않는다).
    float speedForMaxPitch = 40.f;    // cm/s
    float pitchAtRest = 0.80f;
    float pitchAtFullSpeed = 1.65f;
    float swimVolumeMax = 0.55f;
    // 반응음: 재생마다 1.0 +- pitchJitter 안에서 흔든다.
    float pitchJitter = 0.22f;
    // 연타 완화: voiceWindow 안에서 voiceSoftCap을 넘은 만큼만 음량을 깎는다.
    float voiceWindow = 0.45f;        // s
    int voiceSoftCap = 3;
    float minVoiceGain = 0.30f;       // 절대 0이 되지 않는다. 무시당한 느낌 금지.
};

inline float Clamp01(float v) { return v < 0.f ? 0.f : (v > 1.f ? 1.f : v); }

// 속도를 0..1로 정규화한다. speedForMaxPitch가 0이면 항상 0(0으로 나누지 않는다).
inline float SpeedFraction(float speed, const AudioParams& p) {
    if (p.speedForMaxPitch <= 1e-6f) return 0.f;
    return Clamp01(speed / p.speedForMaxPitch);
}

// 시나리오 결정표: "속도가 빠를수록 음이 높아진다". 선형이면 충분하다 -- 아이는
// 음정을 듣는 게 아니라 "내가 빨라지면 소리가 올라간다"를 듣는다.
inline float SwimPitch(float speed, const AudioParams& p) {
    return p.pitchAtRest + (p.pitchAtFullSpeed - p.pitchAtRest) * SpeedFraction(speed, p);
}

// 멈춰 있으면 완전한 무음이어야 한다. 제곱근 곡선을 쓰는 이유는 사람 귀가
// 음량을 로그로 듣기 때문이다 -- 선형이면 느린 헤엄에서 아무것도 안 들린다.
inline float SwimVolume(float speed, const AudioParams& p) {
    const float f = SpeedFraction(speed, p);
    float s = f;
    // sqrt를 <cmath> 없이: 뉴턴 두 번이면 이 용도에 충분하고 헤더가 가벼워진다.
    if (s > 0.f) {
        float g = s;
        g = 0.5f * (g + s / g);
        g = 0.5f * (g + s / g);
        g = 0.5f * (g + s / g);
        s = g;
    }
    return p.swimVolumeMax * s;
}

// 재생마다 흔드는 음높이. 결정적(같은 시드 = 같은 값)이라 테스트가 붙는다.
// 32비트 정수 해시(splitmix32) 한 번이면 연속된 시드도 완전히 흩어진다 --
// 단순한 곱셈-잉여였다면 1,2,3,4...가 거의 같은 값을 냈을 것이다.
inline float JitterPitch(std::uint32_t seed, const AudioParams& p) {
    std::uint32_t x = seed + 0x9E3779B9u;
    x ^= x >> 16; x *= 0x21F0AAADu;
    x ^= x >> 15; x *= 0x735A2D97u;
    x ^= x >> 15;
    const float unit = static_cast<float>(x >> 8) / 16777216.f;   // [0,1)
    return 1.f + (unit * 2.f - 1.f) * p.pitchJitter;
}

// 연타에서 소리가 겹쳐 지지직거리는 것을 막되, **재생을 거절하지 않는다.**
// 시나리오 장면 2 요구사항 4가 금지한 것은 "무시"이지 "작아짐"이 아니다.
// Admit()에는 실패를 표현할 반환값이 없다 -- 구조상 무시가 불가능하다.
class VoiceLimiter {
public:
    // 창이 흐른 만큼 최근 재생 수를 지수적으로 잊는다.
    void Step(float dt) {
        if (dt <= 0.f) return;
        // window_는 마지막 Admit이 params에서 받아 둔 값이다(파라미터를 두 곳에 두지 않는다).
        recent_ -= dt / window_;
        if (recent_ < 0.f) recent_ = 0.f;
    }

    // 이번 재생에 쓸 음량 배수를 돌려준다. 항상 (0, 1] 안이다.
    float Admit(const AudioParams& p) {
        window_ = p.voiceWindow > 1e-4f ? p.voiceWindow : 1e-4f;
        const float cap = static_cast<float>(p.voiceSoftCap);
        const float over = recent_ - cap;
        recent_ += 1.f;
        if (over <= 0.f) return 1.f;
        // 초과분 한 건마다 20%씩 줄이되 minVoiceGain 아래로는 절대 안 간다.
        float gain = 1.f / (1.f + 0.2f * over);
        if (gain < p.minVoiceGain) gain = p.minVoiceGain;
        return gain;
    }

    float RecentCount() const { return recent_; }

private:
    float recent_ = 0.f;
    float window_ = 0.45f;
};

} // namespace aquarium
```

```bash
cd /Users/hans/dev/aquarium && touch tests/test_audio.cpp rules/include/aquarium/Audio.h && cmake --build build -j 2>&1 | tail -3 && ctest --test-dir build 2>&1 | tail -3
```
기대: `100% tests passed out of 110`. **다른 숫자면 멈추고 실제 값을 보고한다**(규약 9).

- [ ] **Step 3: 변이로 무는지 확인한다** — 세 군데를 하나씩 바꾸고 각각 빨간불을 본다. 매번 `touch`로 mtime을 올린다(규약 2).

| 변이 | 바꿀 곳 | 빨간불이 켜져야 하는 테스트 |
|---|---|---|
| A | `SwimPitch` 본문을 `return p.pitchAtRest;`로 | `swim pitch rises with speed` |
| B | `JitterPitch` 본문을 `return 1.f;`로 | `jittered pitch differs between plays...` |
| C | `Admit`의 `if (gain < p.minVoiceGain) gain = p.minVoiceGain;`를 지우고 `0.2f * over`를 `5.f * over`로 | `voice limiter never refuses a play` |

```bash
# 각 변이마다
cd /Users/hans/dev/aquarium && touch rules/include/aquarium/Audio.h && cmake --build build -j 2>&1 | tail -2 && ctest --test-dir build 2>&1 | tail -3
```
기대: 각각 해당 테스트만 실패(`1 tests failed out of 110` 형태의 요약과 이름). **켜지지 않는 변이가 하나라도 있으면** 켜지는 변이를 찾거나 찾지 못했다고 후속 항목에 적는다. 세 변이를 전부 되돌리고 `git diff`가 비었는지 확인한다.

- [ ] **Step 4: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add rules/include/aquarium/Audio.h tests/test_audio.cpp CMakeLists.txt && git commit -q -F - << 'EOF'
feat(m7): 규칙 계층에 소리 규칙 — 속도→음높이, 재생별 음높이 흔들기, 연타 완화

VoiceLimiter는 재생을 거절하지 않는다. 거절을 표현할 반환값 자체가 없어서
"직전 클릭 처리 중이라 무시됨"이 구조적으로 불가능하다 — 시나리오 장면 2
요구사항 4가 금지한 것이 바로 그것이다. 밀도가 오르면 음량만 내리고 절대
minVoiceGain 아래로 가지 않는다.

규칙 103 → 110. 변이 세 개로 빨간불을 확인했다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 5: 장면 1 앞쪽 — 헤엄 소리 한 줄 (`AquariumAudioSubsystem`), Automation 60 → 64

**Files:** Create `unreal/Aquarium/Source/Aquarium/AquariumAudioSubsystem.h`, Create `.../AquariumAudioSubsystem.cpp`, Create `.../Tests/AudioTests.cpp`, Modify `.../DiverPlayerController.cpp`

**시나리오가 순서를 못 박은 자리다.** "소리 묶음 안에서도 헤엄 소리가 먼저다. 앰비언스는 아이가 의식하지 못하는 소리라 그것만 켜면 '아무것도 안 바뀌었는데?'가 된다. **헤엄 소리 한 줄이 켜지는 순간이 아이가 처음 웃는 지점**이다." 그래서 이 태스크가 끝나면 **그 자체로 아이에게 보여 줄 수 있는 완성품**이다 — 방향키를 누르면 소리가 나고 빨리 가면 음이 올라간다.

**헤드리스 검증의 정직한 한계**(규약 10): `-nullrhi` + `-nosound`에서는 오디오 디바이스가 없어 `CreateSound2D`가 `nullptr`을 돌려줄 수 있다. 그래서 서브시스템은 **컴포넌트가 생겼든 아니든** 규칙 계층이 계산한 음높이·음량을 `LastSwimPitch`/`LastSwimVolume`에 기록한다. 테스트는 그 값과 **에셋이 실제로 로드됐는지**를 본다. "소리가 났다"는 주장은 하지 않는다.

- [ ] **Step 1 (RED): 테스트를 먼저 쓴다**

`unreal/Aquarium/Source/Aquarium/Tests/AudioTests.cpp`:

```cpp
#include "Misc/AutomationTest.h"

#if WITH_DEV_AUTOMATION_TESTS && WITH_EDITOR

#include "Tests/AutomationEditorCommon.h"
#include "Engine/World.h"
#include "Sound/SoundWave.h"
#include "AquariumAudioSubsystem.h"
#include "aquarium/Audio.h"

namespace
{
UAquariumAudioSubsystem* MakeAudio(UWorld*& OutWorld)
{
	OutWorld = FAutomationEditorCommonUtils::CreateNewMap();
	return OutWorld ? OutWorld->GetSubsystem<UAquariumAudioSubsystem>() : nullptr;
}
}

// 에셋이 정말로 있는지. import_audio.py를 안 돌린 채 "재생 호출은 했다"로 초록불이
// 켜지는 것이 이 마일스톤에서 가장 쉬운 공허한 테스트다.
IMPLEMENT_SIMPLE_AUTOMATION_TEST(FAudioAssetsLoad, "Aquarium.Audio.AssetsLoad",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FAudioAssetsLoad::RunTest(const FString&)
{
	UWorld* World = nullptr;
	UAquariumAudioSubsystem* Audio = MakeAudio(World);
	if (!TestNotNull(TEXT("audio subsystem"), Audio)) return false;
	TestNotNull(TEXT("S_Swim"), Audio->SwimSound());
	TestNotNull(TEXT("A_Underwater"), Audio->AmbienceSound());
	int32 Loaded = 0;
	for (uint8 i = 0; i < static_cast<uint8>(EAquariumCue::Count); ++i)
	{
		if (Audio->SoundFor(static_cast<EAquariumCue>(i)) != nullptr) ++Loaded;
	}
	// 개수를 리터럴로 베끼지 않는다: Count가 곧 기대값이다.
	TestEqual(TEXT("every cue has a sound"), Loaded, static_cast<int32>(EAquariumCue::Count));
	// 헤엄 소리는 루프여야 한다. import_audio.py가 설정한 플래그를 여기서 되읽는다.
	if (const USoundWave* Wave = Cast<USoundWave>(Audio->SwimSound()))
	{
		TestTrue(TEXT("swim sound loops"), Wave->bLooping);
	}
	return true;
}

IMPLEMENT_SIMPLE_AUTOMATION_TEST(FAudioSwimFollowsSpeed, "Aquarium.Audio.SwimFollowsSpeed",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FAudioSwimFollowsSpeed::RunTest(const FString&)
{
	UWorld* World = nullptr;
	UAquariumAudioSubsystem* Audio = MakeAudio(World);
	if (!TestNotNull(TEXT("audio subsystem"), Audio)) return false;
	const float MaxSpeed = 40.f;
	Audio->UpdateSwim(0.f, MaxSpeed, 0.016f);
	const float RestPitch = Audio->LastSwimPitch();
	TestEqual(TEXT("silent at rest"), Audio->LastSwimVolume(), 0.f, 1e-4f);
	Audio->UpdateSwim(MaxSpeed, MaxSpeed, 0.016f);
	TestTrue(TEXT("pitch rises with speed"), Audio->LastSwimPitch() > RestPitch + 0.1f);
	TestTrue(TEXT("audible at speed"), Audio->LastSwimVolume() > 0.f);
	// 기대값은 규칙 계층에서 파생한다(엔진에 두 번째 사본을 만들지 않는다).
	aquarium::AudioParams P;
	P.speedForMaxPitch = MaxSpeed;
	TestEqual(TEXT("engine agrees with the rules layer"),
		Audio->LastSwimPitch(), aquarium::SwimPitch(MaxSpeed, P), 1e-4f);
	return true;
}

IMPLEMENT_SIMPLE_AUTOMATION_TEST(FAudioCuePitchVaries, "Aquarium.Audio.CuePitchVaries",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FAudioCuePitchVaries::RunTest(const FString&)
{
	UWorld* World = nullptr;
	UAquariumAudioSubsystem* Audio = MakeAudio(World);
	if (!TestNotNull(TEXT("audio subsystem"), Audio)) return false;
	TArray<float> Pitches;
	for (int32 i = 0; i < 12; ++i)
	{
		Audio->PlayCue(EAquariumCue::Startle, static_cast<uint32>(i * 7919 + 13));
		Pitches.Add(Audio->LastCuePitch());
	}
	int32 Distinct = 0;
	for (int32 i = 1; i < Pitches.Num(); ++i) { if (!FMath::IsNearlyEqual(Pitches[i], Pitches[i - 1])) ++Distinct; }
	TestTrue(TEXT("pitch differs between plays"), Distinct >= 10);
	TestEqual(TEXT("every play was counted"), Audio->CuePlayCount(EAquariumCue::Startle), 12);
	return true;
}

// 시나리오 장면 2 요구사항 4를 엔진 쪽에서 한 번 더 못 박는다: 연타해도
// 재생 횟수가 요청 횟수와 정확히 같다. 하나라도 삼켜지면 빨간불이다.
IMPLEMENT_SIMPLE_AUTOMATION_TEST(FAudioRapidCuesAreNeverDropped, "Aquarium.Audio.RapidCuesAreNeverDropped",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FAudioRapidCuesAreNeverDropped::RunTest(const FString&)
{
	UWorld* World = nullptr;
	UAquariumAudioSubsystem* Audio = MakeAudio(World);
	if (!TestNotNull(TEXT("audio subsystem"), Audio)) return false;
	const int32 Requested = 40;
	float MinGain = 2.f;
	for (int32 i = 0; i < Requested; ++i)
	{
		Audio->PlayCue(EAquariumCue::Nibble, static_cast<uint32>(i));
		MinGain = FMath::Min(MinGain, Audio->LastCueVolume());
	}
	TestEqual(TEXT("no cue was dropped"), Audio->CuePlayCount(EAquariumCue::Nibble), Requested);
	aquarium::AudioParams P;
	TestTrue(TEXT("ducked but never silent"), MinGain >= P.minVoiceGain - 1e-4f && MinGain < 1.f);
	return true;
}

#endif
```

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
"$UE/Engine/Build/BatchFiles/Mac/Build.sh" AquariumEditor Mac Development -Project="$PWD/unreal/Aquarium/Aquarium.uproject" -WaitMutex 2>&1 | grep -E "error:|^Result:" | head
```
기대: **컴파일 실패** — `'AquariumAudioSubsystem.h' file not found`. 이것이 RED다.

- [ ] **Step 2 (GREEN): 서브시스템 헤더**

`unreal/Aquarium/Source/Aquarium/AquariumAudioSubsystem.h`:

```cpp
#pragma once

#include "CoreMinimal.h"
#include "Subsystems/WorldSubsystem.h"

#include "aquarium/Audio.h"

#include "AquariumAudioSubsystem.generated.h"

class UAudioComponent;
class USoundBase;

// 한 번에 재생되는 반응음 한 종류. 순서는 SoundPaths와 짝이고, Count는 개수를
// 세는 유일한 출처다(테스트가 기대 개수를 리터럴로 베끼지 않게 하려는 것).
UENUM()
enum class EAquariumCue : uint8
{
	Startle,   // 「꺅」 배경 물고기가 놀람
	Nibble,    // 「뽁」 먹이를 먹음
	Split,     // 「촤악」 무리가 갈라짐
	Bubble,    // 「뽀글」 내 물고기 재롱
	Count UMETA(Hidden)
};

// 이 게임의 모든 소리를 가진 곳. 사운드 그래프(MetaSound/SoundCue)는 쓰지 않는다 --
// 에디터 파이썬으로 만들 수 없기 때문이고, 그 결정이 이 클래스가 존재하는 이유다.
// 소리는 전부 2D다: 카메라가 고정이라 감쇠가 할 일이 없고, 감쇠 에셋을 만들지
// 않는다는 뜻이기도 하다.
UCLASS()
class AQUARIUM_API UAquariumAudioSubsystem : public UWorldSubsystem
{
	GENERATED_BODY()

public:
	virtual void Initialize(FSubsystemCollectionBase& Collection) override;
	virtual void Deinitialize() override;

	// 플레이어 물고기의 현재 속도를 넘긴다. 음높이·음량은 규칙 계층이 정한다.
	// MaxSpeed를 같이 받는 이유: 속도 기준을 C++에 리터럴로 베끼지 않기 위해서다.
	void UpdateSwim(float Speed, float MaxSpeed, float DeltaSeconds);

	// 반응음 한 번. Seed가 재생별 음높이를 정한다(같은 시드 = 같은 음높이).
	// **재생을 거절하지 않는다.** 밀도가 높으면 음량만 내린다(시나리오 장면 2 요구사항 4).
	void PlayCue(EAquariumCue Cue, uint32 Seed);

	// 배경 앰비언스 루프를 시작한다. 이미 돌고 있으면 아무것도 하지 않는다.
	void StartAmbience();
	void StopAmbience();

	// 그림 버튼이 부르는 곳. 소리를 끄는 것이지 게임을 멈추는 것이 아니다.
	void SetMuted(bool bInMuted);
	bool IsMuted() const { return bMuted; }
	void ToggleMuted() { SetMuted(!bMuted); }

	// --- 에셋 접근(테스트가 "에셋이 정말 있는지"를 보게 한다) ---
	USoundBase* SoundFor(EAquariumCue Cue) const;
	USoundBase* SwimSound() const { return SwimWave; }
	USoundBase* AmbienceSound() const { return AmbienceWave; }

	// --- 헤드리스에서 관측 가능한 상태 ---
	// -nullrhi/-nosound 에서는 오디오 디바이스가 없어 UAudioComponent가 안 생길 수
	// 있다. 그래서 규칙 계층이 계산한 값은 컴포넌트와 무관하게 여기 기록된다.
	// 테스트는 이 값을 본다. "소리가 실제로 들렸다"는 주장은 하지 않는다.
	float LastSwimPitch() const { return LastSwimPitchValue; }
	float LastSwimVolume() const { return LastSwimVolumeValue; }
	float LastCuePitch() const { return LastCuePitchValue; }
	float LastCueVolume() const { return LastCueVolumeValue; }
	int32 CuePlayCount(EAquariumCue Cue) const;

	// 개발 전용: 오디오를 통째로 끈다(성능 귀속용). 값을 받지 않는 불리언이라
	// 파싱 실패로 조용히 무시될 토큰이 없다 -- -AquariumAutoClick이 당한 일이다.
	bool bAudioEnabled = true;

private:
	UPROPERTY() TObjectPtr<USoundBase> SwimWave = nullptr;
	UPROPERTY() TObjectPtr<USoundBase> AmbienceWave = nullptr;
	UPROPERTY() TArray<TObjectPtr<USoundBase>> CueWaves;
	UPROPERTY() TObjectPtr<UAudioComponent> SwimComponent = nullptr;
	UPROPERTY() TObjectPtr<UAudioComponent> AmbienceComponent = nullptr;

	aquarium::AudioParams AudioParamsValue;
	aquarium::VoiceLimiter Voices;
	TArray<int32> PlayCounts;
	float LastSwimPitchValue = 0.f;
	float LastSwimVolumeValue = 0.f;
	float LastCuePitchValue = 1.f;
	float LastCueVolumeValue = 1.f;
	bool bMuted = false;

	// 뮤트 상태를 두 컴포넌트에 반영한다. 정지시키지 않고 음량만 0으로 둔다 --
	// 다시 켰을 때 앰비언스가 처음부터 시작하면 "끊겼다 이어졌다"가 들린다.
	void ApplyMute();
	static USoundBase* LoadSound(const TCHAR* Path);
};
```

- [ ] **Step 3 (GREEN): 서브시스템 구현**

`unreal/Aquarium/Source/Aquarium/AquariumAudioSubsystem.cpp`:

```cpp
#include "AquariumAudioSubsystem.h"

#include "Components/AudioComponent.h"
#include "Kismet/GameplayStatics.h"
#include "Misc/CommandLine.h"
#include "Misc/Parse.h"
#include "Sound/SoundBase.h"

namespace
{
	// /Game/Audio 아래 import_audio.py가 만든 에셋들. 순서는 EAquariumCue와 짝이다.
	const TCHAR* kCuePaths[] = {
		TEXT("/Game/Audio/S_Startle.S_Startle"),
		TEXT("/Game/Audio/S_Nibble.S_Nibble"),
		TEXT("/Game/Audio/S_Split.S_Split"),
		TEXT("/Game/Audio/S_Bubble.S_Bubble"),
	};
	static_assert(UE_ARRAY_COUNT(kCuePaths) == static_cast<int32>(EAquariumCue::Count),
		"EAquariumCue and kCuePaths must stay in step");
	constexpr float kAmbienceVolume = 0.42f;
	constexpr float kAmbienceFadeIn = 2.0f;
}

USoundBase* UAquariumAudioSubsystem::LoadSound(const TCHAR* Path)
{
	// 조용히 nullptr이 되는 경로다. 테스트 Aquarium.Audio.AssetsLoad가 이걸 본다.
	return LoadObject<USoundBase>(nullptr, Path);
}

void UAquariumAudioSubsystem::Initialize(FSubsystemCollectionBase& Collection)
{
	Super::Initialize(Collection);
#if !UE_BUILD_SHIPPING
	bAudioEnabled = !FParse::Param(FCommandLine::Get(), TEXT("AquariumNoAudio"));
	bMuted = FParse::Param(FCommandLine::Get(), TEXT("AquariumMuted"));
#endif
	SwimWave = LoadSound(TEXT("/Game/Audio/S_Swim.S_Swim"));
	AmbienceWave = LoadSound(TEXT("/Game/Audio/A_Underwater.A_Underwater"));
	CueWaves.Reset();
	for (const TCHAR* Path : kCuePaths)
	{
		CueWaves.Add(LoadSound(Path));
	}
	PlayCounts.Init(0, static_cast<int32>(EAquariumCue::Count));
}

void UAquariumAudioSubsystem::Deinitialize()
{
	if (SwimComponent) { SwimComponent->Stop(); SwimComponent = nullptr; }
	if (AmbienceComponent) { AmbienceComponent->Stop(); AmbienceComponent = nullptr; }
	Super::Deinitialize();
}

USoundBase* UAquariumAudioSubsystem::SoundFor(EAquariumCue Cue) const
{
	const int32 Index = static_cast<int32>(Cue);
	return CueWaves.IsValidIndex(Index) ? CueWaves[Index].Get() : nullptr;
}

int32 UAquariumAudioSubsystem::CuePlayCount(EAquariumCue Cue) const
{
	const int32 Index = static_cast<int32>(Cue);
	return PlayCounts.IsValidIndex(Index) ? PlayCounts[Index] : 0;
}

void UAquariumAudioSubsystem::UpdateSwim(float Speed, float MaxSpeed, float DeltaSeconds)
{
	Voices.Step(DeltaSeconds);
	// 속도 기준은 물고기에서 파생한다. AFishActor::MaxSpeed를 C++에 두 번 적지 않는다.
	AudioParamsValue.speedForMaxPitch = MaxSpeed > 1e-3f ? MaxSpeed : AudioParamsValue.speedForMaxPitch;
	LastSwimPitchValue = aquarium::SwimPitch(Speed, AudioParamsValue);
	LastSwimVolumeValue = aquarium::SwimVolume(Speed, AudioParamsValue);
	if (!bAudioEnabled || SwimWave == nullptr)
	{
		return;
	}
	if (SwimComponent == nullptr)
	{
		// bAutoDestroy=false: 이 컴포넌트는 세션 내내 살아 있고 음량만 움직인다.
		// 매번 새로 스폰하면 방향키를 누를 때마다 루프가 처음부터 시작해 딱딱 끊긴다.
		SwimComponent = UGameplayStatics::CreateSound2D(this, SwimWave, 0.f, 1.f, 0.f,
			nullptr, /*bPersistAcrossLevelTransition*/ false, /*bAutoDestroy*/ false);
		if (SwimComponent)
		{
			SwimComponent->Play();
		}
	}
	if (SwimComponent)
	{
		SwimComponent->SetPitchMultiplier(LastSwimPitchValue);
		SwimComponent->SetVolumeMultiplier(bMuted ? 0.f : LastSwimVolumeValue);
	}
}

void UAquariumAudioSubsystem::PlayCue(EAquariumCue Cue, uint32 Seed)
{
	const int32 Index = static_cast<int32>(Cue);
	if (!PlayCounts.IsValidIndex(Index))
	{
		return;
	}
	// 횟수·음높이·음량은 오디오 디바이스가 없어도 항상 기록된다. 이것이 헤드리스
	// 검증이 닿는 한계이고, 테스트는 정확히 여기까지만 주장한다.
	++PlayCounts[Index];
	LastCuePitchValue = aquarium::JitterPitch(Seed, AudioParamsValue);
	// 거절이 아니라 음량 완화다. Admit은 실패를 돌려줄 수 없다.
	LastCueVolumeValue = Voices.Admit(AudioParamsValue);
	if (!bAudioEnabled || bMuted)
	{
		return;
	}
	USoundBase* Sound = SoundFor(Cue);
	if (Sound == nullptr)
	{
		return;
	}
	UGameplayStatics::SpawnSound2D(this, Sound, LastCueVolumeValue, LastCuePitchValue);
}

void UAquariumAudioSubsystem::StartAmbience()
{
	if (!bAudioEnabled || AmbienceWave == nullptr || AmbienceComponent != nullptr)
	{
		return;
	}
	AmbienceComponent = UGameplayStatics::CreateSound2D(this, AmbienceWave, kAmbienceVolume, 1.f, 0.f,
		nullptr, false, /*bAutoDestroy*/ false);
	if (AmbienceComponent)
	{
		// 페이드 인: 입장 순간 앰비언스가 툭 켜지면 그것만 의식된다. 앰비언스는
		// 의식되지 않아야 성공이다(시나리오 결정표).
		AmbienceComponent->FadeIn(kAmbienceFadeIn, bMuted ? 0.f : kAmbienceVolume);
	}
}

void UAquariumAudioSubsystem::StopAmbience()
{
	if (AmbienceComponent)
	{
		AmbienceComponent->Stop();
		AmbienceComponent = nullptr;
	}
}

void UAquariumAudioSubsystem::SetMuted(bool bInMuted)
{
	bMuted = bInMuted;
	ApplyMute();
}

void UAquariumAudioSubsystem::ApplyMute()
{
	// 정지가 아니라 음량 0이다. 앰비언스를 정지시켰다 다시 켜면 루프가 처음으로
	// 돌아가 "끊겼다 이어졌다"가 들리고, 그건 의식되는 앰비언스가 된다.
	if (AmbienceComponent) { AmbienceComponent->SetVolumeMultiplier(bMuted ? 0.f : kAmbienceVolume); }
	if (SwimComponent) { SwimComponent->SetVolumeMultiplier(bMuted ? 0.f : LastSwimVolumeValue); }
}
```

- [ ] **Step 4 (GREEN): 컨트롤러에서 매 틱 속도를 넘긴다**

`DiverPlayerController.cpp`의 `ApplyInputToPlayerFish` 끝부분을 아래로 바꾼다:

```cpp
	Fish->SetInputDirection(DirectionFor(ArrowKeys));
	// 장면 1: 방향키를 누르는 순간 물살 소리가 나고, 빠를수록 음이 올라간다.
	// 소리 규칙 자체는 규칙 계층에 있고, 여기서는 속도를 건네줄 뿐이다.
	if (UWorld* W = GetWorld())
	{
		if (UAquariumAudioSubsystem* Audio = W->GetSubsystem<UAquariumAudioSubsystem>())
		{
			Audio->UpdateSwim(Fish->CurrentSpeed(), Fish->MaxSpeed, DeltaSeconds);
		}
	}
```

같은 파일 상단의 include 묶음에 한 줄을 더한다:

```cpp
#include "AquariumAudioSubsystem.h"
```

- [ ] **Step 5: 빌드 두 번 + Automation**

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
for p in 1 2; do "$UE/Engine/Build/BatchFiles/Mac/Build.sh" AquariumEditor Mac Development -Project="$PWD/unreal/Aquarium/Aquarium.uproject" -WaitMutex 2>&1 | grep -E "^Result:|error:"; done
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" -ExecCmds="Automation RunTests Aquarium; Quit" -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -E "Test Completed|Tests Failed" | tail -5
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" -ExecCmds="Automation RunTests Aquarium; Quit" -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -cE "Test Completed.*Success"
```
기대: `Result: Succeeded` 두 번, 마지막 줄 `64`.

**`Aquarium.Audio.AssetsLoad`가 실패하면** 그것은 코드가 아니라 Task 3을 안 돌렸거나 `/Game/Audio` 경로가 다른 것이다. 실제 경로를 확인한다: `ls unreal/Aquarium/Content/Audio`.

- [ ] **Step 6: 네 테스트가 무는지 확인한다(변이)**

| 변이 | 바꿀 곳 | 빨간불 |
|---|---|---|
| A | `Initialize`의 `SwimWave = LoadSound(...)`를 `SwimWave = nullptr;`로 | `Aquarium.Audio.AssetsLoad` |
| B | `UpdateSwim`의 `LastSwimPitchValue = ...`를 `LastSwimPitchValue = 1.f;`로 | `Aquarium.Audio.SwimFollowsSpeed` |
| C | `PlayCue`의 `LastCuePitchValue = aquarium::JitterPitch(...)`를 `= 1.f;`로 | `Aquarium.Audio.CuePitchVaries` |
| D | `PlayCue` 맨 앞에 `if (Voices.RecentCount() > 3.f) return;`를 넣는다 | `Aquarium.Audio.RapidCuesAreNeverDropped` |

변이 D는 특히 중요하다 — **이것이 바로 시나리오가 금지한 "너무 자주 눌러서 제한했다"이고, 그 구현이 들어오면 테스트가 빨간불이 되어야 한다.** 매 변이마다 빌드 두 번 후 Automation을 돌린다. 전부 되돌리고 `git diff`가 비었는지 확인한다.

- [ ] **Step 7: 실제로 들어 본다 — 장면 1의 첫 웃음 확인**

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
"$UE/Engine/Binaries/Mac/UnrealEditor" "$PWD/unreal/Aquarium/Aquarium.uproject" ReefM1 -game -windowed -ResX=1280 -ResY=720 -ForceRes -AquariumAutoNickname="니모" 2>&1 | tail -3
```
방향키를 눌렀다 놓기를 열 번 한다. 확인할 것: (a) **누르면 소리가 난다**, (b) 놓으면 잦아든다, (c) 계속 누르면 음이 올라간다, (d) 루프 이음매가 안 들린다. 안 되면 `AudioParams`를 조정하고 Step 5부터 다시.

- [ ] **Step 8: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add unreal/Aquarium/Source/Aquarium && git commit -q -F - << 'EOF'
feat(m7): 장면 1 앞쪽 — 속도를 따라가는 헤엄 소리

시나리오가 못 박은 순서를 지킨다: 소리 묶음 안에서도 헤엄 소리가 먼저다.
이 커밋만으로도 아이에게 보여 줄 수 있는 완성품이다(누르면 소리가 나고 빨리
가면 음이 올라간다).

사운드 그래프를 쓰지 않으므로 SoundWave + UAudioComponent + 2D 재생뿐이고,
카메라가 고정이라 감쇠 에셋도 없다. 헤드리스에는 오디오 디바이스가 없으므로
테스트는 "소리가 났다"가 아니라 "에셋이 로드됐고 규칙 계층이 계산한 값이
기록됐다"까지만 주장한다.

Automation 60 → 64. 변이 네 개로 빨간불을 확인했고, 그중 하나는 "연타를
제한하는 구현"이 들어오면 빨간불이 되는 변이다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 6: 장면 1 뒤쪽 — 입장하면 물속에 얼굴을 담근다 (앰비언스), Automation 64 → 65

**Files:** Modify `unreal/Aquarium/Source/Aquarium/DiverPlayerController.cpp`, Modify `.../Tests/AudioTests.cpp`

시나리오 장면 1: "이름을 친다. **입장하자마자 물속에 얼굴을 담근 소리**가 난다." 앰비언스는 **세션이 시작될 때** 켜고 **나가기에서** 끈다 — 입장 화면에서 웅— 소리가 나면 그건 방의 공기가 아니라 메뉴 배경음이 된다.

- [ ] **Step 1 (RED): 테스트를 더한다** — `Tests/AudioTests.cpp` 끝(`#endif` 앞)에:

```cpp
IMPLEMENT_SIMPLE_AUTOMATION_TEST(FAudioAmbienceStartsOnce, "Aquarium.Audio.AmbienceStartsOnce",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FAudioAmbienceStartsOnce::RunTest(const FString&)
{
	UWorld* World = nullptr;
	UAquariumAudioSubsystem* Audio = MakeAudio(World);
	if (!TestNotNull(TEXT("audio subsystem"), Audio)) return false;
	TestFalse(TEXT("silent before a session"), Audio->AmbienceRunning());
	Audio->StartAmbience();
	const bool bAfterFirst = Audio->AmbienceRunning();
	Audio->StartAmbience();   // 두 번째 호출이 두 번째 루프를 깔면 소리가 두 겹이 된다
	TestEqual(TEXT("second start is a no-op"), Audio->AmbienceRunning(), bAfterFirst);
	TestEqual(TEXT("started exactly once"), Audio->AmbienceStartCount(), 1);
	Audio->StopAmbience();
	TestFalse(TEXT("stopped on exit"), Audio->AmbienceRunning());
	Audio->StartAmbience();
	TestEqual(TEXT("a new session starts it again"), Audio->AmbienceStartCount(), 2);
	return true;
}
```

- [ ] **Step 2 (GREEN): 서브시스템에 관측 가능한 상태를 더한다**

`AquariumAudioSubsystem.h`의 public 관측 구역에:

```cpp
	// 앰비언스는 오디오 디바이스가 없어도 "켜졌다"를 기록한다. 컴포넌트 유무로
	// 판정하면 -nullrhi에서 이 테스트가 항상 false를 보고 아무것도 검증하지 못한다.
	bool AmbienceRunning() const { return bAmbienceRunning; }
	int32 AmbienceStartCount() const { return AmbienceStarts; }
```

private에:

```cpp
	bool bAmbienceRunning = false;
	int32 AmbienceStarts = 0;
```

`AquariumAudioSubsystem.cpp`의 `StartAmbience`/`StopAmbience`를 아래로 바꾼다:

```cpp
void UAquariumAudioSubsystem::StartAmbience()
{
	if (AmbienceWave == nullptr || bAmbienceRunning)
	{
		return;   // 두 번 깔면 같은 루프가 두 겹으로 돌아 위상 간섭이 들린다
	}
	bAmbienceRunning = true;
	++AmbienceStarts;
	if (!bAudioEnabled)
	{
		return;
	}
	AmbienceComponent = UGameplayStatics::CreateSound2D(this, AmbienceWave, kAmbienceVolume, 1.f, 0.f,
		nullptr, false, /*bAutoDestroy*/ false);
	if (AmbienceComponent)
	{
		AmbienceComponent->FadeIn(kAmbienceFadeIn, bMuted ? 0.f : kAmbienceVolume);
	}
}

void UAquariumAudioSubsystem::StopAmbience()
{
	bAmbienceRunning = false;
	if (AmbienceComponent)
	{
		AmbienceComponent->Stop();
		AmbienceComponent = nullptr;
	}
}
```

- [ ] **Step 3 (GREEN): 세션 시작·종료에 건다**

`DiverPlayerController.cpp`의 `ShowSession()` 끝에:

```cpp
	if (UWorld* W = GetWorld())
	{
		if (UAquariumAudioSubsystem* Audio = W->GetSubsystem<UAquariumAudioSubsystem>())
		{
			Audio->StartAmbience();   // 입장하는 순간 물속에 얼굴을 담근다(장면 1)
		}
	}
```

`ShowEntry()` 끝에:

```cpp
	if (UWorld* W = GetWorld())
	{
		if (UAquariumAudioSubsystem* Audio = W->GetSubsystem<UAquariumAudioSubsystem>())
		{
			Audio->StopAmbience();   // 입장 화면은 조용하다. 메뉴 배경음을 만들지 않는다
		}
	}
```

- [ ] **Step 4: 빌드 두 번 + Automation**

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
for p in 1 2; do "$UE/Engine/Build/BatchFiles/Mac/Build.sh" AquariumEditor Mac Development -Project="$PWD/unreal/Aquarium/Aquarium.uproject" -WaitMutex 2>&1 | grep -E "^Result:|error:"; done
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" -ExecCmds="Automation RunTests Aquarium; Quit" -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -cE "Test Completed.*Success"
```
기대: `65`.

- [ ] **Step 5: 변이 확인** — `StartAmbience` 앞쪽의 `|| bAmbienceRunning` 조건을 지운다(= 세션마다 루프가 겹쳐 쌓이는 구현). 기대: `Aquarium.Audio.AmbienceStartsOnce`가 빨간불. 되돌린다.

- [ ] **Step 6: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add unreal/Aquarium/Source/Aquarium && git commit -q -F - << 'EOF'
feat(m7): 장면 1 — 입장하면 배경 앰비언스가 페이드 인으로 깔린다

입장 화면은 조용히 둔다(메뉴 배경음을 만들지 않는다). 두 번 깔리면 같은 루프가
두 겹으로 돌아 위상 간섭이 들리므로 중복 시작을 막고, 그 조건을 지우는 변이로
빨간불을 확인했다. 헤드리스에 오디오 디바이스가 없으므로 판정은 컴포넌트
유무가 아니라 기록된 상태로 한다.

Automation 64 → 65.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 7: 소리를 끄는 **그림** 버튼, Automation 65 → 68

**Files:** Create `assets/ui/make_icons.py`, Create `unreal/Aquarium/Scripts/import_ui.py`, Modify `.../HudWidget.h`, Modify `.../HudWidget.cpp`, Modify `.../DiverPlayerController.cpp`, Create `.../Tests/MuteButtonTests.cpp`

시나리오 "제약과 위험": **"소리를 끄려고 게임을 종료해야 하면 소리를 넣은 것이 손해가 된다. 스피커에 작대기 그림 하나면 되고, 글자는 쓰지 않는다."** 재미의 문제가 아니라 아이가 이 게임을 계속 켤 수 있느냐의 문제다.

**글자를 쓰지 않으므로 그림이 필요하고, 그림도 스크립트로 만든다**(재현성 제1원칙). PIL 없이 표준 라이브러리 `zlib`+`struct`만으로 PNG를 쓴다 — 128×128 두 장이면 충분하다.

- [ ] **Step 1: 아이콘 생성기**

`assets/ui/make_icons.py`:

```python
#!/usr/bin/env python3
# 스피커 아이콘 두 장(켜짐/꺼짐)을 128x128 RGBA PNG로 굽는다.
# 외부 이미지도, PIL도 쓰지 않는다 -- zlib과 struct만으로 PNG를 직접 쓴다.
# 글자는 한 획도 넣지 않는다(시나리오: 아이는 글자를 읽지 않는다).
#
# 실행: python3 assets/ui/make_icons.py     판정: 마지막 ICONS_OK 줄
import hashlib
import os
import struct
import zlib

SIZE = 128
HERE = os.path.dirname(os.path.abspath(__file__))
WHITE = (250, 250, 252)
RED = (235, 70, 70)


def png(path, pixels):
    """pixels: [ (r,g,b,a) ] * SIZE*SIZE, 행 우선."""
    raw = bytearray()
    for y in range(SIZE):
        raw.append(0)                       # 필터 타입 0 (None) -- 결정적이다
        for x in range(SIZE):
            raw.extend(pixels[y * SIZE + x])

    def chunk(tag, data):
        return (struct.pack(">I", len(data)) + tag + data
                + struct.pack(">I", zlib.crc32(tag + data) & 0xFFFFFFFF))

    ihdr = struct.pack(">IIBBBBB", SIZE, SIZE, 8, 6, 0, 0, 0)   # 8비트 RGBA
    body = (b"\x89PNG\r\n\x1a\n" + chunk(b"IHDR", ihdr)
            + chunk(b"IDAT", zlib.compress(bytes(raw), 9)) + chunk(b"IEND", b""))
    with open(path, "wb") as f:
        f.write(body)
    return len(body)


def blank():
    return [(0, 0, 0, 0)] * (SIZE * SIZE)


def put(px, x, y, rgb, a=255):
    if 0 <= x < SIZE and 0 <= y < SIZE:
        px[y * SIZE + x] = (rgb[0], rgb[1], rgb[2], a)


def speaker(px):
    """스피커 몸통: 왼쪽 사각형 + 오른쪽으로 벌어지는 사다리꼴."""
    for y in range(SIZE):
        for x in range(SIZE):
            fx, fy = x / SIZE, y / SIZE
            in_box = 0.14 <= fx <= 0.30 and 0.38 <= fy <= 0.62
            # 사다리꼴: x가 커질수록 위아래로 벌어진다
            half = 0.12 + (fx - 0.30) * 1.30
            in_cone = 0.30 <= fx <= 0.52 and abs(fy - 0.50) <= half
            if in_box or in_cone:
                put(px, x, y, WHITE)


def waves(px):
    """오른쪽 음파 두 줄. 원호를 두께 4px로 찍는다."""
    cx, cy = 0.50, 0.50
    for y in range(SIZE):
        for x in range(SIZE):
            fx, fy = x / SIZE, y / SIZE
            if fx < 0.53:
                continue
            d = ((fx - cx) ** 2 + (fy - cy) ** 2) ** 0.5
            for r in (0.16, 0.25):
                if abs(d - r) < 0.022 and abs(fy - cy) < r * 0.80:
                    put(px, x, y, WHITE)


def slash(px):
    """오른쪽 위에서 왼쪽 아래로 긋는 붉은 작대기. '꺼짐'을 아이가 읽는 유일한 신호."""
    for y in range(SIZE):
        for x in range(SIZE):
            fx, fy = x / SIZE, y / SIZE
            if abs((fx - 0.18) - (fy - 0.82) * -1.0) < 0.045 and 0.16 <= fx <= 0.86:
                put(px, x, y, RED)


def main():
    os.makedirs(HERE, exist_ok=True)
    rows = []
    for name, build in (("T_SoundOn.png", (speaker, waves)), ("T_SoundOff.png", (speaker, slash))):
        px = blank()
        for fn in build:
            fn(px)
        opaque = sum(1 for p in px if p[3] > 0)
        assert opaque > 500, "%s is nearly empty (%d px)" % (name, opaque)
        path = os.path.join(HERE, name)
        size = png(path, px)
        digest = hashlib.sha256(open(path, "rb").read()).hexdigest()[:16]
        print("  %-16s bytes=%d opaque=%d sha256=%s" % (name, size, opaque, digest))
        rows.append("%s:%d" % (name, opaque))
    # 두 아이콘이 서로 달라야 한다. 같은 그림 두 장이면 버튼이 아무것도 말하지 않는다.
    a = open(os.path.join(HERE, "T_SoundOn.png"), "rb").read()
    b = open(os.path.join(HERE, "T_SoundOff.png"), "rb").read()
    assert a != b, "on/off icons are identical"
    print("ICONS_OK count=2 size=%d [%s]" % (SIZE, " ".join(rows)))


if __name__ == "__main__":
    main()
```

```bash
cd /Users/hans/dev/aquarium && python3 assets/ui/make_icons.py && open assets/ui/T_SoundOn.png assets/ui/T_SoundOff.png
```
기대: `ICONS_OK count=2 size=128 [...]`. **두 장을 실제로 연다.** 확인할 것: 스피커로 읽히는가, 꺼짐 쪽에 붉은 작대기가 있는가, 글자가 없는가. 읽히지 않으면 좌표를 고친다 — 7살이 읽어야 하는 그림이다.

- [ ] **Step 2: 임포트 스크립트**

`unreal/Aquarium/Scripts/import_ui.py`:

```python
# assets/ui/*.png 를 /Game/UI 아래 텍스처로 임포트한다. 멱등하다.
#
# sRGB는 켜 두고 압축은 UI 기본(TC_EditorIcon = 압축 없음)으로 둔다. M4a에서
# 러프니스 텍스처를 TC_Default로 임포트했다가 머티리얼이 회색 기본으로 조용히
# 떨어진 전례가 있어, 임포트 설정은 추측하지 않고 명시한다.
#
# 판정은 마지막 UI_OK 줄로만 한다(종료 코드는 항상 1이다).
import os
import unreal

ROOT = os.path.abspath(os.path.join(unreal.Paths.project_dir(), "..", ".."))
SRC = os.path.join(ROOT, "assets", "ui")
DEST = "/Game/UI"

asset_tools = unreal.AssetToolsHelpers.get_asset_tools()
eal = unreal.EditorAssetLibrary

ICONS = ["T_SoundOn", "T_SoundOff"]
rows = []
for name in ICONS:
    filename = os.path.join(SRC, name + ".png")
    assert os.path.exists(filename), "missing icon: %s (python3 assets/ui/make_icons.py)" % filename
    task = unreal.AssetImportTask()
    task.filename = filename
    task.destination_path = DEST
    task.destination_name = name
    task.automated = True
    task.replace_existing = True
    task.save = True
    asset_tools.import_asset_tasks([task])
    tex = unreal.load_asset(DEST + "/" + name)
    assert tex is not None, "import failed: %s" % name
    tex.set_editor_property("srgb", True)
    tex.set_editor_property("compression_settings", unreal.TextureCompressionSettings.TC_EDITOR_ICON)
    tex.set_editor_property("lod_group", unreal.TextureGroup.TEXTUREGROUP_UI)
    eal.save_loaded_asset(tex)
    check = unreal.load_asset(DEST + "/" + name)
    w = int(check.get_editor_property("imported_size").x)
    h = int(check.get_editor_property("imported_size").y)
    assert w == 128 and h == 128, "%s is %dx%d, expected 128x128" % (name, w, h)
    rows.append("%s:%dx%d" % (name, w, h))
    print("  imported %s %dx%d" % (name, w, h))

print("UI_OK count=%d [%s]" % (len(rows), " ".join(rows)))
```

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" \
  -run=pythonscript -script="$PWD/unreal/Aquarium/Scripts/import_ui.py" \
  -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -E "UI_OK|imported|Traceback|AssertionError"
```
기대: `imported` 2줄 + `UI_OK count=2 [T_SoundOn:128x128 T_SoundOff:128x128]`.
**`TC_EDITOR_ICON`이나 `TEXTUREGROUP_UI`라는 이름이 없다는 오류가 나면** 실제 열거형 이름을 `unreal.TextureCompressionSettings.__dict__`로 찍어 확인하고 고친 뒤 그 사실을 후속 항목에 적는다(계획의 이름을 믿지 않는다 — 규약 9).

- [ ] **Step 3 (RED): 테스트를 먼저 쓴다**

`unreal/Aquarium/Source/Aquarium/Tests/MuteButtonTests.cpp`:

```cpp
#include "Misc/AutomationTest.h"

#if WITH_DEV_AUTOMATION_TESTS && WITH_EDITOR

#include "Tests/AutomationEditorCommon.h"
#include "Blueprint/WidgetTree.h"
#include "Components/Image.h"
#include "Components/TextBlock.h"
#include "Engine/Texture2D.h"
#include "Engine/World.h"
#include "AquariumAudioSubsystem.h"
#include "HudWidget.h"

namespace
{
UHudWidget* MakeHud(UWorld*& OutWorld)
{
	OutWorld = FAutomationEditorCommonUtils::CreateNewMap();
	UHudWidget* Hud = OutWorld ? CreateWidget<UHudWidget>(OutWorld, UHudWidget::StaticClass()) : nullptr;
	if (Hud) { Hud->TakeWidget(); }   // RebuildWidget을 실제로 돌린다
	return Hud;
}
}

// 아이콘 텍스처가 실제로 존재하는지. 없으면 버튼이 빈 사각형으로 그려지는데
// 에디터에서는 그게 "투명한 버튼"으로 보여 멀쩡해 보인다.
IMPLEMENT_SIMPLE_AUTOMATION_TEST(FMuteIconsExist, "Aquarium.Hud.MuteIconsExist",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FMuteIconsExist::RunTest(const FString&)
{
	UTexture2D* On = LoadObject<UTexture2D>(nullptr, TEXT("/Game/UI/T_SoundOn.T_SoundOn"));
	UTexture2D* Off = LoadObject<UTexture2D>(nullptr, TEXT("/Game/UI/T_SoundOff.T_SoundOff"));
	TestNotNull(TEXT("T_SoundOn"), On);
	TestNotNull(TEXT("T_SoundOff"), Off);
	TestTrue(TEXT("icons differ"), On != Off);
	return true;
}

// 시나리오: "글자는 쓰지 않는다". 뮤트 버튼 안에 UTextBlock이 있으면 빨간불이다.
IMPLEMENT_SIMPLE_AUTOMATION_TEST(FMuteButtonHasNoText, "Aquarium.Hud.MuteButtonHasNoText",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FMuteButtonHasNoText::RunTest(const FString&)
{
	UWorld* World = nullptr;
	UHudWidget* Hud = MakeHud(World);
	if (!TestNotNull(TEXT("hud"), Hud)) return false;
	TestNotNull(TEXT("mute icon widget"), Hud->MuteIcon());
	int32 TextInsideMute = 0;
	Hud->WidgetTree->ForEachWidget([&](UWidget* W)
	{
		if (W && W->IsA<UTextBlock>() && W->GetName().Contains(TEXT("Mute"))) ++TextInsideMute;
	});
	TestEqual(TEXT("no text in the mute button"), TextInsideMute, 0);
	return true;
}

IMPLEMENT_SIMPLE_AUTOMATION_TEST(FMuteButtonTogglesAudio, "Aquarium.Hud.MuteButtonTogglesAudio",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FMuteButtonTogglesAudio::RunTest(const FString&)
{
	UWorld* World = nullptr;
	UHudWidget* Hud = MakeHud(World);
	if (!TestNotNull(TEXT("hud"), Hud)) return false;
	UAquariumAudioSubsystem* Audio = World->GetSubsystem<UAquariumAudioSubsystem>();
	if (!TestNotNull(TEXT("audio"), Audio)) return false;
	bool bFired = false;
	Hud->OnToggleMute.BindLambda([&]() { bFired = true; Audio->ToggleMuted(); });
	TestFalse(TEXT("starts unmuted"), Audio->IsMuted());
	Hud->SimulateMuteClick();
	TestTrue(TEXT("delegate fired"), bFired);
	TestTrue(TEXT("muted after one click"), Audio->IsMuted());
	// 그림이 상태를 따라가야 한다. 안 따라가면 아이는 꺼졌는지 알 수 없다.
	UTexture2D* OffTex = LoadObject<UTexture2D>(nullptr, TEXT("/Game/UI/T_SoundOff.T_SoundOff"));
	TestEqual(TEXT("icon shows off"), Hud->CurrentMuteTexture(), OffTex);
	Hud->SimulateMuteClick();
	TestFalse(TEXT("unmuted after a second click"), Audio->IsMuted());
	// 뮤트는 소리만 끈다. 게임은 계속 돈다 -- 벌을 만들지 않는다.
	Audio->SetMuted(true);
	Audio->UpdateSwim(40.f, 40.f, 0.016f);
	TestTrue(TEXT("rules still run while muted"), Audio->LastSwimPitch() > 1.f);
	return true;
}

#endif
```

- [ ] **Step 4 (GREEN): `HudWidget.h`**

`public:` 구역에 아래를 더하고, 기존 `IsPointerOverExitButton`은 **`IsPointerOverButton`으로 이름을 바꾼다**(이제 버튼이 둘이고, 뮤트 버튼 뒤의 물고기도 놀라면 안 된다):

```cpp
	// 소리 끄기 버튼이 눌렸다. 소유자가 오디오 서브시스템을 토글한다.
	FSimpleDelegate OnToggleMute;

	// 커서가 HUD 버튼 **아무거나** 위에 있는지. 버튼이 가져가는 클릭이 뒤의
	// 물고기를 놀래키거나 먹이를 뿌리면 안 된다(F-09, 그리고 이제 장면 3도).
	bool IsPointerOverButton() const;

	// 테스트용 접근자. 슬레이트 입력 없이 버튼 동작을 관측한다.
	class UImage* MuteIcon() const { return MuteImage; }
	class UTexture2D* CurrentMuteTexture() const;
	void SimulateMuteClick() { HandleMuteClicked(); }
	// 아이콘을 상태에 맞춰 바꾼다. 소유자가 뮤트를 토글한 뒤 부른다.
	void SetMutedVisual(bool bInMuted);
```

private에:

```cpp
	UPROPERTY() TObjectPtr<UButton> MuteButton = nullptr;
	UPROPERTY() TObjectPtr<class UImage> MuteImage = nullptr;
	UPROPERTY() TObjectPtr<class UTexture2D> SoundOnTexture = nullptr;
	UPROPERTY() TObjectPtr<class UTexture2D> SoundOffTexture = nullptr;
	bool bMutedVisual = false;

	UFUNCTION() void HandleMuteClicked();
```

- [ ] **Step 5 (GREEN): `HudWidget.cpp`**

include에 `#include "Components/Image.h"`와 `#include "Engine/Texture2D.h"`를 더하고, 익명 네임스페이스에:

```cpp
	// 나가기 버튼과 같은 여백, 그 **왼쪽**에 놓는다. 나가기는 우상단 구석에
	// 그대로 두고(M2 설계: 작고 눈에 안 띄는 자리), 뮤트는 그 옆이다.
	constexpr float kMuteSize = 44.f;
	constexpr float kMuteGap = 92.f;   // 나가기 버튼 너비 + 여백
```

`RebuildWidget()`의 나가기 버튼 슬롯 설정 뒤에 아래를 더한다:

```cpp
		SoundOnTexture = LoadObject<UTexture2D>(nullptr, TEXT("/Game/UI/T_SoundOn.T_SoundOn"));
		SoundOffTexture = LoadObject<UTexture2D>(nullptr, TEXT("/Game/UI/T_SoundOff.T_SoundOff"));

		MuteButton = WidgetTree->ConstructWidget<UButton>(UButton::StaticClass(), TEXT("MuteButton"));
		MuteButton->OnClicked.AddDynamic(this, &UHudWidget::HandleMuteClicked);
		MuteButton->SetBackgroundColor(kButtonTint);

		// 그림만 넣는다. 글자는 한 자도 넣지 않는다(시나리오: 아이는 글자를 읽지 않는다).
		MuteImage = WidgetTree->ConstructWidget<UImage>(UImage::StaticClass(), TEXT("MuteIconImage"));
		MuteImage->SetBrushSize(FVector2D(kMuteSize * 0.62f, kMuteSize * 0.62f));
		if (SoundOnTexture) { MuteImage->SetBrushFromTexture(SoundOnTexture, /*bMatchSize*/ false); }
		if (UButtonSlot* IconSlot = Cast<UButtonSlot>(MuteButton->AddChild(MuteImage)))
		{
			IconSlot->SetPadding(FMargin(6.f));
		}

		if (UCanvasPanelSlot* MuteSlot = Root->AddChildToCanvas(MuteButton))
		{
			MuteSlot->SetAnchors(FAnchors(1.f, 0.f));
			MuteSlot->SetAlignment(FVector2D(1.f, 0.f));
			MuteSlot->SetPosition(FVector2D(-kEdgeMargin - kMuteGap, kEdgeMargin));
			MuteSlot->SetSize(FVector2D(kMuteSize, kMuteSize));
		}
```

그리고 파일 끝의 `IsPointerOverExitButton`을 아래로 바꾼다:

```cpp
void UHudWidget::HandleMuteClicked()
{
	OnToggleMute.ExecuteIfBound();
	SetMutedVisual(!bMutedVisual);
}

void UHudWidget::SetMutedVisual(bool bInMuted)
{
	bMutedVisual = bInMuted;
	UTexture2D* Tex = bMutedVisual ? SoundOffTexture.Get() : SoundOnTexture.Get();
	if (MuteImage && Tex) { MuteImage->SetBrushFromTexture(Tex, /*bMatchSize*/ false); }
}

UTexture2D* UHudWidget::CurrentMuteTexture() const
{
	return bMutedVisual ? SoundOffTexture.Get() : SoundOnTexture.Get();
}

bool UHudWidget::IsPointerOverButton() const
{
	return (ExitButton != nullptr && ExitButton->IsHovered())
		|| (MuteButton != nullptr && MuteButton->IsHovered());
}
```

- [ ] **Step 6 (GREEN): 컨트롤러 배선과 이름 변경 반영**

```bash
cd /Users/hans/dev/aquarium && grep -rn "IsPointerOverExitButton" unreal/Aquarium/Source/
```
나오는 모든 자리를 `IsPointerOverButton`으로 바꾼다(`DiverPlayerController.cpp`의 `HandleClick` 가드와, 있다면 기존 테스트). 그리고 `ShowSession()`에서 HUD를 만든 뒤:

```cpp
	Hud->OnToggleMute.BindLambda([this]()
	{
		if (UWorld* W = GetWorld())
		{
			if (UAquariumAudioSubsystem* Audio = W->GetSubsystem<UAquariumAudioSubsystem>())
			{
				Audio->ToggleMuted();
			}
		}
	});
	// 명령줄 -AquariumMuted로 켜진 상태라면 그림도 그 상태로 시작해야 한다.
	if (UWorld* W = GetWorld())
	{
		if (UAquariumAudioSubsystem* Audio = W->GetSubsystem<UAquariumAudioSubsystem>())
		{
			Hud->SetMutedVisual(Audio->IsMuted());
		}
	}
```

- [ ] **Step 7: 빌드 두 번 + Automation**

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
for p in 1 2; do "$UE/Engine/Build/BatchFiles/Mac/Build.sh" AquariumEditor Mac Development -Project="$PWD/unreal/Aquarium/Aquarium.uproject" -WaitMutex 2>&1 | grep -E "^Result:|error:"; done
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" -ExecCmds="Automation RunTests Aquarium; Quit" -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -cE "Test Completed.*Success"
```
기대: `68`.

- [ ] **Step 8: 변이 확인**

| 변이 | 바꿀 곳 | 빨간불 |
|---|---|---|
| A | `RebuildWidget`에서 `MuteImage->SetBrushFromTexture` 두 줄을 지운다 | `Aquarium.Hud.MuteButtonTogglesAudio`(아이콘 비교) |
| B | `HandleMuteClicked`의 `OnToggleMute.ExecuteIfBound();`를 지운다 | `Aquarium.Hud.MuteButtonTogglesAudio` |
| C | `MuteButton`에 `UTextBlock`(이름 `MuteLabel`)을 넣고 "소리"라고 쓴다 | `Aquarium.Hud.MuteButtonHasNoText` |
| D | `import_ui.py`가 `T_SoundOff`를 임포트하지 않게 한다(목록에서 제거 후 `/Game/UI/T_SoundOff` 삭제) | `Aquarium.Hud.MuteIconsExist` |

변이 C가 특히 중요하다 — 글자가 들어오면 빨간불이 되어야 한다는 것이 시나리오의 요구다. 전부 되돌리고 `git diff`·`git status`가 깨끗한지 확인한다(변이 D는 `import_ui.py`를 다시 돌려 복구한다).

- [ ] **Step 9: 눈으로 본다**

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
"$UE/Engine/Binaries/Mac/UnrealEditor" "$PWD/unreal/Aquarium/Aquarium.uproject" ReefM1 -game -windowed -ResX=1280 -ResY=720 -ForceRes -AquariumAutoNickname="니모" 2>&1 | tail -3
```
확인할 것: (a) 우상단에 나가기, 그 왼쪽에 스피커 그림, (b) 스피커를 누르면 작대기가 그어지고 **소리가 즉시 멎는다**, (c) 다시 누르면 돌아온다, (d) **뮤트 중에도 게임이 그대로 돈다**, (e) 스피커 버튼을 눌러도 뒤의 물고기가 놀라지 않는다, (f) 나가기 버튼이 여전히 작고 구석에 있다(Task 0 Step 9의 판정과 일치).

- [ ] **Step 10: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add assets/ui unreal/Aquarium/Scripts/import_ui.py unreal/Aquarium/Content/UI unreal/Aquarium/Source/Aquarium && git commit -q -F - << 'EOF'
feat(m7): 소리를 끄는 그림 버튼 (글자 없음)

시나리오의 위험 항목 정면 대응 — 소리를 끄려고 게임을 종료해야 하면 소리를
넣은 것이 손해다. 아이콘도 스크립트로 굽는다(PIL 없이 zlib+struct로 PNG를
직접 쓴다). 글자가 들어오면 빨간불이 되는 테스트를 두었고, 실제로 글자를
넣는 변이로 확인했다. 뮤트는 정지가 아니라 음량 0이라 게임은 그대로 돈다.

HUD 버튼이 둘이 되었으므로 클릭 가드를 IsPointerOverButton으로 넓혔다 —
버튼이 가져간 클릭이 뒤의 물고기를 놀래키거나 먹이를 뿌리면 안 된다.

Automation 65 → 68.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 8: 규칙 계층 — 반응의 규칙 (`Reaction.h`)과 **F-11의 의도적 변경**, 규칙 110 → 118

**Files:** Create `rules/include/aquarium/Reaction.h`, Create `tests/test_reaction.cpp`, Modify `rules/include/aquarium/Flee.h`, Modify `tests/test_flee.cpp`, Modify `CMakeLists.txt`

**이 태스크에는 기존 사양을 바꾸는 결정이 하나 들어 있다. 조용히 넘어가지 않는다.**

M5가 구현한 F-11은 **도망 중의 재터치를 무시**한다(`if (state_ == BehaviorState::Fleeing) return;`). 시나리오 장면 2 요구사항 4는 정반대를 요구한다 — **"'직전 클릭 처리 중이라 무시됨'이 되면 즉각성이 통째로 무너진다."** 아이는 초당 서너 번 마구 찍는다.

**결정**: 무시를 없앤다. 재터치는 도망 방향을 **다시 겨누고** 타이머를 새로 채운다. F-11이 원래 막으려던 것은 "상태가 꼬이는 것"이지 "두 번째 클릭이 보이는 것"이 아니었고, 상태 기계는 재진입을 이미 감당한다. 이 변경으로 기존 테스트 하나가 뒤집히므로 **그 테스트를 지우지 않고 뒤집어 다시 쓴다**(왜 뒤집혔는지가 코드에 남아야 한다). `docs/TASK.md`의 사양 변경도 Task 18에서 기록한다.

세 가지를 더 넣는다.

1. **`ReactionStyle`** — 시나리오 장면 2 요구사항 2("어떤 놈은 빙글 돌고, 어떤 놈은 삐끗 뒤집히고, 어떤 놈은 파르르 떤다")
2. **즉시성** — 요구사항 1("0.05초 안에 시작"). 램프 인을 두지 않는 것이 구현이고, 테스트는 **dt=0에서 이미 최대 배율**임을 본다
3. **`PlayerReaction`** — 내 물고기 전용. **입력을 돌려주는 함수가 아예 없다.** 조종권 상실이 구조적으로 불가능하다

- [ ] **Step 1: 기존 F-11 테스트를 찾는다(전제 확인)**

```bash
cd /Users/hans/dev/aquarium && grep -n "re-touch\|F-11" tests/test_flee.cpp
```
기대: 도망 중 재터치를 무시한다고 단언하는 테스트가 하나 이상 보인다. **안 보이면** 계획의 전제가 틀린 것이므로 실제 상태를 보고하고 아래 Step 3의 수정 대상을 다시 정한다.

- [ ] **Step 2 (RED): `tests/test_reaction.cpp`를 새로 쓴다**

```cpp
#include <catch2/catch_test_macros.hpp>
#include <catch2/catch_approx.hpp>

#include "aquarium/Flee.h"
#include "aquarium/Reaction.h"

using namespace aquarium;
using Catch::Approx;

namespace {
const FleeParams kF{};
const PlayerReactionParams kR{};
}

TEST_CASE("reaction style varies between fish (scenario scene 2, req 2)") {
    int counts[3] = {0, 0, 0};
    for (unsigned s = 1; s <= 300; ++s) {
        counts[static_cast<int>(PickReactionStyle(s))] += 1;
    }
    // 세 가지가 전부 나와야 한다. 하나라도 0이면 "매번 조금씩 다르다"가 거짓이 된다.
    REQUIRE(counts[0] > 40);
    REQUIRE(counts[1] > 40);
    REQUIRE(counts[2] > 40);
    // 같은 시드는 같은 스타일(결정적)
    REQUIRE(PickReactionStyle(77u) == PickReactionStyle(77u));
}

TEST_CASE("every style startles: faster than normal and visibly turning or shaking") {
    for (int i = 0; i < 3; ++i) {
        const StartleShape s = ShapeFor(static_cast<ReactionStyle>(i), kF);
        REQUIRE(s.speedScale > 1.2f);                       // 확실히 빨라진다
        REQUIRE((s.spinDegPerSec != 0.f || s.wobbleDeg > 0.f));  // 눈에 보이는 몸짓이 있다
    }
    // 그리고 셋이 서로 달라야 한다 -- 이름만 셋이고 값이 같으면 아무 의미가 없다.
    const StartleShape a = ShapeFor(ReactionStyle::Dart, kF);
    const StartleShape b = ShapeFor(ReactionStyle::Spin, kF);
    const StartleShape c = ShapeFor(ReactionStyle::Tumble, kF);
    REQUIRE(((a.spinDegPerSec != b.spinDegPerSec) || (a.wobbleDeg != b.wobbleDeg)));
    REQUIRE(((b.spinDegPerSec != c.spinDegPerSec) || (b.wobbleDeg != c.wobbleDeg)));
}

TEST_CASE("reaction starts instantly, with no ramp-in (scenario scene 2, req 1)") {
    // 0.05초 안에 시작한다는 요구는 60fps에서 세 프레임이다. 램프 인이 있으면
    // 첫 프레임 값이 1에 가깝다. 그래서 dt가 전혀 흐르지 않은 시점을 본다.
    FleeStateMachine m;
    m.Touch({0.f, 0.f}, {10.f, 0.f}, {0.f, 0.f}, {0.f, 1.f}, kF);
    REQUIRE(m.State() == BehaviorState::Fleeing);
    REQUIRE(m.SpeedScale(kF) == Approx(kF.fleeSpeedScale));
    REQUIRE(m.FleeDirection().Length() == Approx(1.f));
    // 0.05초 뒤에도 여전히 최대치(줄어들기 시작하지 않는다)
    m.Step(0.05f);
    REQUIRE(m.SpeedScale(kF) == Approx(kF.fleeSpeedScale));
}

TEST_CASE("re-touch mid-flee re-aims instead of being ignored (scenario scene 2, req 4)") {
    // 이 테스트는 M5의 'F-11: 도망 중 재터치 무시'를 **의도적으로 뒤집은 것**이다.
    // 아이는 초당 서너 번 마구 찍고, 무시는 즉각성을 통째로 무너뜨린다.
    FleeStateMachine m;
    m.Touch({0.f, 0.f}, {10.f, 0.f}, {0.f, 0.f}, {0.f, 1.f}, kF);
    const Vec2 first = m.FleeDirection();
    m.Step(0.2f);
    m.Touch({20.f, 0.f}, {10.f, 0.f}, {0.f, 0.f}, {0.f, 1.f}, kF);   // 반대쪽에서 다시 찍는다
    const Vec2 second = m.FleeDirection();
    REQUIRE(second.x < 0.f);                       // 새 클릭에서 멀어진다
    REQUIRE(first.x > 0.f);                        // 첫 클릭과는 반대 방향이다
    REQUIRE(m.TouchCount() == 2);                  // 두 번 다 셌다 -- 삼킨 클릭이 없다
    REQUIRE(m.State() == BehaviorState::Fleeing);
}

TEST_CASE("every touch is counted, however fast they come") {
    FleeStateMachine m;
    for (int i = 0; i < 25; ++i) {
        m.Touch({static_cast<float>(i), 0.f}, {10.f, 5.f}, {0.f, 0.f}, {0.f, 1.f}, kF);
        m.Step(0.016f);
    }
    REQUIRE(m.TouchCount() == 25);
}

TEST_CASE("player reaction has no way to take control away (scenario decision table)") {
    // 이것은 성능이 아니라 **정확성** 요구다. 내 물고기는 도망가지 않고, 조종권을
    // 한 순간도 잃지 않는다. 그래서 이 타입에는 방향을 돌려주는 함수가 없다 --
    // 있는 것은 시각 오프셋뿐이라 조종권 상실이 구조적으로 불가능하다.
    PlayerReaction r;
    REQUIRE_FALSE(r.Active());
    r.Touch(5u);
    REQUIRE(r.Active());
    REQUIRE(r.RollOffsetDeg(kR) != 0.f || r.Style() == ReactionStyle::Dart);
    float t = 0.f;
    while (t < kR.duration * 2.f) { r.Step(0.02f); t += 0.02f; }
    REQUIRE_FALSE(r.Active());
    REQUIRE(r.RollOffsetDeg(kR) == Approx(0.f));   // 끝나면 자세가 원래대로 돌아온다
}

TEST_CASE("player reaction bounded: the fish never spins off the screen") {
    PlayerReaction r;
    r.Touch(3u);
    float worst = 0.f;
    for (int i = 0; i < 200; ++i) {
        r.Step(0.01f);
        const float a = r.RollOffsetDeg(kR);
        worst = (a < 0.f ? -a : a) > worst ? (a < 0.f ? -a : a) : worst;
    }
    // 한 바퀴(360도)까지는 재롱이고 그 이상은 고장으로 보인다.
    REQUIRE(worst <= 360.f + 1e-3f);
}

TEST_CASE("player reaction re-touch restarts rather than stacking") {
    PlayerReaction r;
    r.Touch(1u);
    r.Step(kR.duration * 0.9f);
    r.Touch(2u);                     // 아이가 또 찍는다
    REQUIRE(r.Active());
    r.Step(kR.duration * 0.9f);
    REQUIRE(r.Active());             // 새로 시작했으므로 아직 살아 있다
    REQUIRE(r.TouchCount() == 2);
}
```

`CMakeLists.txt`에 `tests/test_reaction.cpp`를 더한다.

```bash
cd /Users/hans/dev/aquarium && touch tests/test_reaction.cpp && cmake -S . -B build && cmake --build build -j 2>&1 | tail -4
```
기대: **컴파일 실패** — `'aquarium/Reaction.h' file not found`. RED다.

- [ ] **Step 3 (GREEN): `Flee.h`를 고친다**

`FleeStateMachine::Touch`를 아래로 바꾸고, `TouchCount()`를 더한다:

```cpp
    // 시나리오 장면 2 요구사항 4: 연타가 이 게임의 기본 사용법이다. M5의 F-11은
    // 도망 중 재터치를 **무시**했는데, 아이에게 그것은 "고장났다"로 읽힌다. 이제
    // 재터치는 다시 겨누고 타이머를 새로 채운다. F-11이 원래 막으려던 것은 상태가
    // 꼬이는 것이었고, 아래 코드는 재진입해도 꼬이지 않는다(모든 필드를 다시 쓴다).
    void Touch(Vec2 touch, Vec2 fishPos, Vec2 velocity, Vec2 fallbackDir, const FleeParams& p) {
        fleeDir_ = ComputeFleeDirection(touch, fishPos, velocity, fallbackDir);
        state_ = BehaviorState::Fleeing;
        timer_ = p.fleeDuration;
        recoverDuration_ = p.recoverDuration;
        ++touchCount_;
    }

    // 받은 터치의 총 수. 삼킨 클릭이 하나도 없다는 것을 테스트가 확인하는 수단이다.
    int TouchCount() const { return touchCount_; }
```

private에 `int touchCount_ = 0;`을 더한다.

- [ ] **Step 4 (GREEN): 뒤집힌 기존 테스트를 고친다**

Step 1에서 찾은 "도망 중 재터치 무시" 테스트를 **지우지 말고** 아래처럼 뒤집는다(이름과 주석에 이유를 남긴다):

```cpp
TEST_CASE("re-touch mid-flee re-aims (M7: F-11's ignore was removed on purpose)") {
    // M5까지는 이 테스트가 '무시된다'를 단언했다. 시나리오(장면 2 요구사항 4)가
    // 연타를 기본 사용법으로 못 박으면서 사양이 뒤집혔다. 기록을 남기려고 지우지
    // 않고 뒤집어 둔다.
    FleeStateMachine m;
    m.Touch({0.f, 0.f}, {10.f, 0.f}, {0.f, 0.f}, {0.f, 1.f}, kP);
    m.Step(0.1f);
    m.Touch({20.f, 0.f}, {10.f, 0.f}, {0.f, 0.f}, {0.f, 1.f}, kP);
    REQUIRE(m.FleeDirection().x < 0.f);
    REQUIRE(m.TouchCount() == 2);
}
```

- [ ] **Step 5 (GREEN): `rules/include/aquarium/Reaction.h`를 쓴다**

```cpp
#pragma once
#include <cstdint>

#include "aquarium/Flee.h"
#include "aquarium/Vec2.h"

namespace aquarium {

// 시나리오 장면 2 요구사항 2: "어떤 놈은 빙글 돌고, 어떤 놈은 삐끗 뒤집히고,
// 어떤 놈은 그 자리에서 파르르 떤다." 세 가지면 충분하다 -- 아이가 세는 것이
// 아니라 "매번 다르네"를 느끼면 되는 것이고, 여기에 재생별 음높이까지 겹치면
// 같은 반응이 두 번 오는 일이 사실상 없다.
enum class ReactionStyle : int { Dart = 0, Spin = 1, Tumble = 2 };

// 결정적 해시. Audio.h의 JitterPitch와 같은 splitmix32를 쓰되 상수를 달리해
// 같은 시드에서 스타일과 음높이가 붙어 다니지 않게 한다.
inline std::uint32_t ReactionHash(std::uint32_t seed) {
    std::uint32_t x = seed + 0x85EBCA6Bu;
    x ^= x >> 16; x *= 0x7FEB352Du;
    x ^= x >> 15; x *= 0x846CA68Bu;
    x ^= x >> 16;
    return x;
}

inline ReactionStyle PickReactionStyle(std::uint32_t seed) {
    return static_cast<ReactionStyle>(static_cast<int>(ReactionHash(seed) % 3u));
}

// 한 번의 놀람이 몸으로 보이는 모양. 속도 배율은 FleeParams에서 **파생**한다 --
// 도망 속도를 두 군데 적지 않기 위해서다(규약 7).
struct StartleShape {
    float speedScale = 1.f;      // 최대 속도 배율
    float spinDegPerSec = 0.f;   // 제자리 회전(부호 있음). 0이면 회전 없음
    float wobbleDeg = 0.f;       // 파르르 떠는 진폭
};

inline StartleShape ShapeFor(ReactionStyle style, const FleeParams& p) {
    StartleShape s;
    switch (style) {
    case ReactionStyle::Dart:    // 그냥 쌩 달아난다. 가장 빠르고 몸짓은 떨림뿐.
        s.speedScale = p.fleeSpeedScale * 1.15f;
        s.spinDegPerSec = 0.f;
        s.wobbleDeg = 6.f;
        break;
    case ReactionStyle::Spin:    // 빙글 돌면서 달아난다.
        s.speedScale = p.fleeSpeedScale;
        s.spinDegPerSec = 520.f;
        s.wobbleDeg = 0.f;
        break;
    case ReactionStyle::Tumble:  // 삐끗 뒤집히며 흔들린다. 가장 덜 빠르고 가장 우습다.
        s.speedScale = p.fleeSpeedScale * 0.85f;
        s.spinDegPerSec = -260.f;
        s.wobbleDeg = 22.f;
        break;
    }
    return s;
}

struct PlayerReactionParams {
    float duration = 0.55f;
    float spinDegTotal = 360.f;      // Spin일 때 한 바퀴. 그 이상은 고장으로 보인다
    float shiverAmplitudeDeg = 20.f; // Dart/Tumble일 때 파르르
    float shiverHz = 9.f;
};

// **내 물고기 전용.** 시나리오 결정표: "도망가지 않는다. 그 자리에서 파르르
// 떨거나 빙글 돌고 기포를 낸다. 조종권을 한 순간도 잃지 않는다."
//
// 그것을 주석이 아니라 타입으로 보장한다: 이 클래스에는 방향·속도·입력을
// 돌려주는 함수가 하나도 없다. 있는 것은 **시각 오프셋 각도** 하나뿐이라,
// 이 반응이 조종을 건드리는 코드를 쓰는 것 자체가 불가능하다.
class PlayerReaction {
public:
    void Touch(std::uint32_t seed) {
        style_ = PickReactionStyle(seed);
        timer_ = -1.f;            // Step이 처음 불릴 때 duration으로 채운다
        elapsed_ = 0.f;
        active_ = true;
        ++touchCount_;
    }

    void Step(float dt) {
        if (!active_ || dt <= 0.f) return;
        elapsed_ += dt;
        (void)timer_;
    }

    bool Active() const { return active_; }
    ReactionStyle Style() const { return style_; }
    int TouchCount() const { return touchCount_; }

    // 몸을 돌리는 **시각** 각도(도). 조종과 무관하고, 끝나면 정확히 0으로 돌아온다.
    float RollOffsetDeg(const PlayerReactionParams& p) const {
        if (!active_ || p.duration <= 0.f) return 0.f;
        const float t = elapsed_ / p.duration;
        if (t >= 1.f) return 0.f;
        const float fade = 1.f - t;                    // 끝으로 갈수록 잦아든다
        if (style_ == ReactionStyle::Spin) {
            // 한 바퀴를 duration 동안 돈 뒤 정확히 0에서 끝난다.
            const float a = p.spinDegTotal * t;
            return a >= p.spinDegTotal ? 0.f : a;
        }
        // 파르르: 사인 몇 번, 진폭은 점점 줄어든다.
        const float phase = 6.2831853f * p.shiverHz * elapsed_;
        return p.shiverAmplitudeDeg * fade * FastSin(phase);
    }

    // Step이 Active를 내려 주어야 하므로 여기서 갱신한다(상수 함수가 아니다).
    void Update(float dt, const PlayerReactionParams& p) {
        Step(dt);
        if (active_ && elapsed_ >= p.duration) { active_ = false; elapsed_ = 0.f; }
    }

private:
    // <cmath>를 끌어오지 않으려고 쓰는 5차 근사. |오차| < 0.001 이고, 이 용도에는
    // 눈으로 구분되지 않는다. 정확도가 필요한 곳(Facing.h)은 std::sin을 그대로 쓴다.
    static float FastSin(float x) {
        const float twoPi = 6.2831853f;
        while (x > 3.14159265f) x -= twoPi;
        while (x < -3.14159265f) x += twoPi;
        const float x2 = x * x;
        return x * (1.f - x2 * (0.16666667f - x2 * (0.00833333f - x2 * 0.00019841f)));
    }

    ReactionStyle style_ = ReactionStyle::Dart;
    float timer_ = 0.f;
    float elapsed_ = 0.f;
    bool active_ = false;
    int touchCount_ = 0;
};

} // namespace aquarium
```

**주의**: 위 테스트는 `r.Step(dt)`만 부르고 `Active()`가 꺼지기를 기대한다. `Step`만으로는 꺼지지 않으므로 **테스트가 아니라 구현을 맞춘다** — `Step(float dt)`를 아래로 바꿔 기본 파라미터로 스스로 만료시킨다:

```cpp
    void Step(float dt) {
        PlayerReactionParams p;   // 기본 파라미터로 만료 판정(엔진은 Update를 쓴다)
        if (!active_ || dt <= 0.f) return;
        elapsed_ += dt;
        if (elapsed_ >= p.duration) { active_ = false; elapsed_ = 0.f; }
    }

    void Update(float dt, const PlayerReactionParams& p) {
        if (!active_ || dt <= 0.f) return;
        elapsed_ += dt;
        if (elapsed_ >= p.duration) { active_ = false; elapsed_ = 0.f; }
    }
```
그리고 `timer_` 멤버와 `(void)timer_;`는 지운다(쓰이지 않는 상태를 남기지 않는다).

```bash
cd /Users/hans/dev/aquarium && touch tests/test_reaction.cpp tests/test_flee.cpp rules/include/aquarium/Reaction.h rules/include/aquarium/Flee.h && cmake --build build -j 2>&1 | tail -3 && ctest --test-dir build 2>&1 | tail -3
```
기대: `100% tests passed out of 118`.

- [ ] **Step 6: 변이 확인**

| 변이 | 바꿀 곳 | 빨간불 |
|---|---|---|
| A | `PickReactionStyle`를 `return ReactionStyle::Dart;`로 | `reaction style varies between fish` |
| B | `Flee.h`의 `Touch` 맨 앞에 `if (state_ == BehaviorState::Fleeing) return;`를 되살린다(= M5 동작) | `re-touch mid-flee re-aims...` 두 개 + `every touch is counted` |
| C | `SpeedScale`의 Fleeing 분기를 `return 1.f + (p.fleeSpeedScale - 1.f) * 0.1f;`로 (= 램프 인) | `reaction starts instantly, with no ramp-in` |
| D | `ShapeFor`의 세 분기를 전부 같은 값으로 | `every style startles...` |

변이 B가 이 태스크의 핵심이다 — **M5의 옛 동작을 되살리면 빨간불이 켜져야 한다.** 전부 되돌린다.

- [ ] **Step 7: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add rules tests CMakeLists.txt && git commit -q -F - << 'EOF'
feat(m7): 반응의 규칙 — 스타일 3종, 즉시성, 내 물고기 재롱

**F-11의 사양을 의도적으로 바꿨다.** M5는 도망 중 재터치를 무시했는데,
시나리오 장면 2 요구사항 4가 연타를 기본 사용법으로 못 박았다. 이제 재터치는
다시 겨누고 타이머를 새로 채우며, 모든 터치를 센다. 뒤집힌 기존 테스트는
지우지 않고 이유를 적어 뒤집었고, 옛 동작을 되살리는 변이로 빨간불을 확인했다.

PlayerReaction에는 방향·속도·입력을 돌려주는 함수가 하나도 없다. 내 물고기가
조종권을 잃지 않는다는 요구를 주석이 아니라 타입으로 보장한다.

규칙 110 → 118.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 9: 규칙 계층 — 기포 (`Bubbles.h`), 규칙 118 → 124

**Files:** Create `rules/include/aquarium/Bubbles.h`, Create `tests/test_bubbles.cpp`, Modify `CMakeLists.txt`

시나리오 장면 2 요구사항 3: **"기포는 위로 올라가 화면 위에서 사라진다. 중간에 툭 없어지면 아이가 알아챈다 — 아이들은 끝까지 눈으로 따라간다. 사실성이 아니라 시선의 약속이고, 먹이 기포를 포함한 이 게임의 모든 기포에 똑같이 적용된다."**

그래서 **수명(lifetime)으로 지우지 않는다.** 지우는 조건은 오직 **화면 위를 넘었는지** 하나뿐이고, 그 높이는 물고기 유영 영역에서 **파생**한다(규약 7).

- [ ] **Step 1 (RED): `tests/test_bubbles.cpp`**

```cpp
#include <catch2/catch_test_macros.hpp>
#include <catch2/catch_approx.hpp>

#include "aquarium/Bubbles.h"

using namespace aquarium;
using Catch::Approx;

namespace {
const BubbleParams kB{};
const float kTop = 150.f;   // 유영 영역 반높이. 엔진이 Rect에서 파생해 넘긴다
}

TEST_CASE("a bubble rises") {
    Bubble b = MakeBubble({10.f, -100.f}, 400.f, 1u, kB);
    const float y0 = b.position.y;
    StepBubble(b, 0.5f, kB);
    REQUIRE(b.position.y > y0);
}

TEST_CASE("bubbles never disappear below the top of the screen (scene 2, req 3)") {
    // 이것이 '시선의 약속'이다. 어떤 시드로도, 얼마나 오래 흘러도, 화면 위를
    // 넘기 전에는 절대 사라지지 않는다.
    for (unsigned s = 1; s <= 64; ++s) {
        Bubble b = MakeBubble({0.f, -kTop}, 400.f, s, kB);
        for (int i = 0; i < 600; ++i) {          // 10초
            StepBubble(b, 1.f / 60.f, kB);
            if (BubbleIsGone(b, kTop)) {
                REQUIRE(b.position.y >= kTop);   // 넘었을 때만 사라졌다
                break;
            }
            REQUIRE(b.position.y < kTop);
        }
    }
}

TEST_CASE("a bubble does eventually leave the top (it is not immortal)") {
    Bubble b = MakeBubble({0.f, -kTop}, 400.f, 9u, kB);
    int steps = 0;
    while (!BubbleIsGone(b, kTop) && steps < 3000) { StepBubble(b, 1.f / 60.f, kB); ++steps; }
    REQUIRE(BubbleIsGone(b, kTop));
    // 그리고 너무 오래 걸리면 아이가 지겨워한다: 2*kTop 높이를 20초 안에 넘는다.
    REQUIRE(steps < 20 * 60);
}

TEST_CASE("bubbles sway but do not drift away sideways") {
    Bubble b = MakeBubble({0.f, 0.f}, 400.f, 4u, kB);
    float worst = 0.f;
    for (int i = 0; i < 600; ++i) {
        StepBubble(b, 1.f / 60.f, kB);
        const float dx = b.position.x < 0.f ? -b.position.x : b.position.x;
        worst = dx > worst ? dx : worst;
    }
    // 좌우 흔들림은 진폭 안에 갇혀 있어야 한다. 누적 표류가 있으면 기포가
    // 화면 옆으로 빠져 '위로 사라진다'는 약속이 깨진다.
    REQUIRE(worst <= kB.swayAmplitude * 1.2f);
}

TEST_CASE("bubble speed and size vary between bubbles") {
    int distinctSpeed = 0, distinctRadius = 0;
    float prevS = -1.f, prevR = -1.f;
    for (unsigned s = 1; s <= 24; ++s) {
        const Bubble b = MakeBubble({0.f, 0.f}, 400.f, s, kB);
        if (b.riseSpeed != prevS) ++distinctSpeed;
        if (b.radius != prevR) ++distinctRadius;
        prevS = b.riseSpeed; prevR = b.radius;
        REQUIRE(b.riseSpeed > 0.f);
        REQUIRE(b.radius > 0.f);
    }
    REQUIRE(distinctSpeed >= 20);
    REQUIRE(distinctRadius >= 20);
}

TEST_CASE("MakeBubble is deterministic") {
    const Bubble a = MakeBubble({3.f, 4.f}, 500.f, 42u, kB);
    const Bubble b = MakeBubble({3.f, 4.f}, 500.f, 42u, kB);
    REQUIRE(a.riseSpeed == Approx(b.riseSpeed));
    REQUIRE(a.radius == Approx(b.radius));
    REQUIRE(a.swayPhase == Approx(b.swayPhase));
}
```

`CMakeLists.txt`에 `tests/test_bubbles.cpp`를 더한다.

```bash
cd /Users/hans/dev/aquarium && touch tests/test_bubbles.cpp && cmake -S . -B build && cmake --build build -j 2>&1 | tail -4
```
기대: `'aquarium/Bubbles.h' file not found`. RED다.

- [ ] **Step 2 (GREEN): `rules/include/aquarium/Bubbles.h`**

```cpp
#pragma once
#include <cstdint>

#include "aquarium/Reaction.h"   // ReactionHash
#include "aquarium/Vec2.h"

namespace aquarium {

// 한 개의 기포. 액터가 아니다 -- 엔진은 이것의 배열을 들고 인스턴스 메시 한 개로
// 그린다. 기포 200개가 떠 있어도 틱은 하나다.
struct Bubble {
    Vec2 position;        // 공유 유영 프레임(x = 월드 Y, y = 월드 Z)
    float depth = 0.f;    // 월드 X. 클릭이 맞힌 평면에서 온다
    float riseSpeed = 0.f;
    float swayPhase = 0.f;
    float radius = 0.f;
    float age = 0.f;
    float baseX = 0.f;    // 좌우 흔들림의 기준선. 표류를 구조적으로 막는다
};

struct BubbleParams {
    float riseSpeed = 62.f;        // cm/s
    float riseSpeedJitter = 0.30f; // +-30%
    float swayAmplitude = 7.f;     // cm
    float swayHz = 0.9f;
    float radius = 3.2f;           // cm
    float radiusJitter = 0.45f;
};

inline float BubbleUnit(std::uint32_t seed) {
    return static_cast<float>(ReactionHash(seed) >> 8) / 16777216.f;   // [0,1)
}

inline Bubble MakeBubble(Vec2 at, float depth, std::uint32_t seed, const BubbleParams& p) {
    Bubble b;
    b.position = at;
    b.baseX = at.x;
    b.depth = depth;
    b.riseSpeed = p.riseSpeed * (1.f + (BubbleUnit(seed * 3u + 1u) * 2.f - 1.f) * p.riseSpeedJitter);
    b.radius = p.radius * (1.f + (BubbleUnit(seed * 5u + 7u) * 2.f - 1.f) * p.radiusJitter);
    b.swayPhase = BubbleUnit(seed * 11u + 3u) * 6.2831853f;
    return b;
}

// 위치를 한 걸음 옮긴다. **수명이 없다.** 지우는 조건은 BubbleIsGone 하나뿐이다.
inline void StepBubble(Bubble& b, float dt, const BubbleParams& p) {
    if (dt <= 0.f) return;
    b.age += dt;
    b.position.y += b.riseSpeed * dt;
    // 좌우는 기준선 주위의 사인이다. 속도를 누적하지 않으므로 표류가 생길 수 없다.
    const float phase = b.swayPhase + 6.2831853f * p.swayHz * b.age;
    b.position.x = b.baseX + p.swayAmplitude * PlayerReactionSin(phase);
}

// 화면 위를 넘었을 때만 참이다. 시간으로는 절대 사라지지 않는다 -- 시나리오
// 장면 2 요구사항 3의 '시선의 약속'이 여기 한 줄로 표현돼 있다. topY는 물고기
// 유영 영역의 반높이에서 **파생**해 엔진이 넘긴다(숫자를 베끼지 않는다).
inline bool BubbleIsGone(const Bubble& b, float topY) {
    return b.position.y >= topY;
}

} // namespace aquarium
```

`PlayerReactionSin`은 `Reaction.h`의 `PlayerReaction::FastSin`을 재사용해야 하는데 private이다. **중복을 만들지 않기 위해** `Reaction.h`에서 그 함수를 클래스 밖 자유 함수로 꺼낸다:

`Reaction.h`에서 `static float FastSin(float x)`를 지우고, 네임스페이스 수준에 아래를 넣은 뒤 `RollOffsetDeg` 안의 `FastSin(phase)`를 `PlayerReactionSin(phase)`로 바꾼다:

```cpp
// <cmath>를 끌어오지 않는 5차 사인 근사. |오차| < 0.001. Bubbles.h도 이것을 쓴다 --
// 같은 근사를 두 번 적지 않기 위해 클래스 밖에 둔다.
inline float PlayerReactionSin(float x) {
    const float twoPi = 6.2831853f;
    while (x > 3.14159265f) x -= twoPi;
    while (x < -3.14159265f) x += twoPi;
    const float x2 = x * x;
    return x * (1.f - x2 * (0.16666667f - x2 * (0.00833333f - x2 * 0.00019841f)));
}
```

```bash
cd /Users/hans/dev/aquarium && touch tests/test_bubbles.cpp rules/include/aquarium/Bubbles.h rules/include/aquarium/Reaction.h && cmake --build build -j 2>&1 | tail -3 && ctest --test-dir build 2>&1 | tail -3
```
기대: `100% tests passed out of 124`.

- [ ] **Step 3: 변이 확인**

| 변이 | 바꿀 곳 | 빨간불 |
|---|---|---|
| A | `BubbleIsGone`를 `return b.age > 1.5f;`로 (= 수명으로 지우기, 시나리오가 금지한 바로 그것) | `bubbles never disappear below the top of the screen` |
| B | `StepBubble`의 `b.position.x = b.baseX + ...`를 `b.position.x += p.swayAmplitude * dt;`로 (= 누적 표류) | `bubbles sway but do not drift away sideways` |
| C | `MakeBubble`의 `riseSpeed`/`radius` 흔들기를 지우고 기본값만 쓴다 | `bubble speed and size vary between bubbles` |

변이 A가 이 태스크의 핵심이다. 전부 되돌린다.

- [ ] **Step 4: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add rules tests CMakeLists.txt && git commit -q -F - << 'EOF'
feat(m7): 기포의 규칙 — 화면 위를 넘을 때만 사라진다

시나리오 장면 2 요구사항 3의 '시선의 약속'을 코드 한 줄로 표현했다.
BubbleIsGone은 높이만 본다 — 수명이 아예 없다. 수명으로 지우는 변이(아이가
알아채는 바로 그 구현)로 빨간불을 확인했다. 좌우 흔들림은 기준선 주위의
사인이라 누적 표류가 구조적으로 불가능하다.

규칙 118 → 124.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 10: 장면 2 — 클릭이 "비켰다"에서 "놀랐다"로, Automation 68 → 72

**Files:** Create `unreal/Aquarium/Scripts/build_fx_m7.py`, Create `.../InstancedFieldActor.h/.cpp`, Create `.../BubbleSubsystem.h/.cpp`, Modify `.../FishActor.h/.cpp`, Modify `.../FishSchoolSubsystem.cpp`, Modify `.../DiverPlayerController.cpp`, Create `.../Tests/BubbleTests.cpp`

**Niagara를 쓰지 않는다**(에디터 Python으로 만들 수 없다). 기포는 **인스턴스 스태틱 메시 하나 + 해석적 반투명 머티리얼**이다 — M4b의 마린 스노가 검증한 경로와 같은 성질이고, 7살은 기포의 물리를 보지 않고 "내가 찍은 자리에서 뭔가 올라갔다"만 본다.

- [ ] **Step 1: 머티리얼 두 개를 만드는 에디터 Python**

`unreal/Aquarium/Scripts/build_fx_m7.py`:

```python
# M7이 런타임에 쓰는 머티리얼 두 개를 만든다. 레벨은 건드리지 않는다 --
# 기포도 먹이도 액터가 아니라 서브시스템이 런타임에 스폰하는 인스턴스라서,
# ReefM1.umap은 M7에서 한 바이트도 바뀌지 않는다(verify_scene.py 기대값 불변).
#
# Niagara는 쓰지 않는다: 에디터 파이썬으로 만들 수 없다(M4b에서 부딪힌 벽).
# 대신 M4b의 마린 스노와 같은 해석적 반투명 머티리얼을 쓴다.
#
# 판정은 마지막 FX_OK 줄로만 한다.
import unreal

DEST = "/Game/Fx"
tools = unreal.AssetToolsHelpers.get_asset_tools()
eal = unreal.EditorAssetLibrary
mel = unreal.MaterialEditingLibrary


def material(name):
    path = DEST + "/" + name
    if eal.does_asset_exist(path):
        eal.delete_asset(path)          # 멱등: 매번 같은 그래프를 다시 만든다
    return tools.create_asset(name, DEST, unreal.Material, unreal.MaterialFactoryNew())


def expr(mat, cls, x, y):
    return mel.create_material_expression(mat, cls, x, y)


def build_bubble(mat):
    """가장자리가 밝고 가운데가 비는 기포. Fresnel 하나면 구가 기포처럼 읽힌다."""
    mat.set_editor_property("blend_mode", unreal.BlendMode.BLEND_TRANSLUCENT)
    mat.set_editor_property("shading_model", unreal.MaterialShadingModel.MSM_UNLIT)
    mat.set_editor_property("two_sided", True)
    fres = expr(mat, unreal.MaterialExpressionFresnel, -600, 0)
    fres.set_editor_property("exponent", 2.6)
    fres.set_editor_property("base_reflect_fraction", 0.06)
    tint = expr(mat, unreal.MaterialExpressionConstant3Vector, -600, 220)
    tint.set_editor_property("constant", unreal.LinearColor(0.72, 0.88, 1.0, 1.0))
    emis = expr(mat, unreal.MaterialExpressionMultiply, -320, 120)
    mel.connect_material_expressions(tint, "", emis, "A")
    mel.connect_material_expressions(fres, "", emis, "B")
    gain = expr(mat, unreal.MaterialExpressionMultiply, -160, 120)
    gval = expr(mat, unreal.MaterialExpressionConstant, -320, 300)
    gval.set_editor_property("r", 1.8)
    mel.connect_material_expressions(emis, "", gain, "A")
    mel.connect_material_expressions(gval, "", gain, "B")
    mel.connect_material_property(gain, "", unreal.MaterialProperty.MP_EMISSIVE_COLOR)
    opac = expr(mat, unreal.MaterialExpressionMultiply, -160, 380)
    oval = expr(mat, unreal.MaterialExpressionConstant, -320, 460)
    oval.set_editor_property("r", 0.85)
    mel.connect_material_expressions(fres, "", opac, "A")
    mel.connect_material_expressions(oval, "", opac, "B")
    mel.connect_material_property(opac, "", unreal.MaterialProperty.MP_OPACITY)


def build_pellet(mat):
    """하얀 먹이 알갱이. 어두운 물속에서 또렷하게 보여야 하므로 언릿 발광이다."""
    mat.set_editor_property("shading_model", unreal.MaterialShadingModel.MSM_UNLIT)
    col = expr(mat, unreal.MaterialExpressionConstant3Vector, -320, 0)
    col.set_editor_property("constant", unreal.LinearColor(1.0, 0.97, 0.86, 1.0))
    gain = expr(mat, unreal.MaterialExpressionMultiply, -160, 0)
    gval = expr(mat, unreal.MaterialExpressionConstant, -320, 160)
    gval.set_editor_property("r", 2.4)
    mel.connect_material_expressions(col, "", gain, "A")
    mel.connect_material_expressions(gval, "", gain, "B")
    mel.connect_material_property(gain, "", unreal.MaterialProperty.MP_EMISSIVE_COLOR)


built = []
for name, build in (("M_Bubble", build_bubble), ("M_FoodPellet", build_pellet)):
    m = material(name)
    assert m is not None, "could not create %s" % name
    build(m)
    mel.recompile_material(m)
    eal.save_loaded_asset(m)
    built.append(name)
    print("  built %s" % name)

# 런타임이 쓰는 엔진 기본 구 메시가 실재하는지 여기서 확인한다. 없으면 기포와
# 먹이가 '보이지 않는 인스턴스'가 되어 조용히 아무것도 안 그린다.
sphere = unreal.load_asset("/Engine/BasicShapes/Sphere.Sphere")
assert sphere is not None, "missing /Engine/BasicShapes/Sphere"
print("FX_OK count=%d materials=[%s] sphere=%s" % (len(built), " ".join(built), sphere.get_path_name()))
```

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" \
  -run=pythonscript -script="$PWD/unreal/Aquarium/Scripts/build_fx_m7.py" \
  -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -E "FX_OK|built |Traceback|AssertionError"
```
기대: `built M_Bubble`, `built M_FoodPellet`, `FX_OK count=2 ...`. **표현식 클래스 이름이 틀리다는 오류가 나면** `build_reef_m1.py`가 실제로 쓰는 이름을 grep해서 맞춘다(`grep -n "MaterialExpression" unreal/Aquarium/Scripts/build_reef_m1.py`) — 계획의 이름을 믿지 않는다.

- [ ] **Step 2: 인스턴스 필드 액터** — 기포와 먹이가 함께 쓴다.

`unreal/Aquarium/Source/Aquarium/InstancedFieldActor.h`:

```cpp
#pragma once

#include "CoreMinimal.h"
#include "GameFramework/Actor.h"

#include "InstancedFieldActor.generated.h"

class UInstancedStaticMeshComponent;
class UMaterialInterface;
class UStaticMesh;

// 작은 것 여러 개를 **틱 없이** 그리는 액터. 기포와 먹이가 각각 하나씩 쓴다.
//
// 액터당 틱을 만들지 않는 것이 요구사항이다(먹이에는 개수 제한이 없어야 하므로
// 아이가 화면을 하얗게 덮을 때까지 뿌릴 수 있어야 한다). 이 액터는 상태를 갖지
// 않고 시뮬레이션도 하지 않는다 -- 서브시스템이 계산한 위치를 받아 인스턴스
// 변환만 통째로 갈아끼운다.
UCLASS()
class AQUARIUM_API AInstancedFieldActor : public AActor
{
	GENERATED_BODY()

public:
	AInstancedFieldActor();

	// 엔진 기본 구 메시 + 지정한 머티리얼로 컴포넌트를 준비한다. 두 번 불러도 안전하다.
	void Configure(UStaticMesh* Mesh, UMaterialInterface* Material);
	// 이번 프레임의 인스턴스 전체. 개수가 바뀌어도 한 번의 배치 갱신으로 끝난다.
	void UpdateInstances(const TArray<FTransform>& Transforms);
	int32 InstanceCount() const;
	UInstancedStaticMeshComponent* Instances() const { return Mesh; }

private:
	UPROPERTY() TObjectPtr<UInstancedStaticMeshComponent> Mesh = nullptr;
};
```

`InstancedFieldActor.cpp`:

```cpp
#include "InstancedFieldActor.h"

#include "Components/InstancedStaticMeshComponent.h"
#include "Engine/StaticMesh.h"
#include "Materials/MaterialInterface.h"

AInstancedFieldActor::AInstancedFieldActor()
{
	// 액터 틱 없음. 이것이 이 클래스의 존재 이유다.
	PrimaryActorTick.bCanEverTick = false;
	Mesh = CreateDefaultSubobject<UInstancedStaticMeshComponent>(TEXT("Instances"));
	RootComponent = Mesh;
	Mesh->SetCollisionEnabled(ECollisionEnabled::NoCollision);
	Mesh->SetCastShadow(false);                 // 알갱이 수백 개의 그림자는 순전히 낭비다
	Mesh->SetMobility(EComponentMobility::Movable);
}

void AInstancedFieldActor::Configure(UStaticMesh* InMesh, UMaterialInterface* Material)
{
	if (Mesh == nullptr || InMesh == nullptr)
	{
		return;
	}
	Mesh->SetStaticMesh(InMesh);
	if (Material)
	{
		Mesh->SetMaterial(0, Material);
	}
}

void AInstancedFieldActor::UpdateInstances(const TArray<FTransform>& Transforms)
{
	if (Mesh == nullptr)
	{
		return;
	}
	// 개수가 같으면 변환만 갈아끼우고, 다르면 통째로 다시 만든다. 한 프레임에
	// 인스턴스 하나씩 더하고 빼는 것보다 이쪽이 싸고, 무엇보다 상태가 없다.
	if (Mesh->GetInstanceCount() != Transforms.Num())
	{
		Mesh->ClearInstances();
		for (const FTransform& T : Transforms) { Mesh->AddInstance(T, /*bWorldSpace*/ true); }
		return;
	}
	for (int32 i = 0; i < Transforms.Num(); ++i)
	{
		Mesh->UpdateInstanceTransform(i, Transforms[i], /*bWorldSpace*/ true,
			/*bMarkRenderStateDirty*/ i == Transforms.Num() - 1, /*bTeleport*/ true);
	}
}

int32 AInstancedFieldActor::InstanceCount() const
{
	return Mesh ? Mesh->GetInstanceCount() : 0;
}
```

- [ ] **Step 3: 기포 서브시스템**

`unreal/Aquarium/Source/Aquarium/BubbleSubsystem.h`:

```cpp
#pragma once

#include "CoreMinimal.h"
#include "Subsystems/WorldSubsystem.h"

#include <vector>

#include "aquarium/Bubbles.h"

#include "BubbleSubsystem.generated.h"

class AInstancedFieldActor;

// 이 게임의 모든 기포. 클릭 한 번에 한 줄기씩 생기고, **화면 위를 넘을 때만**
// 사라진다(시나리오 장면 2 요구사항 3 — 시선의 약속). 액터 틱은 0개다.
UCLASS()
class AQUARIUM_API UBubbleSubsystem : public UTickableWorldSubsystem
{
	GENERATED_BODY()

public:
	virtual void Initialize(FSubsystemCollectionBase& Collection) override;
	virtual void Tick(float DeltaTime) override;
	virtual TStatId GetStatId() const override { RETURN_QUICK_DECLARE_CYCLE_STAT(UBubbleSubsystem, STATGROUP_Tickables); }

	// 한 줄기(Count개)를 월드 지점에서 띄운다. Seed가 속도·크기·흔들림을 정한다.
	void Spawn(const FVector& WorldPoint, int32 Count, uint32 Seed);
	int32 ActiveCount() const { return static_cast<int32>(Bubbles.size()); }
	int32 SpawnedTotal() const { return SpawnedTotalValue; }

	// 기포가 사라지는 높이(월드 Z). 물고기 유영 영역의 위 끝에서 **파생**한다.
	// 여기에 리터럴을 쓰면 M3의 화면 비율 대응이 조용히 깨진다.
	float TopZ() const { return TopZValue; }
	void SetTopZ(float InTopZ) { TopZValue = InTopZ; }

	AInstancedFieldActor* Field() const { return FieldActor; }

private:
	std::vector<aquarium::Bubble> Bubbles;
	aquarium::BubbleParams BubbleParamsValue;
	UPROPERTY() TObjectPtr<AInstancedFieldActor> FieldActor = nullptr;
	float TopZValue = 400.f;
	int32 SpawnedTotalValue = 0;
	uint32 NextSeed = 1u;
	void EnsureField();
};
```

`BubbleSubsystem.cpp`:

```cpp
#include "BubbleSubsystem.h"

#include "Engine/StaticMesh.h"
#include "Engine/World.h"
#include "Materials/MaterialInterface.h"
#include "InstancedFieldActor.h"

void UBubbleSubsystem::Initialize(FSubsystemCollectionBase& Collection)
{
	Super::Initialize(Collection);
	Bubbles.clear();
}

void UBubbleSubsystem::EnsureField()
{
	if (FieldActor != nullptr)
	{
		return;
	}
	UWorld* W = GetWorld();
	if (W == nullptr)
	{
		return;
	}
	FActorSpawnParameters P;
	P.SpawnCollisionHandlingOverride = ESpawnActorCollisionHandlingMethod::AlwaysSpawn;
	FieldActor = W->SpawnActor<AInstancedFieldActor>(AInstancedFieldActor::StaticClass(),
		FVector::ZeroVector, FRotator::ZeroRotator, P);
	if (FieldActor)
	{
		UStaticMesh* Sphere = LoadObject<UStaticMesh>(nullptr, TEXT("/Engine/BasicShapes/Sphere.Sphere"));
		UMaterialInterface* Mat = LoadObject<UMaterialInterface>(nullptr, TEXT("/Game/Fx/M_Bubble.M_Bubble"));
		FieldActor->Configure(Sphere, Mat);
	}
}

void UBubbleSubsystem::Spawn(const FVector& WorldPoint, int32 Count, uint32 Seed)
{
	// 개수에 상한을 두지 않는다. 시나리오가 금지한 것은 아이가 느끼는 제한이고,
	// 기포는 화면 위를 넘으면 스스로 사라지므로 상한 없이도 유한하다.
	EnsureField();
	for (int32 i = 0; i < Count; ++i)
	{
		const uint32 S = Seed * 131u + static_cast<uint32>(i) * 17u + NextSeed++;
		aquarium::Bubble B = aquarium::MakeBubble(
			{static_cast<float>(WorldPoint.Y), static_cast<float>(WorldPoint.Z)},
			static_cast<float>(WorldPoint.X), S, BubbleParamsValue);
		Bubbles.push_back(B);
		++SpawnedTotalValue;
	}
}

void UBubbleSubsystem::Tick(float DeltaTime)
{
	Super::Tick(DeltaTime);
	if (Bubbles.empty())
	{
		if (FieldActor && FieldActor->InstanceCount() > 0) { FieldActor->UpdateInstances({}); }
		return;
	}
	TArray<FTransform> Transforms;
	Transforms.Reserve(static_cast<int32>(Bubbles.size()));
	size_t write = 0;
	for (size_t i = 0; i < Bubbles.size(); ++i)
	{
		aquarium::Bubble& B = Bubbles[i];
		aquarium::StepBubble(B, DeltaTime, BubbleParamsValue);
		// 지우는 조건은 높이 하나뿐이다. 수명으로 지우면 아이가 알아챈다.
		if (aquarium::BubbleIsGone(B, TopZValue))
		{
			continue;
		}
		Bubbles[write++] = B;
		Transforms.Add(FTransform(FQuat::Identity,
			FVector(B.depth, B.position.x, B.position.y),
			FVector(B.radius / 50.f)));   // 엔진 구 메시는 반지름 50 cm다
	}
	Bubbles.resize(write);
	EnsureField();
	if (FieldActor) { FieldActor->UpdateInstances(Transforms); }
}
```

**주의**: `BubbleIsGone`은 공유 프레임의 y(= 월드 Z)를 본다. `TopZValue`도 **월드 Z**여야 한다. 다음 스텝에서 물고기 영역에서 파생해 넣는다.

- [ ] **Step 4: 놀람을 과장하고 기포·소리를 붙인다**

`FishActor.h`의 public에:

```cpp
	// 이번 놀람이 어떤 모양인지. 클릭마다 달라진다(시나리오 장면 2 요구사항 2).
	aquarium::ReactionStyle StartleStyle() const { return StartleStyleValue; }
	// 놀람 중 시각 회전(도/초). 몸짓을 만들되 이동에는 영향이 없다.
	float StartleSpinDegPerSec() const;
```

private에:

```cpp
	aquarium::ReactionStyle StartleStyleValue = aquarium::ReactionStyle::Dart;
	aquarium::StartleShape StartleShapeValue;
	float StartleSpinDeg = 0.f;     // 누적 시각 회전(도)
	aquarium::PlayerReaction PlayerReactionValue;
	aquarium::PlayerReactionParams PlayerReactionParamsValue;
```

`FishActor.h` 상단 include에 `#include "aquarium/Reaction.h"`를 더한다.

`FishActor.cpp`의 `ApplyFleeFrom`을 아래로 바꾼다:

```cpp
void AFishActor::ApplyFleeFrom(const FVector& WorldTouch)
{
	const aquarium::Vec2 TouchLocal{
		static_cast<float>(WorldTouch.Y) - Plane.origin.y,
		static_cast<float>(WorldTouch.Z) - Plane.origin.z};
	const aquarium::Vec2 TowardCentre = (aquarium::Vec2{0.f, 0.f} - Motion.position).Normalized();

	// 시나리오 결정표: **내 물고기는 도망가지 않는다.** 그 자리에서 재롱을 부리고
	// 조종권을 한 순간도 잃지 않는다. 아이가 가장 아끼는 것이 자기 물고기라,
	// 화면 밖으로 튀어나가고 잠깐 조종이 안 먹는 것은 재미가 아니라 사고다.
	if (bIsPlayerFish || bPlayerControlled)
	{
		PlayerReactionValue.Touch(static_cast<uint32>(GetUniqueID()) + static_cast<uint32>(Flee.TouchCount()));
		return;   // Flee에는 손도 대지 않는다 -- 조종을 건드릴 경로가 아예 없다
	}

	// 매번 다른 모양으로 놀란다. 시드는 이 물고기와 이번 터치 번호에서 나온다.
	StartleStyleValue = aquarium::PickReactionStyle(
		static_cast<uint32>(GetUniqueID()) * 2654435761u + static_cast<uint32>(Flee.TouchCount()));
	StartleShapeValue = aquarium::ShapeFor(StartleStyleValue, FleeParamsValue);
	Flee.Touch(TouchLocal, Motion.position, Motion.velocity, TowardCentre, FleeParamsValue);
}

float AFishActor::StartleSpinDegPerSec() const
{
	return Flee.State() == aquarium::BehaviorState::Fleeing ? StartleShapeValue.spinDegPerSec : 0.f;
}
```

`StepSwim`의 속도 배율 한 줄을 아래로 바꾼다(스타일별 배율이 반영되도록):

```cpp
	// 스타일별 배율. ShapeFor가 FleeParams에서 파생하므로 도망 속도 상수는
	// 여전히 규칙 계층 한 곳에만 있다.
	const float StyleScale = (Flee.State() == aquarium::BehaviorState::Normal)
		? 1.f : (StartleShapeValue.speedScale / FleeParamsValue.fleeSpeedScale);
	MotionParamsValue.maxSpeed = MaxSpeed * Flee.SpeedScale(FleeParamsValue) * StyleScale;
```

그리고 `SetActorRotation(Next);` **바로 뒤**에 시각 회전을 덧입힌다:

```cpp
		// 놀람의 몸짓과 내 물고기의 재롱은 **덧붙이는 회전**이다. 속도에도
		// 입력에도 손대지 않으므로 조종권과 경계 보장이 그대로 유지된다.
		PlayerReactionValue.Update(DeltaSeconds, PlayerReactionParamsValue);
		const float SpinRate = StartleSpinDegPerSec();
		StartleSpinDeg = (Flee.State() == aquarium::BehaviorState::Normal)
			? 0.f : (StartleSpinDeg + SpinRate * DeltaSeconds);
		const float ExtraRoll = StartleSpinDeg + PlayerReactionValue.RollOffsetDeg(PlayerReactionParamsValue);
		if (FMath::Abs(ExtraRoll) > 1e-3f)
		{
			SetActorRotation(Next * FQuat(FVector::ForwardVector, FMath::DegreesToRadians(ExtraRoll)));
		}
```

- [ ] **Step 5: 클릭 경로에 소리와 기포를 붙인다**

`DiverPlayerController.cpp`의 `HandleClickRay`에서 `Fish->ApplyFleeFrom(Hit);` 앞뒤를 아래로 바꾼다:

```cpp
	const bool bOwnFish = (Fish == GM->PlayerFish());
	Fish->ApplyFleeFrom(Hit);   // 정확히 한 마리
	if (UWorld* WW = GetWorld())
	{
		if (UAquariumAudioSubsystem* Audio = WW->GetSubsystem<UAquariumAudioSubsystem>())
		{
			// 내 물고기는 「뽀글」, 남의 물고기는 「꺅」. 아이가 소리만으로 구분한다.
			Audio->PlayCue(bOwnFish ? EAquariumCue::Bubble : EAquariumCue::Startle,
				static_cast<uint32>(FMath::Rand()));
		}
		if (UBubbleSubsystem* Bubbles = WW->GetSubsystem<UBubbleSubsystem>())
		{
			Bubbles->SetTopZ(BubbleTopZ());
			Bubbles->Spawn(Hit, bOwnFish ? 5 : 9, static_cast<uint32>(FMath::Rand()));
		}
	}
	return true;
```

그리고 같은 파일에 헬퍼를 더한다(헤더에 `float BubbleTopZ() const;` 선언을 함께):

```cpp
float ADiverPlayerController::BubbleTopZ() const
{
	// 기포가 사라지는 높이는 **물고기 유영 영역에서 파생**한다. 여기에 숫자를
	// 적으면 M3이 화면 비율에 맞춰 유영 평면을 정하는 규칙과 어긋나게 되고,
	// 그 어긋남은 "기포가 화면 중간에서 사라진다"로만 드러난다.
	const AAquariumGameMode* GM = GameMode();
	const AFishActor* Fish = GM ? GM->PlayerFish() : nullptr;
	if (Fish == nullptr)
	{
		return 400.f;
	}
	// 영역 위 끝보다 조금 더 위에서 지운다 -- 화면 가장자리에서 툭 사라지면
	// 아이가 알아챈다(시선의 약속).
	return static_cast<float>(Fish->PlaneOrigin.Z) + Fish->PlaneHalfHeight * 1.25f;
}
```

include에 `#include "BubbleSubsystem.h"`를 더한다.

- [ ] **Step 6 (RED 먼저): `Tests/BubbleTests.cpp`**

```cpp
#include "Misc/AutomationTest.h"

#if WITH_DEV_AUTOMATION_TESTS && WITH_EDITOR

#include "Tests/AutomationEditorCommon.h"
#include "Engine/World.h"
#include "BubbleSubsystem.h"
#include "InstancedFieldActor.h"
#include "FishActor.h"
#include "aquarium/Reaction.h"

IMPLEMENT_SIMPLE_AUTOMATION_TEST(FBubblesRiseAndLeaveTheTop, "Aquarium.Bubbles.RiseAndLeaveTheTop",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FBubblesRiseAndLeaveTheTop::RunTest(const FString&)
{
	UWorld* World = FAutomationEditorCommonUtils::CreateNewMap();
	UBubbleSubsystem* Bubbles = World->GetSubsystem<UBubbleSubsystem>();
	if (!TestNotNull(TEXT("bubble subsystem"), Bubbles)) return false;
	Bubbles->SetTopZ(300.f);
	Bubbles->Spawn(FVector(400.f, 0.f, 100.f), 9, 5u);
	TestEqual(TEXT("nine bubbles"), Bubbles->ActiveCount(), 9);
	// 화면 위에 닿기 전에는 한 개도 줄지 않는다(시선의 약속).
	for (int32 i = 0; i < 30; ++i) { Bubbles->Tick(1.f / 60.f); }
	TestEqual(TEXT("none vanished early"), Bubbles->ActiveCount(), 9);
	// 그리고 결국은 전부 나간다(영원히 쌓이지 않는다).
	for (int32 i = 0; i < 60 * 20; ++i) { Bubbles->Tick(1.f / 60.f); }
	TestEqual(TEXT("all gone eventually"), Bubbles->ActiveCount(), 0);
	return true;
}

IMPLEMENT_SIMPLE_AUTOMATION_TEST(FBubblesUseOneInstancedActor, "Aquarium.Bubbles.UseOneInstancedActor",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FBubblesUseOneInstancedActor::RunTest(const FString&)
{
	UWorld* World = FAutomationEditorCommonUtils::CreateNewMap();
	UBubbleSubsystem* Bubbles = World->GetSubsystem<UBubbleSubsystem>();
	if (!TestNotNull(TEXT("bubble subsystem"), Bubbles)) return false;
	Bubbles->SetTopZ(2000.f);
	for (int32 i = 0; i < 40; ++i) { Bubbles->Spawn(FVector(400.f, i * 3.f, 0.f), 9, static_cast<uint32>(i)); }
	Bubbles->Tick(1.f / 60.f);
	TestEqual(TEXT("360 bubbles"), Bubbles->ActiveCount(), 360);
	// 액터는 여전히 하나다. 기포마다 액터를 만들면 여기서 빨간불이 켜진다.
	int32 FieldActors = 0;
	for (TActorIterator<AInstancedFieldActor> It(World); It; ++It) { ++FieldActors; }
	TestEqual(TEXT("exactly one field actor"), FieldActors, 1);
	if (AInstancedFieldActor* Field = Bubbles->Field())
	{
		TestEqual(TEXT("one instance per bubble"), Field->InstanceCount(), 360);
		TestFalse(TEXT("the field actor never ticks"), Field->PrimaryActorTick.bCanEverTick);
	}
	return true;
}

// 매번 조금씩 다르게 놀란다(시나리오 장면 2 요구사항 2).
IMPLEMENT_SIMPLE_AUTOMATION_TEST(FStartleStyleVariesBetweenClicks, "Aquarium.Fish.StartleStyleVaries",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FStartleStyleVariesBetweenClicks::RunTest(const FString&)
{
	UWorld* World = FAutomationEditorCommonUtils::CreateNewMap();
	FActorSpawnParameters SP;
	SP.SpawnCollisionHandlingOverride = ESpawnActorCollisionHandlingMethod::AlwaysSpawn;
	TSet<int32> Styles;
	for (int32 i = 0; i < 24; ++i)
	{
		AFishActor* Fish = World->SpawnActor<AFishActor>(AFishActor::StaticClass(), FVector::ZeroVector, FRotator::ZeroRotator, SP);
		Fish->Seed = static_cast<uint32>(i + 1);
		Fish->PlaneOrigin = FVector(300.f + i, 0.f, 100.f);
		Fish->InitializeSwim();
		Fish->ApplyFleeFrom(FVector(300.f + i, 30.f, 100.f));
		Styles.Add(static_cast<int32>(Fish->StartleStyle()));
	}
	TestEqual(TEXT("all three styles appear"), Styles.Num(), 3);
	return true;
}

// **정확성 요구**: 내 물고기는 클릭당 조종권을 한 프레임도 잃지 않는다.
IMPLEMENT_SIMPLE_AUTOMATION_TEST(FOwnFishKeepsControlWhenClicked, "Aquarium.Fish.OwnFishKeepsControl",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FOwnFishKeepsControlWhenClicked::RunTest(const FString&)
{
	UWorld* World = FAutomationEditorCommonUtils::CreateNewMap();
	FActorSpawnParameters SP;
	SP.SpawnCollisionHandlingOverride = ESpawnActorCollisionHandlingMethod::AlwaysSpawn;
	// 두 마리를 같은 조건으로 만들고, 한쪽만 클릭한다. 위치 궤적이 완전히 같아야 한다.
	auto Make = [&](int32 i) {
		AFishActor* F = World->SpawnActor<AFishActor>(AFishActor::StaticClass(), FVector::ZeroVector, FRotator::ZeroRotator, SP);
		F->Seed = 11u; F->PlaneOrigin = FVector(220.f, 0.f, 100.f);
		F->InitializeSwim();
		F->bPlayerControlled = true;
		F->SetInputDirection(FVector2D(1.f, 0.f));   // 아이가 오른쪽 키를 누르고 있다
		(void)i;
		return F;
	};
	AFishActor* Clicked = Make(0);
	AFishActor* Control = Make(1);
	for (int32 i = 0; i < 60; ++i)
	{
		if (i == 10) { Clicked->ApplyFleeFrom(FVector(220.f, 0.f, 100.f)); }
		Clicked->StepSwim(1.f / 60.f);
		Control->StepSwim(1.f / 60.f);
	}
	// 위치가 한 치도 달라지면 안 된다 -- 클릭이 이동에 영향을 주었다는 뜻이다.
	const FVector D = Clicked->GetActorLocation() - Control->GetActorLocation();
	TestTrue(TEXT("clicking my own fish does not move it"), D.Size() < 0.01f);
	TestTrue(TEXT("and it is still not fleeing"), Clicked->FleeState() == aquarium::BehaviorState::Normal);
	return true;
}

#endif
```

**include 하나가 더 필요하다**: `#include "EngineUtils.h"`(`TActorIterator`).

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
for p in 1 2; do "$UE/Engine/Build/BatchFiles/Mac/Build.sh" AquariumEditor Mac Development -Project="$PWD/unreal/Aquarium/Aquarium.uproject" -WaitMutex 2>&1 | grep -E "^Result:|error:"; done
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" -ExecCmds="Automation RunTests Aquarium; Quit" -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -cE "Test Completed.*Success"
```
기대: `72`.

- [ ] **Step 7: 변이 확인**

| 변이 | 바꿀 곳 | 빨간불 |
|---|---|---|
| A | `BubbleSubsystem::Tick`의 `BubbleIsGone(B, TopZValue)`를 `B.age > 0.4f`로 | `Aquarium.Bubbles.RiseAndLeaveTheTop` |
| B | `Spawn`에서 기포마다 `AInstancedFieldActor`를 새로 스폰하게 바꾼다 | `Aquarium.Bubbles.UseOneInstancedActor` |
| C | `ApplyFleeFrom`의 `StartleStyleValue = PickReactionStyle(...)`를 고정값으로 | `Aquarium.Fish.StartleStyleVaries` |
| D | `ApplyFleeFrom`에서 `if (bIsPlayerFish \|\| bPlayerControlled)` 분기를 지운다(= 내 물고기도 도망간다) | `Aquarium.Fish.OwnFishKeepsControl` |

변이 D가 이 태스크의 정확성 요구를 지키는 시험이다. 전부 되돌린다.

- [ ] **Step 8: 눈으로 본다**

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
"$UE/Engine/Binaries/Mac/UnrealEditor" "$PWD/unreal/Aquarium/Aquarium.uproject" ReefM1 -game -windowed -ResX=1280 -ResY=720 -ForceRes -AquariumAutoNickname="니모" 2>&1 | tail -3
```
확인할 것: (a) 물고기를 찍으면 「꺅」 + 기포 줄기 + 확실히 빠른 도망, (b) **연달아 다섯 번 찍어도 전부 반응한다**, (c) 기포가 **화면 위로 나가며** 사라진다(중간에 툭 없어지지 않는다), (d) **내 물고기를 찍으면 제자리에서 재롱** + 「뽀글」이고 **방향키가 한 순간도 안 먹히는 구간이 없다**, (e) 매번 조금씩 다르다.

- [ ] **Step 9: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add unreal/Aquarium/Source/Aquarium unreal/Aquarium/Scripts/build_fx_m7.py unreal/Aquarium/Content/Fx && git commit -q -F - << 'EOF'
feat(m7): 장면 2 — 클릭이 "비켰다"에서 "놀랐다"로

Niagara 대신 인스턴스 메시 하나 + 해석적 반투명 머티리얼로 기포를 만든다
(M4b가 검증한 경로). 기포는 액터가 아니라 서브시스템이 가진 POD 배열이라
액터 틱이 0개이고, 사라지는 조건은 화면 위를 넘는 것 하나뿐이다.

내 물고기를 클릭해도 도망가지 않는다. 같은 조건의 두 마리 중 한쪽만 클릭해
궤적이 한 치도 달라지지 않음을 단언하고, 분기를 지우는 변이로 빨간불을
확인했다 — 이건 연출이 아니라 정확성 요구다.

Automation 68 → 72. 레벨은 바뀌지 않았다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 11: 규칙 계층 — 먹이 (`Food.h`), 규칙 124 → 134

**Files:** Create `rules/include/aquarium/Food.h`, Create `tests/test_food.cpp`, Modify `CMakeLists.txt`

시나리오 장면 3. 세 가지가 사양의 핵심이다.

**조건 2가 생각보다 중요한 요구사항이다** — "**화면 먼 쪽에 뿌려도 물고기들이 거기까지 와야 한다.** 내 물고기 근처에서만 반응하면 고리가 끊긴다." 그래서 **먹이는 깊이로 거르지 않는다.** 보이즈는 깊이로 거른다(`depthRadius = 120`), 화면에서 겹쳐 보이는 3 m 떨어진 물고기가 같이 헤엄치면 다른 각도에서 우스워지기 때문이다. 먹이는 반대다 — **카메라가 고정이고 아이의 머릿속 모형은 2D**라, 여러 평면의 물고기가 같은 화면 지점으로 모이는 것이 정확히 아이가 기대하는 그림이고 그것이 "떼가 몰려왔다"로 보인다. **이 비대칭은 실수가 아니라 결정이다.**

**자원 개념은 전부 금지다**(게이지·쿨다운·개수 제한·"먹이가 떨어졌습니다"). 그래서 이 헤더에는 **개수 상한도, 재사용 대기 시간도, 남은 양도 없다.** 아이는 화면이 하얘질 때까지 뿌릴 수 있어야 한다.

**규칙 순서**: 먹이는 "어디로 가고 싶은가"를 **대체**하므로 입력/Wander와 같은 층이고 **도망보다 앞**이다. 놀란 물고기가 먹이에 끌려 되돌아오면 아이가 클릭의 결과를 읽지 못한다(M5가 무리를 도망 뒤로 보낸 것과 같은 이유). **경계는 여전히 마지막이다.**

- [ ] **Step 1 (RED): `tests/test_food.cpp`**

```cpp
#include <catch2/catch_test_macros.hpp>
#include <catch2/catch_approx.hpp>

#include <vector>

#include "aquarium/Food.h"

using namespace aquarium;
using Catch::Approx;

namespace {
const FoodParams kF{};
FoodPellet At(float x, float y, float depth = 400.f) {
    FoodPellet p; p.position = {x, y}; p.depth = depth; return p;
}
}

TEST_CASE("a fish steers toward the nearest pellet") {
    std::vector<FoodPellet> food{At(200.f, 0.f), At(-50.f, 0.f)};
    const FoodSeek s = SeekNearestFood({0.f, 0.f}, 400.f, food.data(), food.size(), kF);
    REQUIRE(s.targetIndex == 1);              // 가까운 쪽
    REQUIRE(s.steer.x < 0.f);                 // 그쪽으로 향한다
    REQUIRE(s.steer.Length() == Approx(1.f)); // 단위 벡터: 속도를 바꾸지 않는다
}

TEST_CASE("no food means no steer, and the caller can tell") {
    const FoodSeek s = SeekNearestFood({0.f, 0.f}, 400.f, nullptr, 0, kF);
    REQUIRE(s.targetIndex == -1);
    REQUIRE(s.steer.Length() == Approx(0.f));
}

TEST_CASE("food attracts across the whole screen (scenario scene 3, condition 2)") {
    // 화면 먼 쪽에 뿌려도 온다. 유영 영역 반너비(300cm)의 네 배 거리에서도 끌린다.
    std::vector<FoodPellet> food{At(1200.f, 600.f)};
    const FoodSeek s = SeekNearestFood({-1200.f, -600.f}, 400.f, food.data(), food.size(), kF);
    REQUIRE(s.targetIndex == 0);
    REQUIRE(s.steer.x > 0.f);
    REQUIRE(s.steer.y > 0.f);
}

TEST_CASE("food is NOT depth-gated, unlike boids (deliberate asymmetry)") {
    // 보이즈는 깊이로 거른다(같이 헤엄치면 다른 각도에서 우스워진다). 먹이는
    // 거르지 않는다 -- 카메라가 고정이고 아이의 모형은 2D라, 여러 평면의
    // 물고기가 같은 화면 지점으로 모이는 것이 바로 "떼가 몰려왔다"이다.
    std::vector<FoodPellet> food{At(100.f, 0.f, /*depth*/ 700.f)};
    const FoodSeek s = SeekNearestFood({0.f, 0.f}, /*depth*/ 330.f, food.data(), food.size(), kF);
    REQUIRE(s.targetIndex == 0);
}

TEST_CASE("eaten pellets are ignored") {
    std::vector<FoodPellet> food{At(10.f, 0.f), At(300.f, 0.f)};
    food[0].eaten = true;
    const FoodSeek s = SeekNearestFood({0.f, 0.f}, 400.f, food.data(), food.size(), kF);
    REQUIRE(s.targetIndex == 1);
}

TEST_CASE("a pellet sinks") {
    FoodPellet p = At(0.f, 100.f);
    StepPellet(p, 1.f, kF);
    REQUIRE(p.position.y < 100.f);
    REQUIRE(p.position.y == Approx(100.f - kF.sinkSpeed));
}

TEST_CASE("a fish eats the pellet it reaches, and only one per call") {
    std::vector<FoodPellet> food{At(0.f, 0.f), At(1.f, 1.f), At(500.f, 0.f)};
    const int eaten = EatAt({0.f, 0.f}, food.data(), food.size(), kF);
    REQUIRE(eaten == 0);
    REQUIRE(food[0].eaten);
    REQUIRE_FALSE(food[1].eaten);     // 한 번에 한 알만 사라진다: 「뽁 뽁 뽁」이 되려면
    REQUIRE_FALSE(food[2].eaten);
}

TEST_CASE("a fish far from every pellet eats nothing") {
    std::vector<FoodPellet> food{At(500.f, 500.f)};
    REQUIRE(EatAt({0.f, 0.f}, food.data(), food.size(), kF) == -1);
    REQUIRE_FALSE(food[0].eaten);
}

TEST_CASE("a scatter spreads pellets around the click, deterministically") {
    // 아이가 찍은 자리에서 후두둑 쏟아진다. 한 점에 겹쳐 쌓이면 알갱이 하나로 보인다.
    Vec2 first{0.f, 0.f};
    float maxR = 0.f;
    int distinct = 0;
    Vec2 prev{9999.f, 9999.f};
    for (int i = 0; i < 12; ++i) {
        const Vec2 o = ScatterOffset(1234u, i, kF);
        if (i == 0) first = o;
        const float r = o.Length();
        maxR = r > maxR ? r : maxR;
        if (!(o.x == prev.x && o.y == prev.y)) ++distinct;
        prev = o;
    }
    REQUIRE(distinct >= 11);
    REQUIRE(maxR <= kF.scatterSpread + 1e-3f);
    REQUIRE(ScatterOffset(1234u, 0, kF).x == Approx(first.x));   // 결정적
}

TEST_CASE("there is no limit anywhere in this header (scenario prohibition)") {
    // 자원 개념(게이지/쿨다운/개수 제한)이 들어오면 아래 루프가 어딘가에서
    // 막히거나 값이 달라진다. 1만 알을 뿌려도 전부 유효한 목표로 남는다.
    std::vector<FoodPellet> food;
    food.reserve(10000);
    for (int i = 0; i < 10000; ++i) food.push_back(At(static_cast<float>(i % 400), 0.f));
    const FoodSeek s = SeekNearestFood({0.f, 0.f}, 400.f, food.data(), food.size(), kF);
    REQUIRE(s.targetIndex >= 0);
    int live = 0;
    for (const FoodPellet& p : food) { if (!p.eaten) ++live; }
    REQUIRE(live == 10000);
}
```

`CMakeLists.txt`에 `tests/test_food.cpp`를 더한다.

```bash
cd /Users/hans/dev/aquarium && touch tests/test_food.cpp && cmake -S . -B build && cmake --build build -j 2>&1 | tail -4
```
기대: `'aquarium/Food.h' file not found`. RED다.

- [ ] **Step 2 (GREEN): `rules/include/aquarium/Food.h`**

```cpp
#pragma once
#include <cstddef>
#include <cstdint>

#include "aquarium/Reaction.h"   // ReactionHash, PlayerReactionSin
#include "aquarium/Vec2.h"

namespace aquarium {

// 먹이 한 알. 액터가 아니다 -- 엔진은 이것의 배열을 들고 인스턴스 메시 하나로
// 그린다. 알갱이 수천 개가 떠 있어도 액터 틱은 0개다.
struct FoodPellet {
    Vec2 position;        // 공유 유영 프레임(x = 월드 Y, y = 월드 Z)
    float depth = 0.f;    // 월드 X. 클릭 광선이 만든 평면
    bool eaten = false;
};

struct FoodParams {
    // **깊이 게이트가 없다.** 보이즈와의 이 비대칭은 결정이다(위 주석 참고).
    float eatRadius = 16.f;       // cm. 이 안에 들어오면 톡 사라진다
    float sinkSpeed = 8.f;        // cm/s. 천천히 가라앉아 아이가 따라갈 수 있다
    float scatterSpread = 46.f;   // cm. 클릭 지점 주위로 흩뿌리는 반경
    int scatterCount = 14;        // 한 번 클릭에 쏟아지는 알갱이 수
    // **상한도, 쿨다운도, 남은 양도 없다.** 시나리오가 금지한 항목이고, 여기에
    // 필드를 하나 더하는 순간 그 금지가 깨진다.
};

struct FoodSeek {
    Vec2 steer;             // 단위 벡터, 또는 목표가 없으면 0
    int targetIndex = -1;   // -1이면 끌리는 먹이가 없다
};

// 가장 가까운 (안 먹힌) 알갱이를 향하는 단위 방향. 화면 전체가 사정거리다 --
// 시나리오 장면 3 조건 2("화면 먼 쪽에 뿌려도 물고기들이 거기까지 와야 한다").
// 비용 주석: 보이즈와 같은 종류의 전수 스캔이다. 36마리 x 알갱이 수이고,
// 알갱이가 수백 개가 되면 재면서 판단한다(예측이 아니라 측정으로 -- M4b 전례).
inline FoodSeek SeekNearestFood(Vec2 pos, float /*depth*/, const FoodPellet* food,
                                std::size_t count, const FoodParams& /*p*/) {
    FoodSeek r;
    if (food == nullptr || count == 0) return r;
    float bestSq = 0.f;
    for (std::size_t i = 0; i < count; ++i) {
        if (food[i].eaten) continue;
        const Vec2 off = food[i].position - pos;
        const float d2 = off.x * off.x + off.y * off.y;
        if (r.targetIndex < 0 || d2 < bestSq) { bestSq = d2; r.targetIndex = static_cast<int>(i); }
    }
    if (r.targetIndex < 0) return r;
    r.steer = (food[static_cast<std::size_t>(r.targetIndex)].position - pos).Normalized();
    // 정확히 알갱이 위에 있으면 방향이 0이 된다. 그 경우 조향하지 않는다 --
    // 어차피 다음 호출에서 EatAt이 먹는다.
    return r;
}

// 알갱이는 천천히 가라앉는다. 수명이 없다 -- 사라지는 유일한 길은 먹히는 것이다.
inline void StepPellet(FoodPellet& p, float dt, const FoodParams& params) {
    if (dt <= 0.f || p.eaten) return;
    p.position.y -= params.sinkSpeed * dt;
}

// 이 물고기가 닿은 알갱이 **한 알**을 먹는다. 한 번에 한 알인 이유는 소리다 --
// 「뽁 뽁 뽁」이 되려면 한 알씩 사라져야 한다. 반환값은 먹은 인덱스 또는 -1.
inline int EatAt(Vec2 fishPos, FoodPellet* food, std::size_t count, const FoodParams& p) {
    if (food == nullptr || count == 0) return -1;
    const float r2 = p.eatRadius * p.eatRadius;
    for (std::size_t i = 0; i < count; ++i) {
        if (food[i].eaten) continue;
        const Vec2 off = food[i].position - fishPos;
        if (off.x * off.x + off.y * off.y <= r2) { food[i].eaten = true; return static_cast<int>(i); }
    }
    return -1;
}

// 클릭 지점 주위로 흩뿌리는 오프셋. 결정적이고 반경 안에 갇혀 있다.
inline Vec2 ScatterOffset(std::uint32_t seed, int index, const FoodParams& p) {
    const std::uint32_t h = ReactionHash(seed * 2246822519u + static_cast<std::uint32_t>(index) * 374761393u);
    const float a = static_cast<float>(h >> 8) / 16777216.f * 6.2831853f;
    const float u = static_cast<float>(ReactionHash(h) >> 8) / 16777216.f;
    // sqrt(u)를 쓰면 원판 안에 고르게 퍼진다. 쓰지 않으면 가운데로 뭉친다.
    float radius = u;
    if (radius > 0.f) {
        float g = radius;
        g = 0.5f * (g + radius / g);
        g = 0.5f * (g + radius / g);
        g = 0.5f * (g + radius / g);
        radius = g;
    }
    radius *= p.scatterSpread;
    // 코사인은 사인을 4분의 1주기 밀어서 얻는다(근사 함수를 하나만 둔다).
    return {radius * PlayerReactionSin(a + 1.5707963f), radius * PlayerReactionSin(a)};
}

} // namespace aquarium
```

```bash
cd /Users/hans/dev/aquarium && touch tests/test_food.cpp rules/include/aquarium/Food.h && cmake --build build -j 2>&1 | tail -3 && ctest --test-dir build 2>&1 | tail -3
```
기대: `100% tests passed out of 134`.

- [ ] **Step 3: 변이 확인**

| 변이 | 바꿀 곳 | 빨간불 |
|---|---|---|
| A | `SeekNearestFood`에 깊이 게이트를 넣는다: `if (fabs(food[i].depth - depth) > 120.f) continue;` | `food is NOT depth-gated, unlike boids` |
| B | `SeekNearestFood`에 사정거리 제한을 넣는다: `if (d2 > 300.f*300.f) continue;` | `food attracts across the whole screen` |
| C | `EatAt`에서 `return`을 지워 닿은 알갱이를 전부 먹게 한다 | `a fish eats the pellet it reaches, and only one per call` |
| D | `FoodParams`에 `int maxPellets = 200;`을 넣고 `SeekNearestFood`에서 `count`를 그걸로 자른다 | `there is no limit anywhere in this header` |

변이 B와 D가 시나리오가 못 박은 두 가지(조건 2, 자원 금지)를 지키는 시험이다. 전부 되돌린다.

- [ ] **Step 4: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add rules tests CMakeLists.txt && git commit -q -F - << 'EOF'
feat(m7): 먹이의 규칙 — 화면 전체가 사정거리, 제한은 하나도 없다

보이즈는 깊이로 거르고 먹이는 거르지 않는다. 이 비대칭은 결정이다 — 카메라가
고정이라 아이의 모형은 2D이고, 여러 평면의 물고기가 같은 화면 지점으로 모이는
것이 바로 "떼가 몰려왔다"이다. 깊이 게이트를 넣는 변이와 사정거리를 좁히는
변이로 각각 빨간불을 확인했다.

FoodParams에는 상한도 쿨다운도 남은 양도 없다. 1만 알을 뿌려도 전부 유효한
목표로 남는지 단언하고, 상한을 넣는 변이로 빨간불을 확인했다.

규칙 124 → 134.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 12: 장면 3 — 빈 물을 찍으면 먹이가 쏟아진다, Automation 72 → 78

**Files:** Create `.../FoodSubsystem.h/.cpp`, Modify `.../FishActor.h/.cpp`, Modify `.../DiverPlayerController.h/.cpp`, Create `.../Tests/FoodTests.cpp`

- [ ] **Step 1: 먹이 서브시스템**

`unreal/Aquarium/Source/Aquarium/FoodSubsystem.h`:

```cpp
#pragma once

#include "CoreMinimal.h"
#include "Subsystems/WorldSubsystem.h"

#include <vector>

#include "aquarium/Food.h"

#include "FoodSubsystem.generated.h"

class AInstancedFieldActor;

// 화면에 떠 있는 모든 먹이. 알갱이는 액터가 아니라 POD이고, 그리기는 인스턴스
// 메시 하나가 한다. **개수 상한이 없다** -- 아이가 화면이 하얘질 때까지 뿌릴 수
// 있어야 한다는 것이 이 기능의 절정이라고 시나리오가 못 박았다.
UCLASS()
class AQUARIUM_API UFoodSubsystem : public UTickableWorldSubsystem
{
	GENERATED_BODY()

public:
	virtual void Initialize(FSubsystemCollectionBase& Collection) override;
	virtual void Tick(float DeltaTime) override;
	virtual TStatId GetStatId() const override { RETURN_QUICK_DECLARE_CYCLE_STAT(UFoodSubsystem, STATGROUP_Tickables); }

	// 빈 물 클릭 한 번. 클릭 지점 주위로 알갱이가 후두둑 쏟아진다.
	void Scatter(const FVector& WorldPoint, uint32 Seed);
	// 물고기가 읽는 스냅샷. 보이즈의 Neighbors()와 같은 모양이다.
	const std::vector<aquarium::FoodPellet>& Pellets() const { return Food; }
	// 이 물고기가 닿은 알갱이 한 알을 먹는다. 먹었으면 true(호출자가 「뽁」을 낸다).
	bool EatAt(const FVector& WorldPos);

	int32 LiveCount() const;
	int32 ScatterCount() const { return ScatterCalls; }
	int32 EatenTotal() const { return EatenTotalValue; }
	AInstancedFieldActor* Field() const { return FieldActor; }

	// 개발 전용 불리언(성능 귀속). 값을 받지 않으므로 조용히 무시될 토큰이 없다.
	bool bFoodEnabled = true;

	const aquarium::FoodParams& Params() const { return FoodParamsValue; }

private:
	std::vector<aquarium::FoodPellet> Food;
	aquarium::FoodParams FoodParamsValue;
	UPROPERTY() TObjectPtr<AInstancedFieldActor> FieldActor = nullptr;
	int32 ScatterCalls = 0;
	int32 EatenTotalValue = 0;
	void EnsureField();
};
```

`FoodSubsystem.cpp`:

```cpp
#include "FoodSubsystem.h"

#include "Engine/StaticMesh.h"
#include "Engine/World.h"
#include "Materials/MaterialInterface.h"
#include "Misc/CommandLine.h"
#include "Misc/Parse.h"
#include "InstancedFieldActor.h"

void UFoodSubsystem::Initialize(FSubsystemCollectionBase& Collection)
{
	Super::Initialize(Collection);
#if !UE_BUILD_SHIPPING
	bFoodEnabled = !FParse::Param(FCommandLine::Get(), TEXT("AquariumNoFood"));
#endif
	Food.clear();
}

void UFoodSubsystem::EnsureField()
{
	if (FieldActor != nullptr) { return; }
	UWorld* W = GetWorld();
	if (W == nullptr) { return; }
	FActorSpawnParameters P;
	P.SpawnCollisionHandlingOverride = ESpawnActorCollisionHandlingMethod::AlwaysSpawn;
	FieldActor = W->SpawnActor<AInstancedFieldActor>(AInstancedFieldActor::StaticClass(),
		FVector::ZeroVector, FRotator::ZeroRotator, P);
	if (FieldActor)
	{
		UStaticMesh* Sphere = LoadObject<UStaticMesh>(nullptr, TEXT("/Engine/BasicShapes/Sphere.Sphere"));
		UMaterialInterface* Mat = LoadObject<UMaterialInterface>(nullptr, TEXT("/Game/Fx/M_FoodPellet.M_FoodPellet"));
		FieldActor->Configure(Sphere, Mat);
	}
}

void UFoodSubsystem::Scatter(const FVector& WorldPoint, uint32 Seed)
{
	if (!bFoodEnabled) { return; }
	EnsureField();
	++ScatterCalls;
	// 상한을 두지 않는다. 시나리오가 금지한 자리이고, 알갱이는 먹히거나 남거나
	// 둘 중 하나일 뿐 프레임 비용은 Task 14에서 실제로 잰다.
	for (int32 i = 0; i < FoodParamsValue.scatterCount; ++i)
	{
		const aquarium::Vec2 Off = aquarium::ScatterOffset(Seed, i, FoodParamsValue);
		aquarium::FoodPellet P;
		P.position = {static_cast<float>(WorldPoint.Y) + Off.x, static_cast<float>(WorldPoint.Z) + Off.y};
		P.depth = static_cast<float>(WorldPoint.X);
		Food.push_back(P);
	}
}

bool UFoodSubsystem::EatAt(const FVector& WorldPos)
{
	if (Food.empty()) { return false; }
	const int Index = aquarium::EatAt(
		{static_cast<float>(WorldPos.Y), static_cast<float>(WorldPos.Z)},
		Food.data(), Food.size(), FoodParamsValue);
	if (Index < 0) { return false; }
	++EatenTotalValue;
	return true;
}

int32 UFoodSubsystem::LiveCount() const
{
	int32 N = 0;
	for (const aquarium::FoodPellet& P : Food) { if (!P.eaten) ++N; }
	return N;
}

void UFoodSubsystem::Tick(float DeltaTime)
{
	Super::Tick(DeltaTime);
	if (Food.empty()) { return; }
	TArray<FTransform> Transforms;
	Transforms.Reserve(static_cast<int32>(Food.size()));
	size_t write = 0;
	for (size_t i = 0; i < Food.size(); ++i)
	{
		aquarium::FoodPellet& P = Food[i];
		if (P.eaten) { continue; }        // 먹힌 알갱이는 톡 사라진다
		aquarium::StepPellet(P, DeltaTime, FoodParamsValue);
		Food[write++] = P;
		Transforms.Add(FTransform(FQuat::Identity,
			FVector(P.depth, P.position.x, P.position.y),
			FVector(2.6f / 50.f)));       // 알갱이 반지름 2.6 cm / 엔진 구 50 cm
	}
	Food.resize(write);
	EnsureField();
	if (FieldActor) { FieldActor->UpdateInstances(Transforms); }
}
```

- [ ] **Step 2: 규칙 순서에 먹이 한 칸을 끼운다**

`FishActor.cpp`의 `StepSwim`에서, `Desired`를 정한 **직후이자 `Flee.Step` 앞**에 넣는다:

```cpp
	// 먹이 층. 입력/Wander와 **같은 층**이다 -- "어디로 가고 싶은가"의 답을
	// 대체하는 것이지 그 답에 가하는 보정이 아니다. 도망보다 앞에 있으므로
	// 놀란 물고기는 먹이에 끌려 되돌아오지 않는다(M5가 무리를 도망 뒤로 보낸
	// 것과 같은 이유: 아이가 클릭의 결과를 읽을 수 있어야 한다).
	// 경계는 이 뒤로도 그대로 마지막이다 -- M3의 "영역을 벗어나지 않는다" 보장.
	bool bSeekingFood = false;
	if (!bPlayerControlled)   // 내 물고기는 먹이에 끌려가지 않는다: 조종이 전부다
	{
		UWorld* WFood = GetWorld();
		UFoodSubsystem* FoodSys = WFood ? WFood->GetSubsystem<UFoodSubsystem>() : nullptr;
		if (FoodSys && FoodSys->bFoodEnabled && !FoodSys->Pellets().empty())
		{
			const aquarium::Vec2 Shared{Plane.origin.y + Motion.position.x, Plane.origin.z + Motion.position.y};
			const aquarium::FoodSeek Seek = aquarium::SeekNearestFood(
				Shared, Plane.origin.x, FoodSys->Pellets().data(), FoodSys->Pellets().size(),
				FoodSys->Params());
			if (Seek.targetIndex >= 0 && Seek.steer.Length() > 1e-6f)
			{
				Desired = Seek.steer;
				bSeekingFood = true;
			}
			// 닿았으면 한 알 먹고 「뽁」. 아이가 소리로 결과를 읽는다.
			if (FoodSys->EatAt(FVector(Plane.origin.x, Shared.x, Shared.y)))
			{
				if (UAquariumAudioSubsystem* Audio = WFood->GetSubsystem<UAquariumAudioSubsystem>())
				{
					Audio->PlayCue(EAquariumCue::Nibble, static_cast<uint32>(GetUniqueID()) + static_cast<uint32>(SwimPhase * 1000.f));
				}
			}
		}
	}
```

그리고 무리 조건에 `&& !bSeekingFood`를 더한다:

```cpp
	if (!bPlayerControlled && !bIsPlayerFish && !bFleeing && !bSeekingFood)
```

**왜 먹이 중에는 무리를 건너뛰는가**: 응집이 같은 먹이를 향하는 물고기들을 서로 끌어당기면 조향이 서로를 상쇄해 떼가 먹이 앞에서 맴돈다. 게다가 **같은 점을 향하는 것만으로 이미 모이므로** 응집을 더하는 것은 같은 일을 두 번 하는 것이다. 도망이 무리를 건너뛰는 것과 같은 판단이다.

`FishActor.cpp` 상단에 `#include "FoodSubsystem.h"`와 `#include "AquariumAudioSubsystem.h"`를 더한다.

- [ ] **Step 3: 빈 물 클릭 → 먹이**

`DiverPlayerController.cpp`의 `HandleClickRay`에서 `if (Fish == nullptr)` 분기를 아래로 바꾼다:

```cpp
	if (Fish == nullptr)
	{
		// 시나리오 장면 3: 빈 물은 이제 무반응이 아니다. 먹이가 쏟아진다.
		// 아이는 마구 찍다가 **우연히 발견한다** -- 설명이 필요 없는 것이 요점이다.
		const FVector Drop = EmptyWaterPoint(RayOrigin, RayDir);
		if (UWorld* WW = GetWorld())
		{
			if (UFoodSubsystem* FoodSys = WW->GetSubsystem<UFoodSubsystem>())
			{
				FoodSys->Scatter(Drop, static_cast<uint32>(FMath::Rand()));
			}
			if (UAquariumAudioSubsystem* Audio = WW->GetSubsystem<UAquariumAudioSubsystem>())
			{
				// 「뽁」은 먹을 때 난다. 뿌릴 때는 기포만 -- 소리가 먼저 나면
				// 아이가 "맞혔다"로 착각한다(장면 3 조건 1의 구분이 흐려진다).
				(void)Audio;
			}
			if (UBubbleSubsystem* Bubbles = WW->GetSubsystem<UBubbleSubsystem>())
			{
				Bubbles->SetTopZ(BubbleTopZ());
				Bubbles->Spawn(Drop, 4, static_cast<uint32>(FMath::Rand()));
			}
		}
		LastClickState = TEXT("Food");
		return false;   // 클릭 로그의 hit 열은 여전히 "물고기를 맞혔는가"를 뜻한다
	}
```

그리고 헬퍼(헤더에 `FVector EmptyWaterPoint(const FVector& RayOrigin, const FVector& RayDir) const;`):

```cpp
FVector ADiverPlayerController::EmptyWaterPoint(const FVector& RayOrigin, const FVector& RayDir) const
{
	// 빈 물 클릭은 어느 평면에도 맞지 않았으므로 깊이를 정해야 한다. 플레이어
	// 물고기의 평면에 떨어뜨린다 -- 아이가 자기 물고기와 같은 층에서 먹이를 보고,
	// 배경 물고기들은 어차피 깊이와 무관하게 몰려온다(먹이는 깊이 게이트가 없다).
	const AAquariumGameMode* GM = GameMode();
	const AFishActor* Fish = GM ? GM->PlayerFish() : nullptr;
	const float Depth = Fish ? static_cast<float>(Fish->PlaneOrigin.X) : 300.f;
	const FVector Dir = RayDir.GetSafeNormal();
	if (FMath::Abs(Dir.X) < 1e-4f)
	{
		return RayOrigin + Dir * 300.f;
	}
	const float S = (Depth - static_cast<float>(RayOrigin.X)) / static_cast<float>(Dir.X);
	return RayOrigin + Dir * FMath::Max(S, 1.f);
}
```

include에 `#include "FoodSubsystem.h"`를 더한다.

- [ ] **Step 4 (RED 먼저): `Tests/FoodTests.cpp`**

```cpp
#include "Misc/AutomationTest.h"

#if WITH_DEV_AUTOMATION_TESTS && WITH_EDITOR

#include "Tests/AutomationEditorCommon.h"
#include "EngineUtils.h"
#include "Engine/World.h"
#include "FishActor.h"
#include "FoodSubsystem.h"
#include "InstancedFieldActor.h"

namespace
{
AFishActor* SpawnBackgroundFish(UWorld* World, const FVector& PlaneOrigin, uint32 Seed)
{
	FActorSpawnParameters SP;
	SP.SpawnCollisionHandlingOverride = ESpawnActorCollisionHandlingMethod::AlwaysSpawn;
	AFishActor* F = World->SpawnActor<AFishActor>(AFishActor::StaticClass(), FVector::ZeroVector, FRotator::ZeroRotator, SP);
	F->Seed = Seed;
	F->PlaneOrigin = PlaneOrigin;
	F->PlaneHalfWidth = 300.f;
	F->PlaneHalfHeight = 150.f;
	F->InitializeSwim();
	return F;
}
}

IMPLEMENT_SIMPLE_AUTOMATION_TEST(FFoodScatterMakesPellets, "Aquarium.Food.ScatterMakesPellets",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FFoodScatterMakesPellets::RunTest(const FString&)
{
	UWorld* World = FAutomationEditorCommonUtils::CreateNewMap();
	UFoodSubsystem* Food = World->GetSubsystem<UFoodSubsystem>();
	if (!TestNotNull(TEXT("food subsystem"), Food)) return false;
	Food->Scatter(FVector(300.f, 50.f, 100.f), 7u);
	// 개수는 규칙 계층 파라미터에서 파생한다(리터럴을 베끼지 않는다).
	TestEqual(TEXT("one scatter drops scatterCount pellets"), Food->LiveCount(), Food->Params().scatterCount);
	// 알갱이들이 한 점에 겹쳐 있지 않아야 한다.
	const std::vector<aquarium::FoodPellet>& P = Food->Pellets();
	bool bSpread = false;
	for (size_t i = 1; i < P.size(); ++i)
	{
		if (FMath::Abs(P[i].position.x - P[0].position.x) > 1.f) { bSpread = true; break; }
	}
	TestTrue(TEXT("pellets are spread, not stacked"), bSpread);
	return true;
}

// 시나리오가 금지한 자원 개념이 들어오지 않았는지 엔진 쪽에서도 본다.
IMPLEMENT_SIMPLE_AUTOMATION_TEST(FFoodHasNoLimit, "Aquarium.Food.HasNoLimit",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FFoodHasNoLimit::RunTest(const FString&)
{
	UWorld* World = FAutomationEditorCommonUtils::CreateNewMap();
	UFoodSubsystem* Food = World->GetSubsystem<UFoodSubsystem>();
	if (!TestNotNull(TEXT("food subsystem"), Food)) return false;
	const int32 Scatters = 200;   // 아이가 화면을 하얗게 덮는다
	for (int32 i = 0; i < Scatters; ++i) { Food->Scatter(FVector(300.f, i % 100, 50.f), static_cast<uint32>(i)); }
	TestEqual(TEXT("every scatter was honoured"), Food->ScatterCount(), Scatters);
	TestEqual(TEXT("no cap on pellets"), Food->LiveCount(), Scatters * Food->Params().scatterCount);
	// 그래도 액터는 하나다.
	int32 Fields = 0;
	for (TActorIterator<AInstancedFieldActor> It(World); It; ++It) { ++Fields; }
	TestEqual(TEXT("still one field actor"), Fields, 1);
	return true;
}

// **장면 3 조건 2**: 화면 먼 쪽에 뿌려도 온다.
IMPLEMENT_SIMPLE_AUTOMATION_TEST(FFishComeFromAcrossTheScreen, "Aquarium.Food.FishComeFromAcrossTheScreen",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FFishComeFromAcrossTheScreen::RunTest(const FString&)
{
	UWorld* World = FAutomationEditorCommonUtils::CreateNewMap();
	UFoodSubsystem* Food = World->GetSubsystem<UFoodSubsystem>();
	if (!TestNotNull(TEXT("food subsystem"), Food)) return false;
	// 물고기는 왼쪽 아래 평면, 먹이는 오른쪽 위 **다른 평면**에 뿌린다.
	AFishActor* Fish = SpawnBackgroundFish(World, FVector(330.f, -250.f, 60.f), 3u);
	const FVector Before = Fish->GetActorLocation();
	Food->Scatter(FVector(690.f, 250.f, 220.f), 5u);
	for (int32 i = 0; i < 60 * 4; ++i) { Fish->StepSwim(1.f / 60.f); }
	const FVector After = Fish->GetActorLocation();
	// 먹이 쪽(월드 +Y, +Z)으로 실제로 움직였다.
	TestTrue(TEXT("moved toward the food in Y"), After.Y > Before.Y + 10.f);
	TestTrue(TEXT("moved toward the food in Z"), After.Z > Before.Z + 5.f);
	return true;
}

IMPLEMENT_SIMPLE_AUTOMATION_TEST(FFoodIsEatenOneAtATime, "Aquarium.Food.EatenOneAtATime",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FFoodIsEatenOneAtATime::RunTest(const FString&)
{
	UWorld* World = FAutomationEditorCommonUtils::CreateNewMap();
	UFoodSubsystem* Food = World->GetSubsystem<UFoodSubsystem>();
	if (!TestNotNull(TEXT("food subsystem"), Food)) return false;
	Food->Scatter(FVector(330.f, 0.f, 0.f), 1u);
	const int32 Start = Food->LiveCount();
	// 먹이 한가운데 서 있는 물고기 한 마리
	TestTrue(TEXT("ate one"), Food->EatAt(FVector(330.f, 0.f, 0.f)));
	Food->Tick(1.f / 60.f);
	TestEqual(TEXT("exactly one gone"), Food->LiveCount(), Start - 1);
	TestEqual(TEXT("counted"), Food->EatenTotal(), 1);
	return true;
}

IMPLEMENT_SIMPLE_AUTOMATION_TEST(FPlayerFishIsNotPulledByFood, "Aquarium.Food.PlayerFishNotPulled",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FPlayerFishIsNotPulledByFood::RunTest(const FString&)
{
	UWorld* World = FAutomationEditorCommonUtils::CreateNewMap();
	UFoodSubsystem* Food = World->GetSubsystem<UFoodSubsystem>();
	if (!TestNotNull(TEXT("food subsystem"), Food)) return false;
	AFishActor* Mine = SpawnBackgroundFish(World, FVector(220.f, 0.f, 100.f), 9u);
	Mine->bPlayerControlled = true;
	Mine->SetInputDirection(FVector2D(0.f, 0.f));   // 아이가 아무 키도 안 누른다
	const FVector Before = Mine->GetActorLocation();
	Food->Scatter(FVector(220.f, 280.f, 140.f), 2u);
	for (int32 i = 0; i < 120; ++i) { Mine->StepSwim(1.f / 60.f); }
	// 키를 안 눌렀으면 제자리다. 먹이가 조종을 대신하면 안 된다.
	TestTrue(TEXT("my fish stays put"), (Mine->GetActorLocation() - Before).Size() < 5.f);
	return true;
}

// 경계는 먹이가 들어온 뒤에도 마지막이다(M3 보장).
IMPLEMENT_SIMPLE_AUTOMATION_TEST(FFoodNeverPushesAFishOutOfTheArea, "Aquarium.Food.NeverLeavesTheArea",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FFoodNeverPushesAFishOutOfTheArea::RunTest(const FString&)
{
	UWorld* World = FAutomationEditorCommonUtils::CreateNewMap();
	UFoodSubsystem* Food = World->GetSubsystem<UFoodSubsystem>();
	if (!TestNotNull(TEXT("food subsystem"), Food)) return false;
	AFishActor* Fish = SpawnBackgroundFish(World, FVector(400.f, 0.f, 100.f), 4u);
	// 영역 바깥 멀리에 먹이를 뿌린다. 물고기는 벽까지만 가야 한다.
	Food->Scatter(FVector(400.f, 5000.f, 5000.f), 6u);
	for (int32 i = 0; i < 60 * 10; ++i)
	{
		Fish->StepSwim(1.f / 60.f);
		const FVector L = Fish->GetActorLocation();
		if (!TestTrue(TEXT("inside the area in Y"), FMath::Abs(L.Y) <= Fish->PlaneHalfWidth + 0.5f)) return false;
		if (!TestTrue(TEXT("inside the area in Z"),
			FMath::Abs(L.Z - Fish->PlaneOrigin.Z) <= Fish->PlaneHalfHeight + 0.5f)) return false;
	}
	return true;
}

#endif
```

- [ ] **Step 5: 빌드 두 번 + Automation**

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
for p in 1 2; do "$UE/Engine/Build/BatchFiles/Mac/Build.sh" AquariumEditor Mac Development -Project="$PWD/unreal/Aquarium/Aquarium.uproject" -WaitMutex 2>&1 | grep -E "^Result:|error:"; done
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" -ExecCmds="Automation RunTests Aquarium; Quit" -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -cE "Test Completed.*Success"
```
기대: `78`.

- [ ] **Step 6: 변이 확인**

| 변이 | 바꿀 곳 | 빨간불 |
|---|---|---|
| A | `StepSwim`의 먹이 블록을 통째로 주석 처리 | `Aquarium.Food.FishComeFromAcrossTheScreen` |
| B | `Scatter` 맨 앞에 `if (Food.size() > 100) return;`(= 개수 제한) | `Aquarium.Food.HasNoLimit` |
| C | 먹이 블록의 `if (!bPlayerControlled)` 조건을 지운다 | `Aquarium.Food.PlayerFishNotPulled` |
| D | `StepSwim`에서 먹이 블록을 **경계 규칙 뒤로** 옮긴다 | `Aquarium.Food.NeverLeavesTheArea` |

**변이 D가 이 마일스톤에서 가장 중요한 변이다** — 규칙 순서가 무너지면 M3의 "영역을 벗어나지 않는다" 보장이 조용히 깨지고, 그 증상은 한참 뒤 캡처에서나 보인다. 빨간불이 안 켜지면 켜지는 배치를 찾거나 후속 항목에 적는다. 전부 되돌린다.

- [ ] **Step 7: 눈으로 본다**

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
"$UE/Engine/Binaries/Mac/UnrealEditor" "$PWD/unreal/Aquarium/Aquarium.uproject" ReefM1 -game -windowed -ResX=1280 -ResY=720 -ForceRes -AquariumAutoNickname="니모" 2>&1 | tail -3
```
확인할 것: (a) 빈 물을 찍으면 하얀 알갱이가 후두둑 쏟아진다, (b) **화면 반대쪽 물고기들이 하나씩 방향을 틀어** 몰려온다, (c) 알갱이가 톡톡 사라지며 「뽁」이 난다, (d) **「꺅」과 「뽁」이 확실히 다르게 들린다**(장면 3 조건 1), (e) 아무 데나 마구 뿌려 화면을 알갱이로 덮어도 제한이 걸리지 않는다, (f) 물고기가 영역 밖으로 나가지 않는다.

- [ ] **Step 8: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add unreal/Aquarium/Source/Aquarium && git commit -q -F - << 'EOF'
feat(m7): 장면 3 — 빈 물을 찍으면 먹이가 쏟아지고 떼가 몰려온다

규칙 순서는 입력/Wander → **먹이** → 도망 → 무리 → 장애물 → 경계 →
StepMotion → Clamp가 됐다. 먹이는 "어디로 가고 싶은가"를 대체하므로 입력과
같은 층이고 도망보다 앞이며, 경계는 여전히 마지막이다. 먹이 블록을 경계 뒤로
옮기는 변이로 M3 보장이 실제로 깨지는 것(=테스트가 무는 것)을 확인했다.

알갱이는 액터가 아니라 POD 배열이고 인스턴스 메시 하나가 그린다. 개수 제한을
넣는 변이로 빨간불을 확인했다 — 시나리오가 가장 강하게 금지한 자리다.

Automation 72 → 78.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 13: 장면 4 — 내가 지나가면 길이 열린다, 규칙 134 → 139, Automation 78 → 82

**Files:** Create `rules/include/aquarium/Split.h`, Create `tests/test_split.cpp`, Modify `CMakeLists.txt`, Modify `.../FishSchoolSubsystem.h/.cpp`, Modify `.../Tests/FishActorTests.cpp`

**새 기능이 아니다.** 지금도 비켜 주기는 한다(`BoidsParams::avoidOnlyRadius = 110`). 문제는 아이가 그걸 느낄 만큼 과하지 않다는 것이다. **있는 것의 볼륨을 3배로 올리는 일**이고, 넷 중 가장 싸며 다른 셋과 독립이다 — 무언가 막히면 순서를 앞으로 당겨도 손해가 없다.

두 가지를 한다.

1. **볼륨**: `avoidOnlyRadius` 110 → **240**, `separationWeight` 1.7 → **2.6**. 숫자는 클립을 보고 조정한다(스틸로는 판단할 수 없다).
2. **「촤악」**: 갈라지는 **순간**을 감지해 소리를 낸다. 감지는 규칙 계층의 순수 함수다.

**재발화 간격에 대하여**: `SplitDetector`에는 쿨다운이 있다. 이것은 시나리오가 금지한 "아이 행동에 거는 제한"이 **아니다** — 아이가 누르는 것이 아니라 **자동으로 일어나는 사건**이고, 쿨다운이 없으면 떼 한가운데를 지나는 1초 동안 「촤악」이 30번 겹쳐 잡음이 된다. 구분 기준은 명확하다: **아이의 입력이 삼켜지면 금지, 세계가 스스로 내는 소리의 간격이면 허용.**

- [ ] **Step 1 (RED): `tests/test_split.cpp`**

```cpp
#include <catch2/catch_test_macros.hpp>

#include "aquarium/Split.h"

using namespace aquarium;

namespace { const SplitParams kS{}; }

TEST_CASE("a split fires when enough fish scatter at once") {
    SplitDetector d;
    REQUIRE_FALSE(d.Observe(0, 100.f, kS));                    // 아무도 없다
    REQUIRE(d.Observe(kS.minFish, 100.f, kS));                 // 떼 한가운데로 들어갔다
}

TEST_CASE("a split does not fire when the player is drifting slowly") {
    // 천천히 지나가는 것은 갈라지는 것이 아니다. 돌진해야 터진다.
    SplitDetector d;
    REQUIRE_FALSE(d.Observe(kS.minFish * 2, kS.minSpeed * 0.5f, kS));
}

TEST_CASE("a split does not fire twice in a row without a gap") {
    SplitDetector d;
    REQUIRE(d.Observe(kS.minFish, 100.f, kS));
    // 같은 프레임 뒤에 바로 또 터지면 「촤악」이 겹쳐 잡음이 된다. 이 간격은
    // 아이의 입력을 삼키는 제한이 아니라 세계가 내는 소리의 간격이다.
    REQUIRE_FALSE(d.Observe(kS.minFish, 100.f, kS));
    d.Step(kS.cooldown * 0.5f);
    REQUIRE_FALSE(d.Observe(kS.minFish, 100.f, kS));
    d.Step(kS.cooldown);
    REQUIRE(d.Observe(kS.minFish, 100.f, kS));
}

TEST_CASE("a split needs a real crowd, not one or two fish") {
    SplitDetector d;
    for (int n = 0; n < kS.minFish; ++n) {
        REQUIRE_FALSE(d.Observe(n, 200.f, kS));
    }
    REQUIRE(d.Observe(kS.minFish, 200.f, kS));
}

TEST_CASE("the avoid radius used for splitting is derived, not a second copy") {
    // 규약 7. 분리 감지의 반경은 SplitParams가 따로 갖는 것이 아니라 보이즈의
    // avoidOnlyRadius를 그대로 쓴다. 엔진이 그 값으로 센 개수를 넘겨줄 뿐이라
    // 이 헤더에는 반경 필드가 아예 없다.
    SplitParams p;
    (void)p;
    // 컴파일 자체가 단언이다: SplitParams에 radius 비슷한 필드가 생기면
    // 아래 주석을 지우고 이 테스트를 고쳐야 하고, 그 순간 중복이 드러난다.
    REQUIRE(sizeof(SplitParams) == sizeof(int) + sizeof(float) + sizeof(float));
}
```

`CMakeLists.txt`에 `tests/test_split.cpp`를 더한다.

- [ ] **Step 2 (GREEN): `rules/include/aquarium/Split.h`**

```cpp
#pragma once

namespace aquarium {

// 무리가 "갈라지는 순간"을 감지한다. 반경 필드가 **없다** -- 엔진이
// BoidsParams::avoidOnlyRadius로 센 이웃 수를 넘겨주기 때문이고, 그래서 반경이
// 두 곳에 존재할 수 없다(규약 7).
struct SplitParams {
    int minFish = 6;        // 이만큼이 한꺼번에 비켜야 '갈라졌다'이다
    float minSpeed = 25.f;  // cm/s. 천천히 지나가는 것은 갈라지는 것이 아니다
    float cooldown = 0.9f;  // s. 소리가 겹쳐 잡음이 되는 것을 막는 간격
};

class SplitDetector {
public:
    void Step(float dt) {
        if (dt <= 0.f) return;
        if (since_ < 1e6f) since_ += dt;
    }

    // 이번 프레임에 「촤악」을 낼지. 참을 돌려준 그 순간이 갈라진 순간이다.
    bool Observe(int nearCount, float playerSpeed, const SplitParams& p) {
        if (nearCount < p.minFish) return false;
        if (playerSpeed < p.minSpeed) return false;
        if (since_ < p.cooldown) return false;
        since_ = 0.f;
        return true;
    }

private:
    // 처음에는 바로 터질 수 있어야 하므로 아주 큰 값에서 시작한다.
    float since_ = 1e6f;
};

} // namespace aquarium
```

```bash
cd /Users/hans/dev/aquarium && touch tests/test_split.cpp rules/include/aquarium/Split.h && cmake -S . -B build && cmake --build build -j 2>&1 | tail -3 && ctest --test-dir build 2>&1 | tail -3
```
기대: `100% tests passed out of 139`. `sizeof` 단언이 패딩 때문에 실패하면 **그 단언만** 실제 값에 맞게 고치고, 왜 고쳤는지 주석에 적는다(중복 없음이라는 의도가 그 주석이다).

- [ ] **Step 3 (GREEN): 볼륨을 올리고 감지를 붙인다**

`FishActor.h`의 보이즈 파라미터 기본값이 있는 곳을 찾아 `avoidOnlyRadius`를 올린다. **`BoidsParams`의 기본값 자체를 바꾸지 말고** 액터에서 덮어쓴다(다른 마일스톤의 테스트 기대값이 기본값에 걸려 있을 수 있다):

```bash
cd /Users/hans/dev/aquarium && grep -rn "avoidOnlyRadius\|separationWeight" rules unreal/Aquarium/Source | grep -v Binaries
```
기대: 정의(`Boids.h`)와 테스트(`test_boids.cpp`)에만 보인다. `FishActor.cpp`의 `InitializeSwim` 안, 보이즈 파라미터를 세팅하는 자리(없으면 `BoidsParamsValue` 초기화 직후)에 아래를 넣는다:

```cpp
	// 장면 4: **있는 것의 볼륨을 3배로.** 조용히 비켜 주면 아이는 모른다.
	// 폭발하듯 갈라져야 안다. 숫자는 스틸이 아니라 클립을 보고 맞춘다.
	BoidsParamsValue.avoidOnlyRadius = 240.f;   // 110 -> 240
	BoidsParamsValue.separationWeight = 2.6f;   // 1.7 -> 2.6
```

`FishSchoolSubsystem.h`에:

```cpp
	// 장면 4: 플레이어 주위에서 한꺼번에 비킨 물고기 수를 세고, 갈라지는 순간을
	// 잡아 「촤악」을 낸다. 반경은 보이즈 파라미터에서 **파생**한다.
	void UpdateSplit(float DeltaSeconds, const FVector& PlayerWorldPos, float PlayerSpeed,
	                 float AvoidRadius);
	int32 SplitFireCount() const { return SplitFires; }
	int32 LastNearCount() const { return LastNear; }
```

private에:

```cpp
	aquarium::SplitDetector Split;
	aquarium::SplitParams SplitParamsValue;
	int32 SplitFires = 0;
	int32 LastNear = 0;
```

include에 `#include "aquarium/Split.h"`.

`FishSchoolSubsystem.cpp`에:

```cpp
void UFishSchoolSubsystem::UpdateSplit(float DeltaSeconds, const FVector& PlayerWorldPos,
                                       float PlayerSpeed, float AvoidRadius)
{
	Split.Step(DeltaSeconds);
	// 반경은 호출자가 BoidsParams::avoidOnlyRadius에서 그대로 넘겨준다.
	const float R2 = AvoidRadius * AvoidRadius;
	const aquarium::Vec2 P{static_cast<float>(PlayerWorldPos.Y), static_cast<float>(PlayerWorldPos.Z)};
	int32 Near = 0;
	for (const aquarium::BoidNeighbor& N : Neighbors())
	{
		if (N.avoidOnly) continue;            // 플레이어 자신은 세지 않는다
		const aquarium::Vec2 D = N.position - P;
		if (D.x * D.x + D.y * D.y <= R2) ++Near;
	}
	LastNear = Near;
	if (Split.Observe(Near, PlayerSpeed, SplitParamsValue))
	{
		++SplitFires;
		if (UWorld* W = GetWorld())
		{
			if (UAquariumAudioSubsystem* Audio = W->GetSubsystem<UAquariumAudioSubsystem>())
			{
				Audio->PlayCue(EAquariumCue::Split, static_cast<uint32>(SplitFires) * 7919u);
			}
		}
	}
}
```

include에 `#include "AquariumAudioSubsystem.h"`와 `#include "Engine/World.h"`.

`DiverPlayerController.cpp`의 `ApplyInputToPlayerFish` 끝(오디오 갱신 옆)에:

```cpp
		if (UFishSchoolSubsystem* School = W->GetSubsystem<UFishSchoolSubsystem>())
		{
			// 반경은 물고기가 실제로 쓰는 값에서 파생한다.
			School->UpdateSplit(DeltaSeconds, Fish->GetActorLocation(), Fish->CurrentSpeed(),
				Fish->AvoidOnlyRadius());
		}
```

`FishActor.h`에 접근자를 더한다:

```cpp
	// 무리가 이 물고기를 피하는 반경. 분리 감지가 같은 값을 쓰게 하려는 것이며,
	// 이 접근자가 있는 한 반경은 이 프로젝트에 한 벌만 존재한다.
	float AvoidOnlyRadius() const { return BoidsParamsValue.avoidOnlyRadius; }
```

- [ ] **Step 4 (RED 먼저): Automation 두 개** — `Tests/FishActorTests.cpp` 끝(`#endif` 앞)에:

```cpp
IMPLEMENT_SIMPLE_AUTOMATION_TEST(FSchoolSplitsWiderThanBefore, "Aquarium.School.SplitsWiderThanBefore",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FSchoolSplitsWiderThanBefore::RunTest(const FString&)
{
	UWorld* World = FAutomationEditorCommonUtils::CreateNewMap();
	AFishActor* Fish = SpawnFish(World, 5u);
	// M4c 기본값(110)보다 확실히 넓어야 한다. 기본값은 규칙 계층에서 읽어 온다
	// -- 숫자를 두 군데 적지 않는다.
	const aquarium::BoidsParams Defaults;
	TestTrue(TEXT("avoid radius was turned up"), Fish->AvoidOnlyRadius() > Defaults.avoidOnlyRadius * 1.8f);
	return true;
}

IMPLEMENT_SIMPLE_AUTOMATION_TEST(FSplitFiresOnceWhenChargingAThrong, "Aquarium.School.SplitFiresOnce",
	EAutomationTestFlags::EditorContext | EAutomationTestFlags::ProductFilter)
bool FSplitFiresOnceWhenChargingAThrong::RunTest(const FString&)
{
	UWorld* World = FAutomationEditorCommonUtils::CreateNewMap();
	UFishSchoolSubsystem* School = World->GetSubsystem<UFishSchoolSubsystem>();
	if (!TestNotNull(TEXT("school"), School)) return false;
	// 한 지점 주위에 떼를 만든다.
	for (int32 i = 0; i < 10; ++i)
	{
		AFishActor* F = SpawnFish(World, static_cast<uint32>(100 + i));
		F->PlaneOrigin = FVector(330.f + i, 0.f, 100.f);
		F->InitializeSwim();
	}
	School->InvalidateSnapshot();
	const FVector Centre(330.f, 0.f, 100.f);
	School->UpdateSplit(1.f / 60.f, Centre, /*speed*/ 120.f, /*radius*/ 240.f);
	TestTrue(TEXT("a crowd was seen"), School->LastNearCount() >= 6);
	TestEqual(TEXT("fired once"), School->SplitFireCount(), 1);
	// 바로 다음 프레임에 또 터지면 소리가 겹친다.
	School->UpdateSplit(1.f / 60.f, Centre, 120.f, 240.f);
	TestEqual(TEXT("still once"), School->SplitFireCount(), 1);
	// 느리게 지나갈 때는 터지지 않는다.
	School->UpdateSplit(2.f, Centre, /*speed*/ 2.f, 240.f);
	TestEqual(TEXT("slow drift does not split"), School->SplitFireCount(), 1);
	return true;
}
```

```bash
cd /Users/hans/dev/aquarium
UE="/Users/Shared/Epic Games/UE_5.8"
for p in 1 2; do "$UE/Engine/Build/BatchFiles/Mac/Build.sh" AquariumEditor Mac Development -Project="$PWD/unreal/Aquarium/Aquarium.uproject" -WaitMutex 2>&1 | grep -E "^Result:|error:"; done
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" -ExecCmds="Automation RunTests Aquarium; Quit" -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -cE "Test Completed.*Success"
```
기대: `82`.

- [ ] **Step 5: 변이 확인**

| 변이 | 바꿀 곳 | 빨간불 |
|---|---|---|
| A | `InitializeSwim`의 `avoidOnlyRadius = 240.f` 줄을 지운다(= M4c 볼륨으로 복귀) | `Aquarium.School.SplitsWiderThanBefore` |
| B | `Observe`의 `if (since_ < p.cooldown) return false;`를 지운다 | `Aquarium.School.SplitFiresOnce`, `a split does not fire twice in a row` |
| C | `Observe`의 `if (playerSpeed < p.minSpeed) return false;`를 지운다 | `Aquarium.School.SplitFiresOnce`(느린 표류 단언), `a split does not fire when the player is drifting slowly` |

전부 되돌린다.

- [ ] **Step 6: 클립으로 볼륨을 맞춘다** — 스틸로는 판단할 수 없다.

Task 15의 하네스를 미리 한 번 돌려도 되고, 수동 실행으로 떼 한가운데를 돌진해 봐도 된다. 확인할 것: (a) **폭발하듯** 갈라지는가(조용히 비키는 수준이면 240을 더 올린다), (b) 갈라지는 **순간에** 「촤악」이 나는가(늦으면 `minFish`를 낮춘다), (c) 물고기들이 서로를 밀어 무리가 통째로 흩어져 버리지는 않는가(그러면 `separationWeight`를 낮춘다). 조정한 값과 이유를 커밋 메시지에 적는다.

- [ ] **Step 7: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add rules tests CMakeLists.txt unreal/Aquarium/Source/Aquarium && git commit -q -F - << 'EOF'
feat(m7): 장면 4 — 무리 갈라지기 볼륨을 올리고 「촤악」을 붙였다

새 기능이 아니라 있는 것의 볼륨이다: avoidOnlyRadius 110 → 240,
separationWeight 1.7 → 2.6. 기본값은 건드리지 않고 액터에서 덮어썼다.

SplitDetector에는 반경 필드가 없다 — 엔진이 BoidsParams::avoidOnlyRadius로
센 개수를 넘겨주므로 반경이 두 곳에 존재할 수 없다. 재발화 간격은 아이의
입력을 삼키는 제한이 아니라 세계가 스스로 내는 소리의 간격이고, 그 구분을
주석에 남겼다.

규칙 134 → 139, Automation 78 → 82.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 14: 성능 — 예측하지 말고 잰다

**Files:** Create `scripts/measure_m7_perf.sh`, Create `docs/reviews/<날짜>-m7-perf.md`

시나리오: "먹이 몰려오기도 같은 종류의 규칙 계층 연산이라 부담이 크지 않을 것으로 본다 — **다만 측정한다.** M4b에서 '반투명 커튼이 제일 비쌀 것'이라는 예측이 측정으로 반증된 전례가 있다."

**기준**: M5 최종 63.50 fps / p95 16.71 ms. **노이즈 바닥 약 1.3 fps.** SRS 게이트 60 fps / 22 ms. 여유는 게이트 대비 약 5.8%로 얇다.

**아껴둔 레버**: `r.VolumetricFog.GridSizeZ` 128→64가 +17.1 fps로 측정돼 있고 아직 쓰지 않았다. **이번에도 기본으로 쓰지 않는다** — 미리 쓰면 M7의 진짜 비용이 영구히 가려진다. 평균이 60 fps 아래로 내려갈 때만 쓴다.

- [ ] **Step 1: 측정 스크립트**

`scripts/measure_m7_perf.sh`:

```bash
#!/bin/bash
# M7 프레임 비용을 항목별로 잰다. 점추정을 믿지 않는다 -- 토글 간 차이가
# 노이즈 바닥(약 1.3 fps) 아래면 '상한'으로 보고한다.
#
# 각 실행은 같은 맵·시드·자동 입력·자동 클릭을 쓴다. 클릭 패턴이 다르면
# 먹이 개수가 달라져 비교 자체가 무의미해진다.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
UE_ROOT="/Users/Shared/Epic Games/UE_5.8"
UE="$UE_ROOT/Engine/Binaries/Mac/UnrealEditor"
PROJ="$ROOT/unreal/Aquarium/Aquarium.uproject"
OUT="${OUT:-/private/tmp/claude-501/-Users-hans-dev-aquarium/670aff5b-0935-4cef-86d7-e73d4193a3f3/scratchpad/m7_perf}"
SECONDS_TO_RUN="${SECONDS_TO_RUN:-45}"
# 빈 물을 아홉 번 찍는다: 먹이가 실제로 쌓인 상태에서 재기 위한 것이고,
# 토큰 문법은 BuildAutoClicks에서 읽은 그대로다(<초>@<nx>x<ny>, 콜론 없음).
AUTO_CLICK="${AUTO_CLICK:-6@0.20x0.20,8@0.80x0.25,10@0.30x0.75,12@0.70x0.70,14@0.50x0.15,16@0.15x0.50,18@0.85x0.55,20@0.45x0.85,22@0.60x0.40}"
CLICK_COUNT="${CLICK_COUNT:-9}"
AUTO_INPUT="${AUTO_INPUT:-R3,U2,L3,D2,0 1}"
mkdir -p "$OUT"

run() {   # run <이름> [추가 플래그...]
  local name="$1"; shift
  local csv="$OUT/$name.csv" log="$OUT/$name.log"
  rm -f "$csv"
  "$UE" "$PROJ" ReefM1 -game -windowed -ResX=1920 -ResY=1080 -ForceRes \
    -notexturestreaming -unattended -nosplash -stdout -FullStdOutLogOutput \
    -seconds="$SECONDS_TO_RUN" \
    -AquariumAutoNickname="니모" -AquariumAssignmentSeed=1 \
    -AquariumAutoInput="$AUTO_INPUT" -AquariumAutoClick="$AUTO_CLICK" \
    -AquariumFrameStats="$csv" "$@" > "$log" 2>&1 || true
  # 하네스가 아무것도 검증하지 않는 것을 막는다(M5의 교훈).
  local armed bad
  armed="$(grep -o 'AquariumAutoClick: armed [0-9]*' "$log" | tail -1 | awk '{print $3}')"
  bad="$(grep -c 'AquariumAutoClick: bad token\|AquariumAutoClick: coords out of range\|AquariumAutoInput: unknown direction\|AquariumAutoInput: bad duration' "$log" || true)"
  [[ "${armed:-none}" == "$CLICK_COUNT" ]] || { echo "ERROR[$name]: armed=${armed:-none}, wanted $CLICK_COUNT" >&2; exit 1; }
  [[ "$bad" == "0" ]] || { echo "ERROR[$name]: parse warnings -- the run is not the one intended" >&2; exit 1; }
  [[ -f "$csv" ]] || { echo "ERROR[$name]: no frame stats written" >&2; exit 1; }
  python3 - "$csv" "$name" << 'PY'
import sys
rows = [float(x) for x in open(sys.argv[1]).read().split() if x.strip()]
rows = [r for r in rows if r > 0]
assert len(rows) > 300, "only %d frames in %s" % (len(rows), sys.argv[1])
rows_sorted = sorted(rows)
p95 = rows_sorted[int(len(rows_sorted) * 0.95)] * 1000.0
avg = len(rows) / sum(rows)
print("%-16s frames=%5d avg_fps=%6.2f p95_ms=%6.2f" % (sys.argv[2], len(rows), avg, p95))
PY
}

echo "== M7 성능 (같은 맵/시드/입력/클릭, ${SECONDS_TO_RUN}s) =="
run full                                   # 전부 켠 상태 = 출하 설정
run full_again                             # 실행 간 노이즈를 재기 위한 재실행
run no_food      -AquariumNoFood
run no_audio     -AquariumNoAudio
run no_both      -AquariumNoFood -AquariumNoAudio
echo "노이즈 바닥은 full과 full_again의 차이다. 항목별 차이가 그보다 작으면"
echo "점추정이 아니라 '상한'으로 보고한다(M4c/M5와 같은 규약)."
```

```bash
cd /Users/hans/dev/aquarium && chmod +x scripts/measure_m7_perf.sh
```

**주의**: `-AquariumFrameStats`가 쓰는 CSV 형식을 먼저 확인한다 — 위 파이썬은 "한 줄에 숫자 하나"를 가정한다.

```bash
cd /Users/hans/dev/aquarium && sed -n '/WriteFrameStats/,/^}/p' unreal/Aquarium/Source/Aquarium/DiverPlayerController.cpp
```
형식이 다르면 **파서를 고친다**(엔진을 고치지 않는다). 기존 `scripts/measure_m2b_perf.sh`가 이미 같은 파일을 읽고 있으면 그 파싱을 그대로 가져다 쓴다.

- [ ] **Step 2: 잰다**

```bash
cd /Users/hans/dev/aquarium && ./scripts/measure_m7_perf.sh 2>&1 | tail -12
```

- [ ] **Step 3: 판정**

- **full의 평균이 60 fps 이상**이면 통과다. `GridSizeZ` 레버는 **쓰지 않는다**.
- **60 fps 아래**면: 먼저 `no_food`/`no_audio` 차이로 원인을 귀속한다. 차이가 노이즈 바닥 아래인데도 느리다면 M7이 아닌 다른 것이 원인이므로 **그렇게 보고한다**(M4c에서 토글을 껐는데 더 느렸던 전례 — 부호가 반대면 비용이 실재하지 않는다). 원인이 먹이로 귀속되고 알갱이가 수백 개일 때만 느려진다면, **개수를 제한하지 말고**(금지 사항) `SeekNearestFood`를 N프레임마다 한 번만 부르는 식으로 **빈도**를 낮춘다 — 아이는 물고기가 0.1초 늦게 방향을 트는 것을 알아채지 못한다.
- 그래도 모자라면 마지막에 `GridSizeZ` 128→64 레버를 쓰고, **썼다는 사실과 이유를 반드시 기록한다.**

- [ ] **Step 4: `docs/reviews/<날짜>-m7-perf.md`에 기록**

표 형식은 `docs/reviews/2026-09-21-m5-perf.md`를 그대로 따른다(먼저 읽고 형식을 맞춘다). 반드시 담을 것: 다섯 실행의 평균·p95, **full과 full_again의 차이(=이번 측정의 노이즈 바닥)**, 항목별 차이와 그것이 노이즈 바닥 대비 몇 분의 일인지, M5 기준선 대비 변화, 게이트 대비 여유(%), `GridSizeZ` 레버를 썼는지 여부와 이유.

- [ ] **Step 5: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add scripts/measure_m7_perf.sh docs/reviews && git commit -q -F - << 'EOF'
perf(m7): 항목별 프레임 비용 측정 (먹이·소리 토글)

예측하지 않고 쟀다. 하네스는 armed 수와 파싱 경고 0건을 단언하므로
"아무 입력도 없는 그럴듯한 측정"이 나올 수 없다. 토글 간 차이가 노이즈
바닥 아래면 점추정이 아니라 상한으로 보고한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 15: 캡처 하네스 — 전/후 클립과 **무는 로그**

**Files:** Modify `.../DiverPlayerController.h/.cpp`, Create `scripts/render_m7_fun.sh`

M5의 캡처 하네스는 **계획한 클릭 일곱 개가 전부 빈 물을 맞혔는데도** 경고 0건에 그럴듯한 클립을 냈다. 그때는 빈 물이 무반응이라 로그가 "전부 미스"였지만, **M7에서는 빈 물도 먹이를 뿌리므로 클립이 더 그럴듯해진다.** 그래서 로그가 더 많이 물어야 한다.

`-AquariumFxLog=<절대 CSV 경로>`를 더한다. 한 행이 클릭 한 번이고, 열은 **무엇이 실제로 일어났는가**다: 물고기를 맞혔는지, 먹이를 몇 알 뿌렸는지, 기포를 몇 개 띄웠는지, 그리고 **먹이가 실제로 먹혔는지**. 닉네임 열은 없다(P-03).

- [ ] **Step 1: 로그 플래그**

`DiverPlayerController.h`에:

```cpp
	// Parses -AquariumFxLog=<absolute csv path>; false when absent or empty. Dev-only.
	// 한 행이 클릭 한 번이다. 닉네임 열은 없고, 앞으로도 없다(P-03).
	static bool ParseFxLogPath(const TCHAR* CmdLine, FString& OutPath);
```

private에:

```cpp
	FString FxLogPath;
	TArray<FString> FxLogRows;
	void StartFxLogIfRequested();
	void WriteFxLog();
	void RecordFx(const TCHAR* Kind, int32 Pellets, int32 Bubbles);
```

`.cpp`에 — `ParseClickLogPath`의 구현을 그대로 본떠 쓰고(같은 파일에 있다), `BeginPlay`/`EndPlay`에 `StartFxLogIfRequested()`/`WriteFxLog()`를 `ClickLog`와 나란히 건다. 기록 함수:

```cpp
void ADiverPlayerController::RecordFx(const TCHAR* Kind, int32 Pellets, int32 Bubbles)
{
#if !UE_BUILD_SHIPPING
	if (FxLogPath.IsEmpty())
	{
		return;
	}
	UWorld* W = GetWorld();
	UFoodSubsystem* FoodSys = W ? W->GetSubsystem<UFoodSubsystem>() : nullptr;
	UBubbleSubsystem* BubbleSys = W ? W->GetSubsystem<UBubbleSubsystem>() : nullptr;
	UFishSchoolSubsystem* School = W ? W->GetSubsystem<UFishSchoolSubsystem>() : nullptr;
	FxLogRows.Add(FString::Printf(TEXT("%d,%.3f,%s,%d,%d,%d,%d,%d"),
		FxLogRows.Num() - 1, AutoClickElapsed, Kind, Pellets, Bubbles,
		FoodSys ? FoodSys->LiveCount() : 0,
		FoodSys ? FoodSys->EatenTotal() : 0,
		School ? School->SplitFireCount() : 0));
	(void)BubbleSys;
#endif
}
```

`HandleClickRay`의 두 경로(빈 물 / 물고기)에서 각각 `RecordFx(TEXT("food"), FoodSys ? FoodSys->Params().scatterCount : 0, 4);`와 `RecordFx(bOwnFish ? TEXT("own") : TEXT("fish"), 0, bOwnFish ? 5 : 9);`를 부른다. `WriteFxLog`는 헤더 `index,time_s,kind,pellets,bubbles,live_pellets,eaten_total,split_fires`를 먼저 쓴다.

- [ ] **Step 2: 하네스**

`scripts/render_m7_fun.sh`:

```bash
#!/bin/bash
# M7 검토 산출물. 이 마일스톤은 소리가 절반이므로 클립에는 **오디오가 없다**
# (엔진 캡처는 프레임만 받는다). 소리는 사용자 수동 확인 두 건으로 넘긴다 --
# docs/reviews/<날짜>-m7-summary.md의 인계 항목.
#
# 산출물 (docs/reviews/<날짜>-m7-*):
#   fun.mp4     30 fps, 40 s. 같은 맵/시드/카메라, 스크립트 클릭과 입력.
#   fx.csv      클릭 한 번당 한 행: 무엇을 맞혔고 무엇이 생겼는가
#   clicks.csv  기존 클릭 로그(적중 판정 쪽)
#   scene.png   t=20 프레임
#   compare.png <before=M5 flee 스틸> | scene.png
#
# 토큰 문법은 기억이 아니라 ADiverPlayerController::BuildAutoClicks /
# BuildAutoInputSteps에서 읽은 것이다:
#   클릭 <초>@<nx>x<ny>  (콜론 없음)      입력 <방향문자><초> (R/L/U/D/0)
# 두 파서 모두 알아볼 수 없는 토큰에 경고만 찍고 조용히 버리므로, 아래에서
# 경고 0건과 armed N을 단언한다. M5는 이 단언이 있었는데도 "일곱 번 다 빈 물"을
# 놓쳤다 -- 그래서 이번에는 fx.csv의 내용까지 단언한다.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
UE_ROOT="/Users/Shared/Epic Games/UE_5.8"
UE="$UE_ROOT/Engine/Binaries/Mac/UnrealEditor"
FFMPEG="${FFMPEG:-/opt/homebrew/bin/ffmpeg}"
PROJ="$ROOT/unreal/Aquarium/Aquarium.uproject"
FRAMES="$ROOT/unreal/Aquarium/Saved/UiFrames"
REVIEWS="$ROOT/docs/reviews"
LOG="${LOG:-/tmp/m7-capture.log}"
MAP="${MAP:-ReefM1}"
FPS="${FPS:-30}"
SECONDS_TO_RUN="${SECONDS_TO_RUN:-40}"
WATCHDOG_SEC="${WATCHDOG_SEC:-900}"
AUTO_NICKNAME="${AUTO_NICKNAME:-니모}"
ASSIGNMENT_SEED="${ASSIGNMENT_SEED:-1}"
SKIP_FRAMES="${SKIP_FRAMES:-15}"
DATE="$(date +%F)"

# 조준점은 추측하지 않는다. M5가 쓴 실측 좌표(592.6/348.0 평면에서 실제로
# 물고기를 맞힌 점)를 재사용하고, 빈 물 좌표는 화면 위쪽 열린 물이다.
#   6.0  배경 물고기          6.3  같은 자리 연타(무시되지 않는다)
#   9.0  빈 물 -> 먹이        11.0 빈 물 -> 먹이(반대쪽)
#   13.0 빈 물 -> 먹이        16.0 내 물고기(재롱, 조종 유지)
#   24.0 배경 물고기          30.0 빈 물 -> 먹이
AUTO_CLICK="${AUTO_CLICK:-6.0@0.30x0.30,6.3@0.30x0.30,9.0@0.50x0.12,11.0@0.80x0.30,13.0@0.20x0.65,16.0@0.50x0.50,24.0@0.35x0.35,30.0@0.65x0.70}"
CLICK_COUNT="${CLICK_COUNT:-8}"
# t=17부터 오른쪽으로 달려 떼 한가운데를 통과한다(장면 4가 터지는 구간).
AUTO_INPUT="${AUTO_INPUT:-0 15,R6,U2,L4,D2,0 11}"
BEFORE="${BEFORE:-$REVIEWS/2026-09-21-m5-flee.png}"
FX="$REVIEWS/$DATE-m7-fx.csv"
CSV="$REVIEWS/$DATE-m7-clicks.csv"
OUT="$REVIEWS/$DATE-m7-fun.mp4"
STILL="$REVIEWS/$DATE-m7-scene.png"
COMPARE="$REVIEWS/$DATE-m7-compare.png"

# 비교 대상이 정말 M5인지. M4b는 한때 자기 자신과 비교했고 hstack은 멀쩡해 보였다.
case "$(basename "$BEFORE")" in
  *m7*) echo "ERROR: BEFORE points at an M7 artefact - that compares M7 against itself" >&2; exit 1 ;;
  *m5*) ;;
  *) echo "ERROR: BEFORE is not the M5 still ($BEFORE)" >&2; exit 1 ;;
esac
[[ -f "$BEFORE" ]] || { echo "ERROR: before image not found: $BEFORE" >&2; exit 1; }
mkdir -p "$REVIEWS"

if [[ "${SKIP_BUILD:-0}" != "1" ]]; then
  for pass in 1 2; do   # UBT가 UnrealEditor.modules를 한 빌드 늦게 쓴다
    echo "build pass $pass"
    "$UE_ROOT/Engine/Build/BatchFiles/Mac/Build.sh" AquariumEditor Mac Development \
      -Project="$PROJ" -WaitMutex 2>&1 | grep -E "Result:|error:|Error:" || true
  done
fi

if [[ "${SKIP_CAPTURE:-0}" != "1" ]]; then
  rm -rf "$FRAMES"; mkdir -p "$FRAMES"; rm -f "$CSV" "$FX"
  START=$(date +%s)
  "$UE" "$PROJ" "$MAP" -game -windowed -ResX=1920 -ResY=1080 -ForceRes \
    -benchmark -fps="$FPS" -seconds="$SECONDS_TO_RUN" \
    -notexturestreaming -unattended -nosplash -stdout -FullStdOutLogOutput \
    -AquariumAutoNickname="$AUTO_NICKNAME" -AquariumAssignmentSeed="$ASSIGNMENT_SEED" \
    -AquariumAutoInput="$AUTO_INPUT" -AquariumAutoClick="$AUTO_CLICK" \
    -AquariumClickLog="$CSV" -AquariumFxLog="$FX" \
    -AquariumCaptureUI="$FRAMES" > "$LOG" 2>&1 &
  PID=$!
  while kill -0 "$PID" 2>/dev/null; do
    if (( $(date +%s) - START > WATCHDOG_SEC )); then
      echo "watchdog: killing pid $PID" >&2; kill "$PID" 2>/dev/null || true
      sleep 5; kill -9 "$PID" 2>/dev/null || true; break
    fi
    sleep 2
  done
  wait "$PID" || true
  echo "engine wall time: $(( $(date +%s) - START ))s frames=$(ls "$FRAMES"/UiFrame*.png 2>/dev/null | wc -l | tr -d ' ')"
fi

# --- 단언: 아무것도 검증하지 않은 캡처는 캡처가 없는 것보다 나쁘다 -----------
ARMED="$(grep -o 'AquariumAutoClick: armed [0-9]*' "$LOG" | tail -1 | awk '{print $3}')"
BAD="$(grep -c 'AquariumAutoClick: bad token\|AquariumAutoClick: coords out of range\|AquariumAutoInput: unknown direction\|AquariumAutoInput: bad duration' "$LOG" || true)"
MATERIAL="$(grep -c 'Failed to compile Material' "$LOG" || true)"
echo "ARMED=${ARMED:-none} PARSE_WARNINGS=$BAD MATERIAL_COMPILE_FAILURES=$MATERIAL"
[[ "${ARMED:-none}" == "$CLICK_COUNT" ]] || { echo "ERROR: auto-click script was silently truncated" >&2; exit 1; }
[[ "$BAD" == "0" ]] || { echo "ERROR: parse warnings - the capture is not the one intended" >&2; grep -h 'Aquarium\(AutoClick\|AutoInput\):' "$LOG" | head >&2; exit 1; }
[[ "$MATERIAL" == "0" ]] || { echo "ERROR: materials fell back to the grey default" >&2; exit 1; }

[[ -f "$FX" ]] || { echo "ERROR: no fx log written" >&2; exit 1; }
head -1 "$FX" | grep -qx 'index,time_s,kind,pellets,bubbles,live_pellets,eaten_total,split_fires' \
  || { echo "ERROR: unexpected fx-log columns" >&2; exit 1; }
grep -qF "$AUTO_NICKNAME" "$FX" && { echo "ERROR: nickname leaked into the fx log" >&2; exit 1; } || true

FOOD_ROWS="$(awk -F, 'NR>1 && $3=="food"' "$FX" | wc -l | tr -d ' ')"
FISH_ROWS="$(awk -F, 'NR>1 && $3=="fish"' "$FX" | wc -l | tr -d ' ')"
OWN_ROWS="$(awk -F, 'NR>1 && $3=="own"' "$FX" | wc -l | tr -d ' ')"
EATEN="$(awk -F, 'NR>1{e=$7} END{print e+0}' "$FX")"
SPLITS="$(awk -F, 'NR>1{s=$8} END{print s+0}' "$FX")"
echo "FX food=$FOOD_ROWS fish=$FISH_ROWS own=$OWN_ROWS eaten_total=$EATEN split_fires=$SPLITS"
# M5는 "일곱 번 다 빈 물"을 놓쳤다. 이번에는 네 장면이 전부 실제로 일어났는지 본다.
[[ "$FOOD_ROWS" -ge 3 ]] || { echo "ERROR: 빈 물 클릭이 먹이를 뿌리지 않았다 (장면 3 미확인)" >&2; exit 1; }
[[ "$FISH_ROWS" -ge 2 ]] || { echo "ERROR: 배경 물고기를 한 번도 못 맞혔다 (장면 2 미확인)" >&2; exit 1; }
[[ "$OWN_ROWS" -ge 1 ]] || { echo "ERROR: 내 물고기를 한 번도 못 맞혔다 (재롱 미확인)" >&2; exit 1; }
[[ "$EATEN" -ge 1 ]] || { echo "ERROR: 먹이를 아무도 먹지 않았다 -- 뿌리기만 하고 오는 물고기가 없다" >&2; exit 1; }
[[ "$SPLITS" -ge 1 ]] || { echo "ERROR: 무리가 한 번도 갈라지지 않았다 (장면 4 미확인)" >&2; exit 1; }
echo "GUARDS_OK"

# --- 산출물 -------------------------------------------------------------------
COUNT="$(ls "$FRAMES"/UiFrame*.png | wc -l | tr -d ' ')"
(( COUNT >= FPS * SECONDS_TO_RUN - FPS )) || { echo "ERROR: expected ~$(( FPS * SECONDS_TO_RUN )) frames, got $COUNT" >&2; exit 1; }
"$FFMPEG" -y -loglevel error -framerate "$FPS" -start_number "$SKIP_FRAMES" \
  -i "$FRAMES/UiFrame%05d.png" -c:v libx264 -pix_fmt yuv420p -crf 18 "$OUT"
echo "VIDEO_OK $OUT frames=$(( COUNT - SKIP_FRAMES ))"
"$FFMPEG" -y -loglevel error -ss 20 -i "$OUT" -frames:v 1 "$STILL"
echo "STILL_OK $STILL"
"$FFMPEG" -y -loglevel error -i "$BEFORE" -i "$STILL" -filter_complex hstack "$COMPARE"
W="$(/opt/homebrew/bin/ffprobe -v error -select_streams v:0 -show_entries stream=width -of csv=p=0 "$COMPARE")"
HALF=$(( W / 2 ))
LDIFF="$($FFMPEG -hide_banner -i "$COMPARE" -i "$BEFORE" -filter_complex "[0:v]crop=$HALF:ih:0:0[l];[l][1:v]psnr" -f null - 2>&1 | sed -n 's/.*average:\([0-9.a-z]*\).*/\1/p' | head -1)"
RDIFF="$($FFMPEG -hide_banner -i "$COMPARE" -i "$STILL" -filter_complex "[0:v]crop=$HALF:ih:$HALF:0[r];[r][1:v]psnr" -f null - 2>&1 | sed -n 's/.*average:\([0-9.a-z]*\).*/\1/p' | head -1)"
echo "HSTACK_LEFT_PSNR_vs_BEFORE=$LDIFF HSTACK_RIGHT_PSNR_vs_AFTER=$RDIFF"
[[ "$LDIFF" == "inf" && "$RDIFF" == "inf" ]] || { echo "ERROR: hstack halves are not (M5 | M7)" >&2; exit 1; }
echo "COMPARE_OK $COMPARE"
ls -l "$REVIEWS/$DATE-m7-"*
```

```bash
cd /Users/hans/dev/aquarium && chmod +x scripts/render_m7_fun.sh && ./scripts/render_m7_fun.sh 2>&1 | tail -20
```
기대: `GUARDS_OK`, `VIDEO_OK`, `STILL_OK`, `COMPARE_OK`. **단언 중 하나라도 걸리면 조준점을 고친다** — 클립이 그럴듯해 보인다는 것은 증거가 아니다.

- [ ] **Step 3: 클립을 실제로 열어서 본다** — 이 스텝을 건너뛰지 않는다.

```bash
cd /Users/hans/dev/aquarium && open "docs/reviews/$(date +%F)-m7-fun.mp4" "docs/reviews/$(date +%F)-m7-compare.png"
```

확인 목록(하나씩 적어 둔다):
1. 물고기를 찍으면 **확실히 빠르게** 달아나고 **매번 다른 몸짓**이다
2. 기포가 **화면 위로 나가며** 사라진다(중간에 툭 없어지지 않는다)
3. 빈 물을 찍으면 알갱이가 **흩뿌려진다**(한 점에 겹치지 않는다)
4. **화면 먼 쪽의 물고기들이 방향을 틀어 온다** — 이것이 장면 3 조건 2다
5. 떼 한가운데를 통과할 때 **폭발하듯** 갈라진다
6. 내 물고기를 찍었을 때 화면 밖으로 튀어나가지 **않는다**
7. M5 스틸(왼쪽)과 비교해 **바다의 룩이 망가지지 않았다**(M4b의 안개·빛줄기가 그대로)

- [ ] **Step 4: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add scripts/render_m7_fun.sh unreal/Aquarium/Source/Aquarium docs/reviews && git commit -q -F - << 'EOF'
test(m7): 캡처 하네스와 무는 FX 로그

M5의 하네스는 경고 0건·armed 7을 단언하고도 "일곱 번 다 빈 물"을 놓쳤다.
M7에서는 빈 물도 먹이를 뿌려 클립이 더 그럴듯해지므로 로그가 더 물어야 한다 —
fx.csv가 클릭마다 무엇이 실제로 일어났는지 적고, 하네스는 네 장면이 전부
일어났음(먹이 뿌림·물고기 맞힘·내 물고기 맞힘·먹이 먹힘·무리 갈라짐)을
단언한다. 닉네임 열은 없다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 16: 문서 갱신과 **수동 확인 인계**

**Files:** Modify `docs/TASK.md`, Modify `docs/SETUP.md`, Create `docs/reviews/<날짜>-m7-summary.md`

- [ ] **Step 1: `docs/TASK.md`의 로드맵 표에 M7 행을 더한다** (M6 행 뒤):

```markdown
| M7 | 재미 1차 — 소리·클릭 과장·빈물 먹이·무리 갈라지기 | `docs/scenario.md`의 네 장면과 검증표. **2026-09-21 구현 완료**: 규칙 103 → **139** + Automation 60 → **82** 통과. 반응음 5종을 `assets/audio/make_sounds.py`(표준 라이브러리만)가 합성하고 배경만 CC0 녹음이며, **MetaSound·SoundCue를 쓰지 않는다**(에디터 파이썬으로 만들 수 없다 — M4b의 Niagara와 같은 벽). 소리는 전부 2D라 감쇠 에셋이 없다. 먹이·기포·반응·분리 감지는 전부 규칙 계층 순수 함수이고, **규칙 순서는 입력/Wander → 먹이 → 도망 → 무리 → 장애물 → 경계 → StepMotion → Clamp**로 먹이 한 칸만 끼웠다(경계는 여전히 마지막 — M3 보장). 먹이와 기포는 액터가 아니라 서브시스템의 POD 배열이고 인스턴스 메시 하나가 그리므로 **액터 틱이 0개**이며 개수 제한이 없다. **내 물고기는 클릭해도 도망가지 않는다**(조종권 불변, 같은 조건의 대조군과 궤적이 한 치도 다르지 않음을 단언). 소리를 끄는 **그림 전용** 버튼 추가. 성능·산출물은 아래 참고. **사용자 수동 확인 2건 대기(소리)** |
```

- [ ] **Step 2: `docs/TASK.md`의 "하드 트랩" 목록에 M7이 새로 배운 것을 더한다**

```markdown
- **M7에서 확정한 사실 네 가지**:
  1. **이 기계에는 numpy가 없다**(system python3.14, UE 번들 python3.11 둘 다). 소리·아이콘 생성은 표준 라이브러리(`wave`/`array`/`math`/`zlib`/`struct`)만으로 쓰여 있고, 그래서 의존성 설치 없이 어디서나 재현된다.
  2. **MetaSound·SoundCue는 에디터 파이썬으로 만들 수 없다** — M4b의 Niagara와 같은 벽이다. 이 프로젝트의 소리는 `USoundWave` + C++ 재생뿐이고, `unreal.SoundFactory`는 루프 플래그를 갖지 않으므로 **임포트된 SoundWave에 직접 설정하고 되읽어 확인**해야 한다.
  3. **F-11의 "도망 중 재터치 무시"를 의도적으로 폐기했다.** 시나리오 장면 2 요구사항 4(연타가 기본 사용법)와 정면으로 충돌하기 때문이다. 뒤집힌 테스트는 지우지 않고 이유를 적어 뒤집어 두었다.
  4. **먹이는 깊이로 거르지 않고 보이즈는 거른다.** 비대칭이 의도다 — 카메라가 고정이라 아이의 모형은 2D이고, 여러 평면의 물고기가 같은 화면 지점으로 모이는 것이 곧 "떼가 몰려왔다"이다.
```

- [ ] **Step 3: `docs/SETUP.md`에 M7 항목을 더한다** — 스크립트 순서(Task 3 Step 5에서 이미 고쳤다)가 맞는지 확인하고, 소리 생성 단계를 파이프라인 앞에 적는다:

```bash
# 0. 스크립트가 굽는 에셋(소리·아이콘)을 먼저 만든다
python3 assets/audio/make_sounds.py        # SOUNDS_OK
python3 assets/ui/make_icons.py            # ICONS_OK
./scripts/check_ambience.sh                # AMBIENCE_OK (CC0 녹음이 있어야 한다)
```

그리고 함정 목록에 한 줄: **에디터 파이썬 판정은 `AUDIO_OK`/`UI_OK`/`FX_OK` grep으로만 한다**(종료 코드는 항상 1).

- [ ] **Step 4: `docs/reviews/<날짜>-m7-summary.md`를 쓴다**

`docs/reviews/2026-09-21-m6-summary.md`의 형식을 먼저 읽고 맞춘다. 반드시 담을 것:

1. **네 장면 각각이 어디에 구현됐는가**(파일·함수 이름)
2. **시나리오 결정표의 모든 행 → 태스크 대응표**
3. **성능 결과**와 `GridSizeZ` 레버 사용 여부
4. **사용자 수동 확인 인계 — 두 건이고 서로 다른 검사다**:

```markdown
### 사용자 수동 확인 (사람만 할 수 있는 것)

**① 각 소리가 제때 제대로 들리는가** (약 5분)
```bash
"/Users/Shared/Epic Games/UE_5.8/Engine/Binaries/Mac/UnrealEditor" \
  "<저장소>/unreal/Aquarium/Aquarium.uproject" ReefM1 -game -windowed \
  -ResX=1280 -ResY=720 -ForceRes
```
확인: (a) 입장 순간 물속 느낌이 깔린다 (b) 방향키를 누르면 물살 소리가 나고 빨리 가면 음이 올라간다 (c) 물고기를 찍으면 「꺅」, 빈 물을 찍고 물고기가 먹으면 「뽁」이고 **둘이 확실히 다르다** (d) 떼를 가르면 「촤악」 (e) 내 물고기를 찍으면 「뽀글」 (f) 초당 서너 번 마구 찍어도 **소리가 지지직거리지 않고, 무시당한 느낌도 없다** (g) 스피커 그림을 누르면 즉시 조용해지고 게임은 그대로 돈다.

**② 앰비언스를 스피커로 20분 틀어 놓고 일해 본다** (20분, **헤드폰이 아니라 스피커**)
게임을 켜 둔 채 다른 일을 한다. 저음이 너무 두꺼우면 **어른은 못 느끼는데 아이는 20분쯤 뒤에 이유 없이 짜증을 낸다.** 20분 뒤 스스로에게 묻는다: 소리를 끄고 싶었던 순간이 있었는가, 머리가 무거운가. 있으면 `A_Underwater.wav`를 더 얌전한 것으로 바꾸거나 `kAmbienceVolume`을 낮춘다.

**③ 최종 판정은 아이가 한다.** 키즈부장이 아이 눈으로 읽는 것까지가 우리 몫이고, 진짜 판정은 아이가 앉아 보는 것이다. 볼 것 하나: **처음 10초 안에 웃는가.**
```

- [ ] **Step 5: 커밋**

```bash
cd /Users/hans/dev/aquarium && git add docs && git commit -q -F - << 'EOF'
docs(m7): 로드맵·설치 절차 갱신과 수동 확인 인계

소리는 헤드리스로 들을 수 없다. 자동 검증이 닿은 한계(재생 호출·파형 결정성)와
사람만 할 수 있는 확인 두 건(각 소리 확인 / 앰비언스 20분 스피커 재생)을
명시적으로 갈라 적었다. M7이 확정한 환경 사실 네 가지도 하드 트랩 목록에
남겼다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -1
```

---

### Task 17: 자체 검토와 마감

**Files:** Modify `docs/superpowers/plans/2026-09-21-m7-fun-first.md`(후속 항목), 필요 시 소스

- [ ] **Step 1: 시나리오 결정표를 한 행씩 대조한다** — 구현물에 실제로 있는지 확인하고, 없으면 그 이유를 아래 후속 항목에 적는다.

| 시나리오 행 | 어디에 있는가 | 확인 |
|---|---|---|
| 1차는 "볼륨 올리기" | 새 모드 없음. 레벨 무변경 | `git diff --stat main...HEAD -- unreal/Aquarium/Content/Maps` 가 비어 있다 |
| 네 장면을 한 덩어리로 | Task 5·6 / 10 / 12 / 13 | 넷 다 커밋됨 |
| 물고기=놀람, 빈물=먹이 | `HandleClickRay` 두 갈래 | fx.csv에 `fish`/`food` 행이 둘 다 있다 |
| 배경 소리 = CC0 녹음 | `assets/audio/ambience/` + ASSETS.md | `check_ambience.sh`가 `AMBIENCE_OK` |
| 반응 소리 = 파이썬 합성 | `assets/audio/make_sounds.py` | `SOUNDS_OK`, 두 번 실행 바이트 동일 |
| 헤엄 소리 = 속도에 따라 | `aquarium::SwimPitch` | `Aquarium.Audio.SwimFollowsSpeed` |
| 낚시꾼 없음 | 구현 없음 | `grep -ri "fisher\|hook\|낚시" unreal/Aquarium/Source` 가 0건 |
| Q·W·E·R 말풍선 없음 | 구현 없음 | `grep -rn "BindKey(EKeys::Q\|EKeys::W\|EKeys::E\|EKeys::R" unreal/Aquarium/Source` 가 0건 |
| 내 물고기는 도망 안 감 | `ApplyFleeFrom` 분기 | `Aquarium.Fish.OwnFishKeepsControl` |
| Niagara 안 씀 | 인스턴스 메시 + 해석적 머티리얼 | `grep -ri niagara unreal/Aquarium/Source unreal/Aquarium/Scripts` 가 0건 |
| MetaSound 안 씀 | SoundWave + C++ | `grep -ri "metasound\|SoundCue" unreal/Aquarium` 가 0건 |
| 먹이에 자원 개념 없음 | `FoodParams`에 상한 없음 | `Aquarium.Food.HasNoLimit`, `there is no limit anywhere in this header` |
| 소리 끄는 그림 버튼 | `HudWidget` 뮤트 버튼 | `Aquarium.Hud.MuteButtonHasNoText` |
| 「나가기」 위치 확인 | Task 0 Step 9 | 판정을 summary에 적었다 |
| 성능은 측정한다 | Task 14 | `docs/reviews/<날짜>-m7-perf.md` |

- [ ] **Step 2: 전체 테스트를 마지막으로 한 번 더 돌린다**

```bash
cd /Users/hans/dev/aquarium
cmake --build build -j 2>&1 | tail -2 && ctest --test-dir build 2>&1 | tail -3
UE="/Users/Shared/Epic Games/UE_5.8"
for p in 1 2; do "$UE/Engine/Build/BatchFiles/Mac/Build.sh" AquariumEditor Mac Development -Project="$PWD/unreal/Aquarium/Aquarium.uproject" -WaitMutex 2>&1 | grep -E "^Result:"; done
"$UE/Engine/Binaries/Mac/UnrealEditor-Cmd" "$PWD/unreal/Aquarium/Aquarium.uproject" -ExecCmds="Automation RunTests Aquarium; Quit" -unattended -nopause -nosplash -nullrhi -stdout -FullStdOutLogOutput 2>&1 | grep -cE "Test Completed.*Success"
```
기대: `100% tests passed out of 139`, `82`.

- [ ] **Step 3: 개인정보 검사와 레벨 무변경을 확인한다**

```bash
cd /Users/hans/dev/aquarium
./scripts/verify_privacy.sh
git diff --stat main...HEAD -- unreal/Aquarium/Content/Maps assets/models assets/textures assets/fonts
grep -rn "니모\|AutoNickname" docs/reviews/*m7* | grep -v "AutoNickname=" | head
```
기대: 개인정보 검사 통과, 레벨·기존 에셋 변경 0건, 리뷰 CSV에 닉네임 없음.

- [ ] **Step 4: 아래 "구현 중 발견한 후속 항목"을 채운다.** 빈칸으로 두지 않는다 — **아무것도 없었다면 "없음"이라고 적고 그 근거를 적는다.** 특히 적을 것:
  - 빨간불이 **켜지지 않은** 변이가 있었는가(있었다면 그 테스트는 증거가 아니다)
  - 계획서의 이름·숫자·API 중 **실제와 달랐던 것 전부**(열거형 이름, 테스트 개수, CSV 형식 등)
  - `GridSizeZ` 레버를 썼는가
  - 조정한 튜닝 값(`avoidOnlyRadius`, `scatterCount`, 볼륨)과 그 근거

- [ ] **Step 5: 마무리 커밋**

```bash
cd /Users/hans/dev/aquarium && git add docs && git commit -q -F - << 'EOF'
docs(m7): 시나리오 결정표 전수 대조와 후속 항목 기록

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01574CFGe3fEWvWY7mPHqLKJ
EOF
git log --oneline -3
```

---

## 구현 중 발견한 후속 항목

*(실행 중에 채운다. 비워 둔 채로 마감하지 않는다 — 아무것도 없었다면 "없음"과 그 근거를 적는다.)*

### 사양 변경 (조용히 넘어가지 않는 것)

- **F-11 폐기 (Task 8).** M5의 "도망 중 재터치 무시"를 없앴다. 시나리오 장면 2 요구사항 4가
  연타를 기본 사용법으로 못 박았다. `FleeStateMachine::Touch`는 이제 재터치마다 다시 겨누고
  타이머를 새로 채우며 `TouchCount()`로 모든 터치를 센다. 옛 동작을 되살리는 변이로 규칙 3개와
  Automation 1개가 빨간불이 되는 것을 확인했다. `docs/TASK.md`에도 적었다.
- **F-12가 실질적으로 폐기됐다 (Task 10).** 시나리오 결정표가 "내 물고기는 클릭해도 도망가지
  않는다"로 정했으므로, "도망이 방향키를 이긴다"는 F-12는 게임 안에서 도달할 수 없는 조건이
  되었다. `Aquarium.Fish.FleeBeatsArrowKeys`와 `RecoveryUsesCurrentInput`을 지우지 않고
  **뒤집어** 두었다(이제 방향키가 이긴다). SRS 문구 개정은 사용자 결정 사항이라 하지 않았다.
- **대상 나이 변경(7살 → 초등 5~6학년) 반영 (Task 10).** "1.5배로 팍 부풀기"는 구현하지 않았다.
  과장은 전부 `StartleShape::speedScale`(최대 2.53배)로만 표현된다 — 크기 배율 코드가 한 줄도
  없다. `Reaction.h`에 이유를 적어 두었다.

### 계획의 오류와 수정

- **Task 7 Step 2**: `UTexture2D`에 `imported_size` 프로퍼티가 없다. `blueprint_get_size_x/y`로
  고쳤다(`dir()`로 확인). `TC_EDITOR_ICON`·`TEXTUREGROUP_UI`는 계획대로 맞았다.
- **Task 7 Step 8 변이 C**: `MuteButton->AddChild(UTextBlock)`은 `UButton`이 단일 자식
  위젯이라 **아무 일도 일어나지 않는 무효 변이**였다(테스트는 초록불인 채였다). 그림 자리를
  글자로 **대체**하는 변이로 바꿔 빨간불을 확인했다. 변이 A도 마찬가지로 무효였고
  (`SetMutedVisual`이 다시 칠한다), `SetMutedVisual` 안의 브러시 갱신을 지우는 변이로 바꿨다.
  테스트에는 "상태 변수"가 아니라 **실제 브러시 텍스처**를 보는 단언을 더했다.
- **Task 8 Step 2**: 계획의 `PlayerReaction` 테스트는 `Touch` 직후 `RollOffsetDeg != 0`을
  요구했지만 `elapsed=0`에서는 어느 스타일이든 0이라 **통과할 수 없는 기대값**이었다. 요구사항
  (0.05초 안에 시작)을 직접 재는 테스트로 바꾸고, 떨림을 사인이 아니라 코사인으로 두어 첫
  프레임부터 최대 진폭이 나오게 했다.
- **Task 10 Step 5의 `BubbleTopZ`가 틀렸다 — 시선의 약속을 정면으로 깨는 오류였다.** 계획은
  소멸 높이를 **플레이어 평면 하나**에서 파생했는데, 배경 물고기는 X 330~700에 있고 플레이어
  평면은 X 220이다. 원근이라 깊은 평면일수록 화면 위 끝이 높아서, 가장 깊은 물고기의 기포는
  **화면 절반 높이에서 사라졌을 것이다**(측정: 소멸 높이 194.4 vs 그 깊이의 실제 화면 위 끝
  389.9). 소멸 높이를 **기포마다** 그 기포가 난 깊이에서 파생하도록 고쳤다(`Bubble::topY`,
  `AAquariumGameMode::ScreenTopZAt`). `Aquarium.Bubbles.TopFollowsDepth`가 이것을 지키고,
  계획의 식으로 되돌리는 변이로 빨간불을 확인했다.
- **Task 10에서 실제 결함 하나를 찾아 고쳤다**: 방향 제한기가 `GetActorQuat()`을 되먹임하는데
  놀람의 연출 회전이 거기 섞여 있어, 제한기가 매 프레임 그것을 되돌리려 들면서 프레임당 회전이
  **178도까지 튀었다**(F-13 연속성 검사가 빨간불). 연출 회전을 뺀 자세를 `FacingQuat`에 따로
  들고 그것을 제한기의 입력으로 쓴다. 놀람 회전도 회복 구간에서 360의 배수까지 마저 돌아
  끝나므로 Normal로 돌아갈 때 튐이 없다.
- **Task 10의 기존 테스트 헬퍼 `SpawnAimedFish`**: 헤딩을 고정하려고 `bPlayerControlled`를 켜
  두었는데, 내 물고기가 도망하지 않게 되면서 F-10~F-13 테스트가 **놀라지 않는 물고기를 보는
  빈 테스트**가 될 뻔했다. 워밍업 뒤 플래그를 끄도록 고쳤다(그 결과 `FleeDoesNotSlide`의 표본이
  줄어, 기준을 낮추는 대신 **관찰 구간을 180 → 240프레임으로 늘렸다**).

### 남은 것 / 별도 태스크로 넘긴 것

- **화면 흔들림·물 왜곡·난류 자국**(대상 나이 변경으로 새로 요구된 연출)은 이 태스크에서
  구현하지 않았다. 카메라·포스트프로세스 작업이라 Task 10의 범위 밖이고, 별도 태스크가 맞다.
- **반응음의 톤**(「꺅」 → 「퍽」·「쿵」 쪽으로 낮고 짧게)도 그대로다. `make_sounds.py`를 다시
  합성하고 재임포트하는 일이라 Task 1의 영역이다.
- **기포 머티리얼**이 "귀여운 방울"로 읽히는지는 헤드리스로 판정할 수 없다. 실제 실행 화면에서
  사용자 확인이 필요하다.

---

## 이 계획이 하지 않는 것

시나리오가 **2차 또는 범위 밖**으로 명시한 것들이다. 구현 중에 "겸사겸사" 넣지 않는다.

- **뽑기 연출**(2차) — 1차가 나가 아이가 여러 번 들락거리기 시작한 뒤에 가장 크게 먹힌다
- **그림 말풍선**(2차) — 지금 넣으면 같은 말을 두 번 하는 것이고 아이는 아예 안 본다
- **잠수부**(2차, 선택)
- **둘이 같이(옵션 D)** — 1차 묶음 **다음** 순서다. 싸다고 앞으로 당기지 않는다
- **낚시꾼·점수·타이머·실패 화면·먹이 자원 개념** — 영구 금지
- **Niagara·MetaSound·SoundCue** — 스크립트로 만들 수 없는 에셋은 쓰지 않는다
